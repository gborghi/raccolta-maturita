---
title: 2023 Straordinaria — Problema 2 — Problema 1 — Svolgimento
tipo: soluzione
item_id: sol_prob_2023_sessione_straordinaria_2023_problema2_53_1
of_item: prob_2023_sessione_straordinaria_2023_problema2_53_1
prova_id: prova_2023_sessione_straordinaria_2023_problema2_53
anno: '2023'
pdf: Prova_Maturita_2023.pdf
cluster: Studio di Funzione
tags:
  - maturita/soluzione
  - area/analisi
  - cluster/studio_di_funzione
  - anno/2023
---

**Problema:** [[Problemi/prob_2023_sessione_straordinaria_2023_problema2_53_1|2023 Straordinaria — Problema 2 — Problema 1]] · **Prova:** [[Prove/2023_sessione_straordinaria_2023_problema2_53|2023 Straordinaria — Problema 2]]

Sessione Straordinaria 2023 - Problema $2$

LICEO SCIENTIFICO STRAORDINARIA 2023 - PROBLEMA $2$

Si considerino le famiglie di funzioni fa(x) =
$2 (eax-e-ax)$ e ga(x) =
$2 (eax+ e-ax)$ con a parametro
reale positivo.

a)

Si traccino, al variare del parametro, i grafici rappresentativi $\gamma f$ e $\gamma g$ delle funzioni fa(x) e ga(x)
evidenziando simmetrie, estremi e flessi.

$y=$ fa(x) $= 1$
$2(eax-e-ax),$
con $a> 0$

(N.B. La funzione in questione è il seno iperbolico di $(ax): y=$ sin h(ax) ed il grafico è del tipo (con $a= 1/2)):$

Studiamo comunque la funzione.

E’ definita su tutto $R$ ed è sempre continua e derivabile. Si tratta di una funzione dispari perché
$f(-x) =$
$2 (e-ax-eax) = -$
$2 (eax-e-ax) = -f(x):$ il grafico è simmetrico rispetto all’origine degli
assi cartesiani.

Intersezioni con gli assi:

Se $x= 0 , y= 0.$ Se $y= 0,$
$2 (eax-e-ax) = 0: eax= e-ax , ax= -ax , 2ax= 0, x= 0.$

Segno della funzione:

$2 (eax-e-ax) > 0,$
$eax> e-ax,$
$ax> -ax>, 2ax> 0$ per $x> 0$ (ricordiamo che $a> 0):$

quindi la funzione è positiva per $x> 0$ e negativa per $x< 0.$

Limiti:

$$ lim $$
$x\to +\infty$
$2 (eax-e-ax) = +\infty$ ,
$$ lim $$
$x\to -\infty$
$2 (eax-e-ax) = -\infty$

Sessione Straordinaria 2023 - Problema $2$

Non ci sono asintoti verticali né orizzontali. Verifichiamo che non ci sono neanche asintoti obliqui:

$$ lim $$
$x\to +\infty$
$2 (eax-e-ax) ∙1$
$x=$ lim
$x\to +\infty$
$2$ (eax) $∙1$
$x= +\infty$ ( per ogni $a> 0).$

$N. B.$ eax è infinito di ordine superiore rispetto $ad x.$

In base alla simmetria già evidenziata non può esserci asintoto obliquo neanche per $x\to -\infty$ .

Studio derivata prima:

$y' =$
$2 (aeax+ ae-ax> 0$ per ogni $x:$ la funzione è sempre crescente. Non ci sono estremanti.

Studio derivata seconda:

y'' $= 1$
$2 (a2eax-a2e-ax) \ge 0$ se $eax\ge e-ax: ax\ge -ax$ ,
$2ax\ge 0,$
$x\ge 0.$

Pertanto il grafico della funzione volge la concavità verso l’alto se $x> 0$ , verso il basso se $x< 0$ ed ha
pertanto in $x= 0$ un punto di flesso: $F= (0; 0).$

La tangente nel punto di flesso ha coefficiente angolare $f'(0) = a> 0.$

Il grafico è simile a quello già indicato, in cui per comodità abbiamo posto $a=$
$$ 1 2 . $$

Studiamo la seconda funzione:

$y=$ ga(x) $= 1$
$2(eax+ e-ax),$
con $a> 0$

(N.B. La funzione in questione è il coseno iperbolico di $(ax): y=$ cosh(ax) , detta anche “catenaria” (vedi
approfondimento su Wikipedia: Catenaria - Wikipedia) ed il grafico è del tipo (con $a= 1/2)):$

Studiamo la funzione.

E’ definita su tutto $R$ ed è sempre continua e derivabile. Si tratta di una funzione pari perché
$g(-x) =$
$2 (e-ax+$ eax) =
$2 (eax+ e-ax) =$ g(x): il grafico è simmetrico rispetto all’asse delle $y.$

Intersezioni con gli assi:

Sessione Straordinaria 2023 - Problema $2$

Se $x= 0 , y= 1$ per ogni valore di a. Se $y= 0,$
$2 (eax+ e-ax) = 0:$ mai.

Non ci sono intersezioni con l'asse delle $x.$

Segno della funzione:

$2 (eax+ e-ax) > 0,$ per ogni $x:$ quindi la funzione è sempre positiva.

Limiti:

$$ lim $$
$x\to \pm \infty$
$2 (eax+ e-ax) = +\infty$

Non ci sono asintoti verticali né orizzontali. Verifichiamo che non ci sono neanche asintoti obliqui:

$$ lim $$
$x\to +\infty$
$2 (eax+ e-ax)$ ∙
$x=$ lim
$x\to +\infty$
$2$ (eax) ∙
$x= +\infty$ ( per ogni $a> 0,$ eax è infinito di ordine superiore

rispetto $ad x).$

In base alla simmetria già evidenziata non può esserci asintoto obliquo neanche per $x\to -\infty$ .

Studio derivata prima:

$y' =$
$2 (aeax-ae-ax\ge 0$ se $eax\ge e-ax, ax\ge -ax, 2ax\ge 0, x\ge 0:$ la funzione è crescente per $x> 0$
e decrescente per $x< 0$ ed ha quindi in $x= 0$ un punto di minimo relativo (e assoluto), con ordinata
uguale $ad 1$ per ogni valore di a.

Studio derivata seconda:

y'' =
$2 (a2eax+ a2e-ax) > 0$ per ogni $x:$ pertanto il grafico della funzione volge la concavità sempre
verso l’alto, quindi non ci sono flessi.

Il grafico è simile a quello già indicato, in cui per comodità abbiamo posto $a=$
$$ 1 2 . $$

$b)$
Siano $P$ e $Q$ due punti, rispettivamente su $\gamma f$ e $\gamma g$ , aventi la stessa ascissa positiva, P’ e Q’ le loro
proiezioni sull’asse delle ordinate. Si individui il valore del parametro a in corrispondenza del quale la
massima area del rettangolo PQQ'P' vale $e-1.$

Indicata con $x> 0$ l’ascissa di $P$ e $Q,$ le loro ordinate sono
rispettivamente: $yP =$
$2$ (eax $-e-ax)$ e $yQ =$
$2$ (eax $+ e-ax)$
L’area del rettangolo PQQ'P' è data da:
$A=$ PP' $∙PQ= x|yQ -yP|.$
Osserviamo che risulta sempre $yQ > yP,$ infatti:
$2$ (eax $+ e-ax) >$
$2$ (eax $-e-ax),$ eax $+ e-ax >$ eax $-e-ax,$

Sessione Straordinaria 2023 - Problema $2$

$e-ax > -e-ax , 2e-ax> 0$ per ogni $x.$ Quindi:
$A=$ x(yQ $-yP) = x(1$
$2$ (eax $+ e-ax) -1$
$2$ (eax $-e-ax)) = x(e-ax),$
con $x> 0$ .
Osserviamo che la funzione area è sempre continua e derivabile nel suo dominio.
Risulta:
$A' = e-ax-axe-ax\ge 0$ se $1 -ax\ge 0, x\le 1$
a (ricordiamo che $a> 0).$
Essendo $x> 0$ la funzione è crescente per $0 < x<$
a e decrescente per $x>$
a : $x=$
a è punto di massimo
relativo e assoluto.
$A(1$
a) $= 1$
$ae-1 = e-1$ se $a= 1.$

D’ora in avanti, si assuma $a= 1.$

$c)$
Verificare l’identità $g^{2}(x) -f^{2}(x) = 1$ e determinare il numero intero per cui
$50 \le$ g(x) $-f(x) \le 100.$ Specificare quale, tra f(x) e g(x), è una funzione invertibile in $R$ e ricavare
l’espressione analitica della funzione inversa.

Verifichiamo che $g^{2}(x) -f^{2}(x) = 1.$

N.B. Si tratta di una nota proprietà che lega il seno iperbolico al coseno iperbolico dello stesso angolo:

$Ch2 x -Sh2x = 1$

Verifichiamola direttamente:
$g^{2}(x) -f^{2}(x) = (1$
$2 (ex+ e-x))$
$-(1$
$2 (ex-e-x))$
$$ 2 = 1 $$
$4 [(e2x+ 2 + e-2x) -(e2x-2 + e-2x)] =$
$4 [4] = 1 c. v. d.$

Veniamo alla seconda domanda del quesito.

$50 \le$ g(x) $-f(x) \le 100, 50 \le$
$2 (ex+ e-x) -$
$2 (ex-e-x) \le 100 ; 50 \le e-x\le 100.$
Valutiamo $e-x,$ con $x$ che deve essere chiaramente negativo.

Per $x=-3$ abbiamo $e^{3} \cong 20.1,$ per $x=-4$ abbiamo $e^{4} \cong 54.6;$ per $x=-5$ abbiamo $e^{5} = 148.41.$

Il numero intero per cui $50 \le$ g(x) $-f(x) \le 100$ è quindi $x= -4.$

Rispondiamo all’ultima richiesta del quesito. Dai grafici delle due funzioni si deduce chiaramente che
la funzione invertibile è quella che corrisponde al seno iperbolico (strettamente monotona), quindi
tra $f$ e $g$ la funzione invertibile è f(x) =
$2 (ex-e-x).$
Ricaviamo l’espressione analitica della sua inversa:

Sessione Straordinaria 2023 - Problema $2$

$y= 1$
$2 (ex-e-x); 2y= (ex-1$
$ex) = e2x-1$
$$ ex $$
; $e2x-2yex-1 = 0 ; ex= y\pm \sqrt{y}^{2} + 1$

La soluzione accettabile e $ex= y+ \sqrt{y}^{2} + 1$ , perche $ex$ e sempre positiva e la soluzione
$ex= y- \sqrt{y}^{2} + 1$ e negativa per ogni $y,$ infatti: $y-\sqrt{y}^{2} + 1 < 0$ se $\sqrt{y}^{2} + 1 > y$ e questa
disequazione e sempre verificata (se $y< 0$ verificata, se $y\ge 0$ elevando al quadrato:
$y^{2} + 1 > y^{2}$ sempre).

Quindi:

$ex= y+ \sqrt{y}^{2} + 1; x= ln(y+ \sqrt{y}^{2} + 1) = f-1$

(N.B. Questa funzione è l’arcoseno iperbolico, che si indica con arcsinh).

La funzione inversa di $y=$
$2 (ex-e-x)$ è la funzione $y= ln(x+ \sqrt{x}^{2} + 1).$

$d)$

Determinare l’equazione $y=$ P(x) della parabola $\gamma$ avente il vertice nel punto di minimo assoluto della
funzione g(x) e retta tangente, per $x= 1,$ parallela alla retta di equazione $2x+ y= 0.$ Calcolare l’area della
regione finita $R$ delimitata da $\gamma$ , dal grafico di g(x) e dalle rette di equazione $x= \pm 1.$ Verificare che l’area di
$R$ può essere approssimata con quella del triangolo isoscele inscritto nel segmento parabolico delimitato da $\gamma$ e
dall’asse delle ascisse.

$y=$ P(x): parabola $\gamma$ con vertice $V= (0; 1),$ asse parallelo all’asse $y,$ tangente in
$x= 1$ parallela a $2x+ y= 0$ (che ha coefficiente anhgolare $m= -2)$
La parabola è del tipo: $y= ax2 + bx+ c.$
La parabola di dato vertice $V= (xV; yV)$ può essere scritta nella forma:
$y-yV= a(x-xV)2.$
Nel nostro vaso: $y-1 = ax2, y= ax2 + 1.$ La tangente in $x=1$ deve avere coefficiente angolare
$-2$ (quello della retta data), quindi $y'(1) = -2.$ Ma y'(x) $= 2ax,$ quindi $y'(1) = -2 = 2a,$
$a= -1.$
La parabola richiesta ha equazione: $\gamma : y= -x^{2} + 1.$

Sessione Straordinaria 2023 - Problema $2$

Rappresentiamo graficamente il grafico della parabola, della funzione $g,$ della regione $R$ e del
triangolo:

Area(R) $= 2 \int$ [
$2 (ex+ e-x) -(-x^{2} + 1)]$
$$ 1 0 $$
$dx= 2 [1$
$2 ex-1$
$2 e-x+ 1$
$3 x^{2} -x]$
$$ 0 1 = = 2 [1 $$
$2 e-1$
$2 e-1 + 1$
$3 -1 -(1$
$2 -1$
$2)] = e-e-1 -4$
$3 \cong 1.02 =$ Area(R) $\cong 1$
L’area del triangolo ABV vale:
$$ 1 2 (2)(1) = 1. $$

Quindi : Area(R) $\cong$ Area(triangolo).

Con la collaborazione di Angela Santamaria

*Fonte:* [📄 PDF p.53](https://drive.google.com/file/d/1SlPvapgs-eNHrpZYtGGUXCPHVcacwdKR/view)


#maturita/soluzione #area/analisi #cluster/studio_di_funzione
