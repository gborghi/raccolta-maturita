---
title: 2012 Comunicazione Ordinaria — Problema 1 — Problema 1 — Svolgimento
tipo: soluzione
item_id: sol_prob_2012_liceo_della_comunicazione_2012_sessione_ordinaria_problema1_42_1
of_item: prob_2012_liceo_della_comunicazione_2012_sessione_ordinaria_problema1_42_1
prova_id: prova_2012_liceo_della_comunicazione_2012_sessione_ordinaria_problema1_42
anno: '2012'
pdf: Prova_Maturita_2012.pdf
cluster: Studio di Funzione
tags:
  - maturita/soluzione
  - area/analisi
  - cluster/studio_di_funzione
  - anno/2012
---

**Problema:** [[Problemi/prob_2012_liceo_della_comunicazione_2012_sessione_ordinaria_problema1_42_1|2012 Comunicazione Ordinaria — Problema 1 — Problema 1]] · **Prova:** [[Prove/2012_liceo_della_comunicazione_2012_sessione_ordinaria_problema1_42|2012 Comunicazione Ordinaria — Problema 1]]

Liceo della comunicazione 2012
Sessione $Ordinaria-$ Problema $1$

Liceo della comunicazione 2012 - PROBLEMA $1$

Sia f(x) $= \sqrt{ln2}(-x) -ln x^{2} + 1$ e sia g(x) $= \sqrt{ln2}(x) -ln x^{2} + 1$

Si determinino i domini di $f$ e di $g.$

f(x) $= \sqrt{ln2}(-x) -ln x^{2} + 1$

$-x> 0$
$ln2(-x) -ln x^{2} + 1 \ge 0$ ; {
$x< 0$
$ln2(-x) -2ln |x| + 1 \ge 0$ ; {
$x< 0$
$ln2(-x) -2ln(-x) + 1 \ge 0$

$ln2(-x) -2ln(-x) + 1 \ge 0 ; (ln(-x) -1)^{2} \ge 0$ :per ogni $x$ .

Il dominio di f(x) è quindi: $-\infty < x< 0$ .

g(x) $= \sqrt{ln2}(x) -ln x^{2} + 1$

$x> 0$
$ln2(x) -ln x^{2} + 1 \ge 0$ ; {
$x> 0$
$ln2(x) -2ln |x| + 1 \ge 0$ ; {
$x> 0$
$ln2(x) -2ln(x) + 1 \ge 0$

$ln2(x) -2ln(x) + 1 \ge 0$ ; (ln(x) $-1)^{2} \ge 0$ :per ogni $x$ .

Il dominio di g(x) è quindi: $0 < x< +\infty$ .

Si disegnino, nel medesimo sistema di assi cartesiani ortogonali Oxy, i grafici di $f$ e di $g.$

f(x) $= \sqrt{ln2}(-x) -ln x^{2} + 1 = \sqrt{ln2}(-x) -2ln(-x) + 1 = \sqrt (ln(-x) -1)^{2} = |ln(-x) -1|$

Il grafico di questa funzione si deduce da quello di $y=ln(x)$ mediante le seguenti
trasformazioni geometriche:
$y=$ ln(x) = a(x);
$y= ln(-x) =$ b(x): simmetria rispetto all'assey;
$y= ln(-x) -1 =$ c(x) :traslazione verso il basso di $1;$
$|ln(-x) -1| =$ f(x): ribaltamento rispetto all'asse $x$ della parte negativa.

Liceo della comunicazione 2012
Sessione $Ordinaria-$ Problema $1$

g(x) $= \sqrt{ln2}(x) -ln x^{2} + 1 = \sqrt$ (ln(x) $-1)^{2} = |ln(x) -1|$

Il grafico di questa funzione si deduce da quello di $y=ln(x)$ mediante le seguenti
trasformazioni geometriche:
$y=$ ln(x) = a(x);
$y=$ ln(x) $-1 =$ b(x) :traslazione verso il basso di $1;$
$|ln(x) -1| =$ g(x): ribaltamento rispetto all'assex della parte negativa.

Osserviamo che risulta: $g(x)=f(-x),$ quindi il grafico di g(x) è simmetrico rispetto all’asse $y$
del grafico di f(x).

Rappresentiamo le due funzioni nello stesso piano cartesiano:

Liceo della comunicazione 2012
Sessione $Ordinaria-$ Problema $1$

Si determinino, se esistono, le coordinate degli eventuali punti di discontinuità o di non
derivabilità di $f$ e di $g$ rispettivamente.

Analizziamo la funzione g(x) $= \sqrt{ln2}(x) -ln x^{2} + 1 = |ln(x) -1|$
Questa funzione è continua nel suo dominio $(x>0)$ e derivabile per ogni $x\ne$ e , dove c’è
un punto angoloso. Determiniamo la derivata destra e la derivata sinistra in $x=e.$
Se $x>e: y=ln(x)-1,$ quindi $y' =$
$x , y+$
' (e) =
$$ 1 e $$
Se $0<x<e: y=-ln(x)+1,$ quindi $y' = -$
$x , y-$
' (e) = -
$$ 1 e $$
In modo analogo si ragiona per f(x), che, come già notato è simmetrica di g(x) rispetto
all’asse $y,$ quindi ha un punto angoloso in $x=-e.$

Si calcoli l’area compresa tra g(x) e l’asse $x$ per $e\le x\le 2e.$

g(x) $= |ln(x) -1| =$ ln(x) $-1$ se $x>$ e

L’area richiesta si ottiene mediante il seguente calcolo integrale:

$Area= \int$
(ln(x) $-1)$
$2e$
$dx=$

Calcoliamo per parti $\int$ ln(x) $dx$ .

$\int$ ln(x) $dx= \int$ (x)'ln(x) $dx=$ xln(x) $-\int$ x∙
$xdx=$ xln(x) $-x+ K;$ quindi:

$$ \int $$
(ln(x) $-1)$
$2e$
$dx=$ [xln(x) $-x-x]e$
$2e=$ [xln(x) $-2x]e$
$2e= 2e(ln2e) -4e-(e-2e) =$

$= 2e(ln2 +$ lne) $-3e= 2e(ln2) + 2e-3e= 2e(ln2) -e= e(ln4 -1) \cong 1.05 u^{2} =$ Area

Con la collaborazione di Angela Santamaria

*Fonte:* [📄 PDF p.42](https://drive.google.com/file/d/15uTp-FXyM3r6gZHrCu6HThex3dLEYCIU/view)


#maturita/soluzione #area/analisi #cluster/studio_di_funzione
