---
title: 2003 Suppletiva PNI — Problema 1 — Problema 1 — Svolgimento
tipo: soluzione
item_id: sol_prob_2003_suppletiva_pni_2003_problema1_122_1
of_item: prob_2003_suppletiva_pni_2003_problema1_122_1
prova_id: prova_2003_suppletiva_pni_2003_problema1_122
anno: '2003'
pdf: Prova_Maturita_2003.pdf
cluster: Studio di Funzione
tags:
  - maturita/soluzione
  - area/analisi
  - cluster/studio_di_funzione
  - anno/2003
---

**Problema:** [[Problemi/prob_2003_suppletiva_pni_2003_problema1_122_1|2003 Suppletiva PNI — Problema 1 — Problema 1]] · **Prova:** [[Prove/2003_suppletiva_pni_2003_problema1_122|2003 Suppletiva PNI — Problema 1]]

Suppletiva PNI 2003 - Problema $1$

PNI 2003 SESSIONE SUPPLETIVA - PROBLEMA $1$

In un piano, riferito a un sistema di assi cartesiani ortogonali (Oxy), sono
assegnate le parabole di equazione:

$y= (a-1)x^{2}-2ax+ a^{2}$

dove a è un parametro reale diverso da $1.$

a)

Determinare quali tra esse hanno punti in comune con l’asse $x$ e quali no.

Intersechiamo la generica parabola con l’asse $x:$

${y= (a-1)x^{2} -2ax+ a^{2}$
$y= 0$
$\implies (a-1)x^{2} -2ax+ a^{2} = 0 ; \Delta$
$4 = a^{2} -a^{2}(a-1) \ge 0$

$a^{2}(1 -a+ 1) = a^{2}(2 -a) \ge 0$ se $2 -a\ge 0 , a\le 2$

Le parabole hanno punti in comune con l’asse $x$ se $a\le 2$ , con $a\ne 1$ .

$b)$

Trovare le due parabole che hanno il vertice in un punto di ascissa a.

Calcolare l’ascissa del vertice della generica parabola:

$y= (a-1)x^{2} -2ax+ a^{2} ; xV= -b$
$2a=$
$2a$
$2(a-1) =$
$a-1 =$ a se $a= a(a-1):$
$a= 0$ oppure $a= 2$

Le due parabole con il vertice di ascissa a sono:

$a= 0: y= -x^{2} ; a= 2: y= x^{2} -4x+ 4$

Suppletiva PNI 2003 - Problema $1$

$c)$

Stabilire se le due parabole trovate sono congruenti o no, fornendo un’esauriente
spiegazione della risposta.

Le parabole di equazioni: $y= -x^{2}$ e $y= x^{2} -4x+ 4$ sono congruenti poiché hanno i
coefficienti del termine di secondo grado uguali in valore assoluto; in tal caso, infatti, si
può passare da una parabola all’altra mediante una traslazione (che porta il vertice di una
nel vertice dell’altra) ed eventualmente una simmetria assiale (se i coefficienti del termine
di secondo grado sono opposti). Oppure si può operare una simmetria con centro il punto
medio del segmento che congiunge i due vertici.
Nel nostro caso si può passare dalla parabola di equazione $y= -x^{2}$ alla parabola di
equazione $y= x^{2} -4x+ 4$ eseguendo:
una simmetria rispetto all’asse $x:$ otteniamo così $y= x^{2};$
una traslazione che porta il vertice di questa, che è $(0;0),$ nel vertice della seconda
parabole, che è $(2;0).$ Tale traslazione ha equazioni:
${X= x+ 2$
$Y= y$
; ${x= X-2$
$y= Y$
; la parabola $y= x^{2}$ diventa:

$Y= (X-2)^{2} = X^{2} -4X+ 4$ , che coincide con la seconda parabola.

Allo stesso risultato si perviene se applichiamo alla parabola $y= x^{2}$ la simmetrica di
centro $(1;0),$ che è il punto medio del segmento che congiunge i due vertici; tale simme
tria ha equazioni:

${X= 2a-x= 2 -x$
$Y= 2b-y= -y ; {x= 2 -X$
$y= -Y$ ; quindi $y= -x^{2}$ diventa $-Y= -(2 -X)2$ cioè:

$Y= X^{2} -4X+ 4$ che equivale alla seconda parabola.

Rappresentiamo graficamente le due parabole per evidenziare meglio quanto detto:

Suppletiva PNI 2003 - Problema $1$

$d)$

Scrivere l’equazione del luogo geometrico $L$ dei vertici delle parabole assegnate e
disegnarne l’andamento dopo averne determinato in particolare asintoti, estremi e
flessi.

$y= (a-1)x^{2} -2ax+ a^{2} ; xV= -b$
$2a=$
$2a$
$2(a-1) =$
$a-1 ; yV= -\Delta$
$4a= -a^{2}(2 -a)$
$(a-1)$

Quindi il luogo $ei$ vertici ha equazioni parametriche:

$x=$
$a-1$
$y=$
$a^{2}(a-2)$
$a-1$
con $a\ne 1$ ; eliminiamo il parametro per trovare l'equazione cartesiana.

$(a-1)x=$ a
$y= a^{2}(a-2)$
$a-1$
; ${a=$
$x-1$
$y=$ ⋯
; $x\ne 1 \implies y=$
$x-1)$
$$ 2 ( x $$
$x-1 -2)$
$x-1 -1$

$y=$
$x-1)$
$(x-2x+ 2$
$x-1$
$x-x+ 1$
$x-1$
$x^{2}(2 -x)$
$(x-1)^{3}$
$x-1$
$= x^{2}(2 -x)$
$(x-1)^{2} = y$ (con $x\ne 1):$ luogo dei vertici $L$ .

Dobbiamo studiare la seguente funzione:

$y=$ f(x) $= x^{2}(2-x)$
$(x-1)^{2}$

Dominio: $x\ne 1 , -\infty < x< 1 \cup 1 < x< +\infty$

Intersezioni con gli assi:

Se $x=0: y=0$ ; se $y=0: x=0$ e $x=2.$

Simmetrie notevoli: visto il dominio, la funzione non può essere pari né dispari.

Limiti:

$limx\to \mp \infty$
$x^{2}(2-x)$
$(x-1)^{2} = limx\to \mp \infty$
$-x^{3}$
$x^{2} = \pm \infty$ (può esserci asintoto obliquo, che effettivamente
c’è, poiché si tratta di una funzione razionale fratta il cui grado del numeratore supera di $1$
il grado del denominatore).

$limx\to 1\mp$
$x^{2}(2-x)$
$(x-1)^{2} = + \infty : x=1$ è asintoto verticale destro e sinistro.

Suppletiva PNI 2003 - Problema $1$

Cerchiamo l’asintoto obliquo:

$m=$ lim
$x\to \mp \infty$
$x^{2}(2 -x)$
$(x-1)^{2} ∙1$
$x=$ lim
$x\to \mp \infty$
$-x^{3}$
$x^{3} = -1$

$q=$ lim
$x\to \mp \infty [x^{2}(2 -x)$
$(x-1)^{2} -mx] =$ lim
$x\to \mp \infty [x^{2}(2 -x)$
$(x-1)^{2} +$ x] = lim
$x\to \mp \infty [2x^{2} -x^{3} + x^{3} -2x^{2} + x$
$(x-1)^{2}$

$= limx\to \mp \infty$ [
$(x-1)^{2}] = 0$ : asintoto obliquo $y= -x$ .

Eventuali intersezioni con l’asintoto obliquo:

$y= -x$
$y= x^{2}(2 -x)$
$(x-1)^{2}$
$\implies x^{2}(2 -x)$
$(x-1)^{2} = -x$ da cui $x= 0 x(2 -x)$
$(x-1)^{2} = -1$ ;

$2x-x^{2} = -x^{2} + 2x-1$ : mai verificato.

L’asintoto obliquo interseca quindi il grafico della funzione nel punto $(0;0).$

Derivata prima:

$y' = -x(x^{2} - 3 x + 4)$
$(x -1)^{3}$
$\ge 0$ se -
$x-1 \ge 0 (x^{2} -3x+ 4 > 0$ per ogni $x$ perchè $\Delta < 0)$

Quindi $y' \ge 0$ se
$x-1 \le 0 : 0 \le x< 1$
La funzione è quindi crescente se $0 \le x< 1$ e decrescente se $x< 0$ e $x> 1$ :

$x= 0$ è punto di minimo relativo, con ordinata $y= f(0) = 0.$

Derivata seconda:

y'' =
$2(x + 2)$
$(x-1)^{4} \ge 0$ se $x\ge -2$ (con $x\ne 1);$ pertanto:

il grafico della funzione volge la concavità verso l’alto se $x> -2$ (con $x\ne 1)$ e verso il
basso se $x< -2 = x=-2$ è punto di flesso, con ordinata: $f(-2) =$
$$ 16 $$
$9 ; F= (-2;$
$$ 16 9 ). $$

La funzione ha il seguente grafico:

Suppletiva PNI 2003 - Problema $1$

e)

Calcolare l’area della regione finita di piano delimitata dalla curva $L$ e dalla retta di
equazione $y=$
$$ 3 2 . $$

Cerchiamo le intersezioni tra la retta e la curva:

$y= 3$
$y= x^{2}(2 -x)$
$(x-1)^{2}$
$\implies x^{2}(2 -x)$
$(x-1)^{2} = 3$
$2x^{2}(2 -x) = 3(x-1)^{2}$ ,

$2x^{3} -x^{2} -6x+ 3 = 0 , (2 x -1)(x^{2} -3) = 0 : x= 1$
$2$ e $x= \pm \sqrt{3}$

Suppletiva PNI 2003 - Problema $1$

Le intersezione che ci interessano sono:

$A= (-\sqrt{3}: 3$
$2)$ e $B= (1$
$$ 2 ; 3 2) $$

L’area richiesta si ottiene calcolando il seguente integrale:

$Area= \int$
$2 -x^{2}(2 -x)$
$(x-1)^{2}$ ] $dx= \int$
$(2x^{3} -x^{2} -6x+ 3$
$2(x^{2} -2x+ 1) ) dx=$
$$ 1 2 $$
$-\sqrt{3}$
$$ 1 2 $$
$-\sqrt{3}$

$$ = 1 2 \int $$
$(2x^{3} -x^{2} -6x+ 3$
$x^{2} -2x+ 1$
) $dx$
$$ 1 2 $$
$-\sqrt{3}$

Eseguendo la divisione tra il numeratore e il denominatore otteniamo:

$2x^{3} -x^{2} -6x+ 3 = (x^{2} -2x+ 1)(2x+ 3) -2x;$ quindi:

$$ 1 2 \int $$
$(2x^{3} -x^{2} -6x+ 3$
$x^{2} -2x+ 1$
) $dx$
$$ 1 2 $$
$-\sqrt{3}$
$$ = 1 2 \int $$
$(2x+ 3 -$
$2x$
$x^{2} -2x+ 1) dx$
$$ 1 2 $$
$-\sqrt{3}$

$2x$
$x^{2}-2x+1 =$
$(x-1)^{2} +$
$x-1 =$
$a+b(x-1)$
$x^{2}-2x+1$ , da cui: $2x= bx+ a-b$ e per il principio di identità

dei polinomi si ha: $b=2 a-b=0;$ quindi: $b=2$ e $a=2.$ Ritornando all’integrale:

$$ 1 2 \int $$
$(2x+ 3 -$
$(x-1)^{2} -$
$x-1) dx= 1$
$2 [x^{2} + 3x+$
$x-1 -2ln|x-1|]$
$-\sqrt{3}$
$$ 1 2 = 1 2 $$
$-\sqrt{3}$

$$ = 1 2 [1 4 + 3 $$
$2 -4 -2 ln (1$
$2) -(3 -3\sqrt{3} +$
$-\sqrt{3} -1$
$-2 ln(\sqrt{3} + 1))] =$
$= [ln(\sqrt{3} + 1) + ln(2) + 2\sqrt{3} -25$
$8$ ] $u^{2} \cong 2.04 u^{2} =$ Area

L’area della regione richiesta è:

$Area= [ln(\sqrt{3} + 1) + ln(2) + 2\sqrt{3} -25$
$8$ ] $u^{2} \cong 2.04 u^{2}$

Con la collaborazione di Angela Santamaria

*Fonte:* [📄 PDF p.122](https://drive.google.com/file/d/1n_07H2pKPIZ0p5d9GN7HvXoIlCFb8amD/view)


#maturita/soluzione #area/analisi #cluster/studio_di_funzione
