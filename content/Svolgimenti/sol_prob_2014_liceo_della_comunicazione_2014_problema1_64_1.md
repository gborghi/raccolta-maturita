---
title: 2014 Liceo Comunicazione — Problema 1 — Problema 1 — Svolgimento
tipo: soluzione
item_id: sol_prob_2014_liceo_della_comunicazione_2014_problema1_64_1
of_item: prob_2014_liceo_della_comunicazione_2014_problema1_64_1
prova_id: prova_2014_liceo_della_comunicazione_2014_problema1_64
anno: '2014'
pdf: Prova_Maturita_2014.pdf
cluster: Studio di Funzione
tags:
  - maturita/soluzione
  - area/analisi
  - cluster/studio_di_funzione
  - anno/2014
---

**Problema:** [[Problemi/prob_2014_liceo_della_comunicazione_2014_problema1_64_1|2014 Liceo Comunicazione — Problema 1 — Problema 1]] · **Prova:** [[Prove/2014_liceo_della_comunicazione_2014_problema1_64|2014 Liceo Comunicazione — Problema 1]]

LICEO DELLA COMUNICAZIONE 2014 - PROBLEMA $1$

f(x) $= x\sqrt{4}-x$

Anche se non richiesto esplicitamente, facciamo
lo studio completo della funzione.

Dominio: $4 -x \ge 0 \implies - \le x\le$
Simmetrie notevoli: $f(-x)=-f(x),$ quindi la
funzione è dispari (grafico simmetrico rispetto
all’origine).

Intersezioni con gli assi cartesiani:

$x=0, y=0$
$y=0, x=0, x=2, x=-2$

Segno della funzione: f(x) $\ge 0$ se $x\ge 0$ (nel dominio).

Limiti: la funzione è continua in un intervallo chiuso e limitato.

Derivata prima:

La funzione non è derivabile in $x= -2$ ed in $x=2.$
Il limite per $x$ che tende a $(-2)+$ oppure a $2-$ della derivata è - infinito, quindi avremo
tangenti verticali in $x= -2$ ed in $x= 2$

$f (x) \ge 0$ se $x -2 \le 0 \implies -\sqrt \le x\le \sqrt$

Quindi la funzione è crescente in tale intervallo e decrescente negli intervalli

$-2 \le x< -\sqrt{2}$ e $\sqrt{2} < x\le 2$

Avremo quindi un minimo (assoluto) in $m= (-\sqrt ; -$ ) ed un massimo (assoluto) nel
punto $M=(\sqrt$ ; )

Derivata seconda:

$f (x) \ge 0$ se $2x(x -6) \ge 0 \implies$
(fuori dominio),

Quindi il grafico ha la concavità verso l’alto nell’intervallo
, verso il basso nella
parte rimanente del dominio. Flesso nel punto $(0;0)$ con tangente inflessionale di

coefficiente angolare $f’(0)=2$

Il grafico della funzione è pertanto il seguente:

Come detto nel punto $1)$ la funzione è dispari, quindi il suo grafico è simmetrico
rispetto all’origine.

f(x) $= x\sqrt{4}-x$

$f’(0)=2,$ quindi, detto $\alpha$ l’angolo formato dalla tangente nell’origine con la direzione
positiva dell’asse $x,$ risulta:

$tg \alpha = 2,$ da cui $\alpha = arctg(2) \cong 63^\circ 26'$

Consideriamo la curva di equazione $y = x (4-x$ )
Posto $4 -x \ge 0,$ cioè $-2 \le x\le 2,$ otteniamo:

$y= \pm |x|\sqrt{4} -x =$ {
$\pm x\sqrt{4} -x$ se $0 \le x\le 2$
$\mp x\sqrt{4} -x$ se $-2 \le x< 0$

Il grafico è quindi quello della f(x) unito al grafico di - f(x)

L’area richiesta è data da:

$Area= 4 ∙\int f(x)dx=$

$4 ∙\int x\sqrt{4} -x dx=$

$-2 ∙\int -2x\sqrt{4} -x dx= -2 ∙[2$
$3 (4 -x$ )

$= -4$
$3 (0 -8) = 32$
$3 u \cong 10.67 u$

Si consideri il solido $W$ che la regione delimitata da  e dall’asse $x$ genera nella rotazione
attorno all’asse $x$ . Si calcoli il volume di $W.$

f(x) $= x\sqrt{4}-x$

Il volume di $W$ si ottiene calcolando il seguente integrale:

V(W) $= 2 (\pi \int f$ (x)dx

$= 2\pi \int x (4 -x )dx$

$= 2\pi \int (4x -x )dx=$

$= 2\pi [4$
$3 x -1$
$5 x$ ]

$= 2\pi (32$
$3 -32$
$$ 5 ) = 128 $$
$15 \pi \cong 26.808 u$

Con la collaborazione di Angela Santamaria, Simona Scoleri e Stefano Scoleri

*Fonte:* [📄 PDF p.64](https://drive.google.com/file/d/1WH26K6aDhT0rm439hBww1SX_Dwrvky9g/view)


#maturita/soluzione #area/analisi #cluster/studio_di_funzione
