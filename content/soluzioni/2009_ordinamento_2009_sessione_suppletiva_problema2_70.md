---
tipo: soluzione
prova_stem: 2009_ordinamento_2009_sessione_suppletiva_problema2_70
pdf: Prova_Maturita_2009.pdf
source: text
title: 2009 Ordinamento Suppletiva — Problema 2 — Svolgimento
---


ORDINAMENTO 2009 SESSIONE SUPPLETIVA - PROBLEMA $2$

Sia data la funzione:

f(x) $= {x(2 -lnx)$ , per $x> 0$
per $x= 0$

Questa funzione è continua nel punto di ascissa $0?$ E’ derivabile in tale punto?

$limx\to 0+$ f(x) $= limx\to 0+ x(2 -lnx) = limx\to 0+(-xlnx) = 0 = f(0)$ : la funzione è continua.

Per $x> 0$ risulta:

f'(x) $= 2 -lnx+ x(-1$
$x) = 2 -lnx-1 = 1 -lnx$

Poiché: $limx\to 0+$ f'(x) $= + \infty$ , la funzione non è derivabile in $x= 0$ (dove si ha un punto
a tangente verticale).

Si studi la funzione f(x) e se ne tracci il grafico $\gamma$ , su un piano riferito $ad$ un sistema di
assi cartesiani ortogonali (Oxy).

Studiamo, per $x>0,$ la funzione

$y=$ f(x) $= x(2 -lnx)$

Dominio: $0 < x< +\infty$

Simmetrie notevoli:

Visto il dominio, la funzione non può essere né pari né dispari.

Intersezioni con gli assi cartesiani:

Se $x= 0$ , la funzione non esiste (nel suo prolungamento vale $0).$

Se $y= 0, x(2 -lnx) = 0, lnx= 2 , x= e^{2}$

Segno della funzione:

$y> 0$ se $2 -lnx> 0 \implies lnx< 2 , x< e^{2}$

Limiti:

$$ lim $$
$x\to 0+ x(2 -lnx) =$ lim
$x\to 0+(-xlnx) = 0$

$$ lim $$
$x\to +\infty x(2 -lnx) = -\infty$

Non c’è asintoto obliquo, poiché la funzione non è un infinito del primo ordine.

Derivata prima:

f'(x) $= 1 -lnx\ge 0$ se $lnx\le 1 \implies 0 < x\le$ e

Pertanto la funzione è crescente se $0 < x<$ e e decrescente se $x>$ e

$x=$ e punto di massimo relativo (e assoluto), f(e) $\cong$ e

Derivata seconda:

f''(x) = -
$x \ge 0$ mai: la concavità è sempre rivolta verso il basso.

Il grafico della funzione è il seguente (ricordiamo che $f(0)=0):$

Si calcoli l’espressione, in funzione di $t (t>0),$ dell’integrale

I(t) $= \int$
$x(2 -lnx) dx$ .
$$ e^{2} t $$

I(t) $= -\int x(2 -lnx) dx$
$$ t e^{2 $$
$= \int x(lnx-2) dx$
$$ t e^{2 $$

Cerchiamo, integrando per parti, una primitiva di $y= x(lnx-2)$ .

$\int x(lnx-2) dx= \int (x^{2}$
'
$(lnx-2) dx= x^{2}$
$2 ∙(lnx-2) -\int x^{2}$
$2 ∙(1$
$x) dx=$

$= x^{2}$
$2 ∙(lnx-2) -1$
$2 \int x dx= x^{2}$
$2 ∙(lnx-2) -1$
$4 x^{2} + K= 1$
$4 x^{2}(2lnx-5) + K$

I(t) $= \int x(lnx-2) dx$
$$ t e^{2} = [1 $$
$4 x^{2}(2lnx-5)]$
$$ e^{2} t = 1 $$
$4 t^{2}(2lnt-5) + 1$
$4 e^{4}$

Si faccia vedere che I(t) tende verso un limite finito quando $t$ tende a $0.$
Cosa rappresenta questo limite nel grafico precedente ?

I(t) $= \int$
$x(2 -lnx) dx$ .
$$ e^{2} t $$

$limt\to 0$ I(t) $= limt\to 0$ [
$4 t^{2}(2lnt-5) +$
$4 e^{4}] =$
$4 e^{4} = \int$
$x(2 -lnx) dx$
$$ e^{2} 0 $$

Il limite richiesto rappresenta l’area della regione piana delimitata dal grafico della
funzione e dall’asse $x$ nell’intervallo $[0; e^{2}].$

Con la collaborazione di Angela Santamaria, Simona Scoleri e Stefano Scoleri
