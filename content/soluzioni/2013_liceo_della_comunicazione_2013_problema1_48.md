---
tipo: soluzione
prova_stem: 2013_liceo_della_comunicazione_2013_problema1_48
pdf: Prova_Maturita_2013.pdf
source: text
title: Liceo Comunicazione 2013 — Problema 1 — Svolgimento
---


LICEO DELLA COMUNICAZIONE 2013 - PROBLEMA $1$

I due cerchi $\Sigma$ e $\Delta$ , in figura, hanno uguale raggio $4$ e i rispettivi centri nei punti $(-2; 0)$ e
$(2; 0)$ . Con $\Gamma$ è denotata la loro parte comune e con A e $B$ le intersezioni delle loro
circonferenze.

Si calcoli l’area di $\Gamma$ .

L’area di è il doppio dell’area del segmento
circolare ACBO, il cui valore si ottiene
sottraendo all’area del settore circolare ADB
l’area del triangolo ABD.
Siccome $AC$ è uguale a $4$ (è il raggio della
circonferenza ), il triangolo ACD è equilatero,
quindi l’angolo ADC misura $60^\circ$ , e perciò
l’angolo ADB misura $120^\circ$ .
Il settore circolare ADB è quindi $1/3$ del
cerchio, perciò la sua area è:

( )

$$ \sqrt \sqrt $$

Quindi l’area del segmento circolare ACBO è uguale a:

( )
$$ \sqrt{ } ( \sqrt ) ( \sqrt ) $$

Fra tutti i rettangoli inscritti in $\Gamma$ e aventi i lati paralleli agli assi cartesiani, si determini
quello di perimetro massimo.

Il generico vertice E del rettangolo CDEF ha coordinate E $(x; y),$ con $0 \le x\le$ ,
$y\ge 0$ ed appartiene a , quindi le sue coordinate soddisfano l’equazione della
circonferenza
che è: $(x+ ) + y$ .

Risulta: $p( EF) x+ y x$

Da $(x+ ) + y$ ricaviamo $y \sqrt{x+$ } $\sqrt{x} x$

Il perimetro è massimo se è massima l’espressione $z x+ y x+ \sqrt{x} x$

{z $x+ y x+ \sqrt{x} x$
$0 \le x\le$

$z > 0$ se:

Verificata se:

E tenendo presente che $0 \le x\le$ abbiamo:

Quindi $z$ è crescente in

e decrescente in

Il perimetro del rettangolo è quindi massimo se $x 2(\sqrt{2} 1) 0$

Si calcoli il volume del solido generato dalla rotazione di $180^\circ$ di $\Gamma$ attorno all’asse $x$ .

Il volume richiesto è il doppio del volume del solido generato dalla rotazione di $360^\circ$
attorno all’asse $x$ della porzione $S$ di $\Gamma$ che si trova nel primo quadrante.

L’arco $AC$ della circonferenza ha equazione: $y$ f(x) $\sqrt{x} x.$

( ) $\int f (x) x$

$\int (\sqrt{x} x)$

$\int ( x x) x$

[ $x x$
$x$ ]

Quindi il volume richiesto è:

( ) $0$
$$ ( 0 ) 77 $$

Preso un punto $P$ sulla circonferenza $\Sigma$ , si indichi con $Q$ l’ulteriore intersezione della retta
$PA$ con la circonferenza $\Delta$ . Si provi che il triangolo PQB è equilatero e si determini la
posizione di $P$ affinché il triangolo abbia lato massimo.

Risulta:
$\alpha$ PQ̂ Q̂ Ê : perché insistono sullo stesso arco $AB$ (in $\Delta$ )
$\beta$ QP̂ P̂ F̂ : perché insistono sullo stesso arco $AB$ (in $\Sigma$ )

Ma i triangoli AEB e AFB sono equilateri, poiché l’altezza relativa alla base $AB$ misura $6,$
che è uguale $ai 3/2$ del raggio $(4),$ e questa è una proprietà caratteristica dei triangoli
equilateri inscritti in una circonferenza.

Quindi $\alpha \beta$ misurano $60^\circ$ e ciò è sufficiente per dedurre che il triangolo PQB è
equilatero.

Al variare di $P$ su $\Sigma$ il massimo valore di $BP$ (corda di $\Sigma$ con l’estremo $B$ fisso) si ha
quando $BP$ è un diametro: quindi il massimo valore del lato del triangolo PBQ è
In tal caso il triangolo ABP è rettangolo in A, quindi, essendo $\beta 0$ , risulta:

. Inoltre, essendo $AP$ perpendicolare $ad AB,$ l’ordinata di $P$ è uguale
a quella di A. Essendo $AB$ lato di triangolo equilatero inscritto in una circonferenza:
$\sqrt \sqrt$ , l’ordinata di A vale $\sqrt$ .

Pertanto le coordinate di $P$ quando il triangolo ha lato
massimo sono:

$P ( ; \sqrt$ )

Con la collaborazione di Angela Santamaria, Simona Scoleri e Stefano Scoleri
