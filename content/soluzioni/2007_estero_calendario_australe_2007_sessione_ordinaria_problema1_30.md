---
tipo: soluzione
prova_stem: 2007_estero_calendario_australe_2007_sessione_ordinaria_problema1_30
pdf: Prova_Maturita_2007.pdf
source: text
title: 2007 Estero Australe — Problema 1 — Svolgimento
---


Estero (Calendario australe) 2007
Sessione $Ordinaria-$ Problema $1$

Scuole italiane all’estero (Calendario australe ) 2007 - PROBLEMA $1$

Sia $f$ la funzione definita da: f(x) $= alog10 x+ 1$ , ove a è un parametro reale.

Dopo aver precisato il campo di esistenza di $f$ si stabilisca per quali valori di a la funzione
$f$ è crescente.

Il campo di esistenza di $f$ è $x>0.$

Calcoliamo la derivata prima della funzione: f'(x) =
$ln10$ ∙
$x> 0$ se $a> 0$ (con $x> 0)$
Quindi: se $a>0$ la funzione è crescente in tutto il dominio $x>0.$
Se $a<0$ la funzione è sempre decrescente.
Se $a=0$ la funzione equivale a $f(x)=1,$ quindi è costante per ogni $x.$
In conclusione: la funzione è crescente per $a>0.$

Si disegnino i grafici $F$ e $G$ di $f$ corrispondenti, rispettivamente, $ai$ valori a $= 2$ e a $= -2$ e
siano $b$ e $c$ le ascisse delle loro rispettive intersezioni con l’asse $x.$

Se $a=2:$ f(x) $= 2log10 x+ 1$ ed il suo grafico $F$ si ottiene dal grafico di $y= log10 x$
mediante una dilatazione verticale di fattore $2$ ed una successiva traslazione verticale di
$1.$ Grafico di $F:$

Estero (Calendario australe) 2007
Sessione $Ordinaria-$ Problema $1$

Cerchiamo $b: 2log10 x+ 1 = 0, log10 x= -$
$2 , x=$
$\sqrt{10} = b$

Se $a=-2:$ f(x) $= -2log10 x+ 1$ ed il suo grafico $G$ si ottiene dal grafico di $y= log10 x$
mediante un ribaltamento rispetto all’asse $x,$ una dilatazione verticale di fattore $2,$ ed una
successiva traslazione verticale di $1.$ Grafico di $G:$

Cerchiamo $c: -2log10 x+ 1 = 0, log10 x=$
$2 , x= \sqrt{10} = c$

Si calcoli l’area del triangolo mistilineo di base l’intervallo [b, c] e vertice il punto
d’intersezione tra $F$ e $G$ e, con l’aiuto di una calcolatrice, se ne dia un valore
approssimato.

Abbiamo la seguente situazione grafica:

Cerchiamo l’intersezione A tra $F$ e $G: 2log10 x+ 1 = -2log10 x+ 1 , x= 1$ e $y= 1.$
L’area richiesta si ottiene mediante il seguente calcolo integrale:

Estero (Calendario australe) 2007
Sessione $Ordinaria-$ Problema $1$

$A= A^{1} + A^{2} = \int$
$[2log10 x+ 1]$
$$ 1 1 \sqrt{10 $$
$dx+ \int$
$[-2log10 x+ 1]$
$$ \sqrt{10} 1 dx $$

Cerchiamo una primitiva di $log10 x=$
$ln10$ lnx.
Integrando per parti abbiamo:

$\int$ lnx $dx= xlnx-x+ C,$ quindi $\int log10 x dx=$
$ln10 (xlnx-x) + C.$ Pertanto:

$$ \int $$
$[2log10 x+ 1]$
$$ 1 1 \sqrt{10 $$
$dx+ \int$
$[-2log10 x+ 1]$
$$ \sqrt{10} 1 $$
$dx=$ [ $2$
$ln10 (xlnx-x) +$ x] $1$
$$ \sqrt{10} 1 + $$

+ [ $-2$
$ln10 (xlnx-x) +$ x]
$$ 1 \sqrt{10 $$
= [-
$ln10 + 1 -$
$ln10 ( 1$
$$ \sqrt{10} ln1 \sqrt{10 $$
-
$$ 1 \sqrt{10 $$
) -
$$ 1 \sqrt{10} ] + $$

+ [ $-2$
$ln10 (\sqrt{10ln}\sqrt{10} -\sqrt{10}) + \sqrt{10} -( 2$
$ln10) -1] =$

$$ = [(\sqrt{10} 5 $$
$-2) log10 e+ 1] + [(2 \sqrt{10} -2) log10 e-1] = (11\sqrt{10}$
$-4) log10 e\cong 1.28 u^{2} =$ Area

Sia g(x) $= x^{2}.$ Si determini il valore di a per cui $f$ e $g$ hanno la stessa tangente nel punto
di ascissa $1.$

Abbiamo le due funzioni g(x) $= x^{2}$ e f(x) $= alog10 x+ 1.$ Le due funzioni hanno la
stessa tangente per $x=1$ se:
{ $g(1) = f(1)$
$g'(1) = f'(1)$ ; {
$$ 1 = 1 $$
$(2x)x=1 =$ ( a
$ln10 ∙1$
$x)$
$x=1$
$$ ; 2 = a $$
$ln10 ; a= 2ln10$
Graficamente:

Con la collaborazione di Angela Santamaria
