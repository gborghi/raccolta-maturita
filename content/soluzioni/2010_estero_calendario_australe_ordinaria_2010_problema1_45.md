---
tipo: soluzione
prova_stem: 2010_estero_calendario_australe_ordinaria_2010_problema1_45
pdf: Prova_Maturita_2010.pdf
source: text
title: 2010 Estero Australe Ordinaria — Problema 1 — Svolgimento
---


Estero (calendario australe)
Ordinaria $2010-$ Problema $1$

Scuole italiane all’estero (America latina) 2010

PROBLEMA $1$

Sia $f$ la funzione di dominio $I= ]-$
$2 ; +\infty$ [ definita da f(x) $= ln(1 + 2x).$

a)

Quale è il codominio di $f$ ? Si dimostri che $f$ è strettamente crescente su I e se ne tracci
il grafico $\gamma$ .

Dominio: $1 + 2x> 0 , x> -$
$2 : I= ]-$
$2 ; +\infty$ [
Codominio: la funzione è continua in tutto il suo dominio e risulta:

$$ lim $$
$x\to (-1$
$+ ln(1 + 2x) = -\infty$ e lim
$x\to +\infty ln(1 + 2x) = +\infty$

Quindi il codominio è: $]-\infty ; +\infty$ [ $= R$ .

Calcoliamo la derivata prima della funzione: f'(x) =
$1+2x> 0$ su tutto I: la funzione è
quindi strettamente crescente su I .
Si può anche verificare direttamente che, per ogni $x^{1} < x^{2}$ del dominio si ha $2x^{1} < 2x^{2},$
$1 + 2x^{1} < 1 + 2x^{2}, ln(1 + 2x^{1}) < ln (1 + 2x^{2}).$

Il grafico della funzione si può facilmente ottenere dal grafico di $y= ln x$ mediante una
traslazione di vettore $(-1; 0)$ ed una dilatazione verticale di fattore $2:$

Estero (calendario australe)
Ordinaria $2010-$ Problema $1$

Allo stesso risultato si può arrivare con uno studio diretto della funzione.
Abbiamo già calcolato i limiti e studiato la monotonia. Manca lo studio della derivata
seconda:

f’’ $(x) =$
$-4$
$(1+2x)2 < 0$ in tutto il dominio: concavità sempre verso il basso.
Osserviamo che non c’è asintoto obliquo per $x\to +\infty$ essendo:

$$ lim $$
$x\to +\infty$
f(x)
= lim
$x\to +\infty$
$ln (1+2x)$
$= 0 (x$ è infinito di ordine superiore rispetto a $ln(1 + 2x))$

C’è l’asintoto verticale $x= -$
$$ 1 2 . $$
Inoltre il grafico taglia l’asse $x$ quando $ln(1 + 2x) = 0, 1 + 2x= 1, x= 0$ :

$b)$

Sia g(x) = f(x) $-x$ con $x\in$ I; si studi come varia g(x) su I .

Risulta: g(x) = f(x) $-x= ln(1 + 2x) -x$
Il dominio è quello di $f.$ L’asse $x$ viene ancora intersecato nell’origine degli assi (infatti
risulta $g(0) = f(0) -0 = 0$ ), e anche $g,$ come $f,$ ha l’asintoto verticale $x= -$
$2$ . Inoltre il
grafico di g(x) interseca l’asse $x$ in un altro punto (come richiesto nel punto $c).$ Risulta poi:

$$ lim $$
$x\to +\infty (ln(1 + 2x) -x) = -\infty$ , perché $-x$ è infinito di ordine superiore rispetto a $ln(1 + 2x)$
Vediamo se c’è asintoto obliquo:

$$ lim $$
$x\to +\infty$
g(x)
= lim
$x\to +\infty$
$ln(1 + 2x) -x$
= lim
$x\to +\infty [ln(1 + 2x)$
$-1] = -1 = m$

$$ lim $$
$x\to +\infty$ (g(x) $-mx) =$ lim
$x\to +\infty (ln(1 + 2x) -x+ x) =$ lim
$x\to +\infty (ln(1 + 2x)) = +\infty$

Non c’è quindi asintoto obliquo.

Estero (calendario australe)
Ordinaria $2010-$ Problema $1$

Studiamo la derivata prima:

g'(x) =
$1+2x-1 =$
$1-2x$
$1+2x> 0 per-$
$2 < x<$
$2$ : la funzione è crescente in tale intervallo e

decrescente per $x>$
$2 ; x=$
$2$ è punto di massimo relativo (e assoluto), con ordinata:

$g(1$
$2) = ln(2) -1$
$2 \cong 0.2$ .

Studiamo la derivata seconda:

g’’ $(x) =$
$-4$
$(1+2x)2 < 0$ per ogni $x$ del dominio: concavità sempre verso il basso, non ci sono
flessi.

Grafico:

$c)$

Si dimostri che l’equazione g(x) $= 0$ ammette due soluzioni: $0$ e un’altra, denotata con
$\beta$ , appartenente all’intervallo $[1, 2].$ Si dimostri altresì che per tutti gli $x$ reali dell’intervallo
$J= ]0; \beta$ [ anche f(x) appartiene a $J$ .

Essendo
$$ lim $$
$x\to (-1$
+ g(x) $= -\infty$ e $g$ crescente per -
$2 < x<$
$2$ , in tale intervallo g(x) $= 0$
solo per $x= 0$ . Per $x=$
$2$ la funzione è positiva e per $x>$
$2$ è decrescente; siccome
$$ lim $$
$x\to +\infty$ g(x) $= -\infty$ , per $x>$
$2$ risulta g(x) $= 0$ in uno ed un solo valore.

Risulta poi:
$g(1) = ln(3) -1 > 0$ e $g(2) = ln(5) -2 < 0$ : quindi risulta $1 < \beta < 2$ .

Estero (calendario australe)
Ordinaria $2010-$ Problema $1$

Dobbiamo infine dimostrare che se $0 < x< \beta$ allora $0 <$ f(x) $< \beta$ . Ricordiamo che:
f(x) $= ln(1 + 2x)$ e g(x) = f(x) $-x$
Risulta quindi: $f(0) = 0$ e $f(\beta ) = g(\beta ) + \beta = 0 + \beta = \beta$
Essendo $f$ sempre crescente, se $0 < x< \beta$ allora $f(0) <$ f(x) $< f(\beta$ ) cioè:

$0 <$ f(x) $< \beta$ .

$d)$

Si calcoli l’area della parte di piano delimitata dal grafico di $f$ e dall’asse $x$ sull’intervallo
$$ [0, 2]. $$

Rappresentiamo la regione di cui si chiede l’area:

L’area richiesta si ottiene calcolando il seguente integrale:

$Area= \int ln (1 + 2x)$
$$ 2 0 dx $$

Cerchiamo una primitiva di $ln (1 + 2x)$ integrando per parti:
$\int ln(1 + 2x) dx= \int 1 ∙ln(1 + 2x) dx= \int$ (x)' $∙ln(1 + 2x) dx= xln(1 + 2x) -$
$-\int$ x∙
$1 + 2xdx= xln(1 + 2x) -\int$
$2x$
$2x+ 1 dx= xln(1 + 2x) -\int 2x+ 1 -1$
$2x+ 1$
$dx=$
$= xln(1 + 2x) -\int (1 -$
$2x+ 1) dx= xln(1 + 2x) -\int dx+ \int$
$2x+ 1 dx=$
$= xln(1 + 2x) -x+$
$2 ln(2x+ 1) + K.$ Quindi:
$Area= \int ln(1 + 2x)$
$$ 2 0 $$
$dx= [xln(1 + 2x) -x+ 1$
$2 ln(2x+ 1)]$
$$ 0 2 $$
$= 2 ln(5) -2 + 1$
$2 ln(5) =$
$2 ln(5) -2) u^{2} \cong 2.02 u^{2} =$ Area
Con la collaborazione di Angela Santamaria
