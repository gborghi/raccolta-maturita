---
tipo: soluzione
prova_stem: 2024_sessione_straordinaria_2024_problema1_72
pdf: Prova_Maturita_2024.pdf
source: text
title: 2024 Straordinaria — Problema 1 — Svolgimento
---


Sessione Straordinaria 2024 - Problema $1$

LICEO SCIENTIFICO STRAORDINARIA 2024 - PROBLEMA $1$

Sia data la seguente funzione parametrica:

$y= x^{3} + ax2 + c$

a)

Si dimostri che per ogni valore dei parametri reali a e $c (a\ne 0),$ il flesso coincide con il punto medio del
segmento che ha per estremi i punti di massimo e di minimo relativi.

$y= x^{3}+ ax2+ c (a\ne 0)$

Osserviamo che la funzione (una cubica) è definita, continua e derivabile su tutto $R.$

Cerchiamo i punti di massimo e minimo relativi.

$y' = 3x^{2} + 2ax= 0$ se $x= 0$ e $x= -2$
$3$ a ; y'' $= 6x+ 2a= 0$ se $x= -1$
$3$ a

$y''(0) = 2a$ ,
y'' $(-2$
$3$ a) $= 6 (-2$
$3$ a) $+ 2a= -2a$

Quindi in $x= 0$ e $x= -$
$3$ a avremo un massimo e un minimo relativi (o viceversa).

Ricordiamo che tutte le cubiche hanno uno ed un solo flesso, nella cui ascissa si annulla la derivata
seconda. Quindi il flesso ha ascissa $x= -$
$3$ a e ordinata $y(-$
$3$ a) = -
$27 a^{3} +$
$9 a^{3} + c=$
$27 a^{3} + c$

Il flesso ha quindi coordinate: $F= (-$
$3$ a;
$27 a^{3} + c).$

Cerchiamo le ordinate del massimo e minimo relativi.

$y(0) = c , y(-2$
$3$ a) $= -8$
$27 a^{3} + 4$
$9 a^{3} + c= 4$
$27 a^{3} + c$

Il massimo (o minimo) ha coordinate $A= (0; c)$ ed minimo (o massimo) ha coordinate

$B= (-$
$3$ a;
$27 a^{3} + c).$

Il punto medio $M$ di $AB$ ha le seguenti coordinate:

$xM=$
$0 -2$
$3$ a
$= -1$
$3 a= xF ; yM=$
$c+ 4$
$27 a^{3} + c$
$$ 2 = 2 $$
$27 a^{3} + c= yF$

Sessione Straordinaria 2024 - Problema $1$

Abbiamo quindi dimostrato che il flesso coincide con il punto medio del segmento che ha per estremi i
punti di massimo e di minimo relativi.

$b)$

Si determinino i valori dei parametri affinché la funzione abbia il massimo in $x= -2$ e abbia un flesso di
ordinata $6.$

Abbiamo visto che l’ordinata del flesso è $y=$
$27 a^{3} + c$ e che le ascisse dei punti di massimo e minimo
relativo (o viceversa) sono $x= 0$ e $x= -$
$3$ a. Solo il secondo valore può assumere il valore $-2,$ deve
quindi essere:

$27 a^{3} + c= 6$
$-2$
$3 a= -2 \Rightarrow a= 3$
; ${2 + c= 6$
$a= 3$
; ${c= 4$
$a= 3$ ;

I valori richiesti sono quindi: $a= 3$ e $c= 4.$

La funzione avrà quindi equazione: $y= x^{3} + 3x^{2} + 4$

$c)$
Si disegni quindi il grafico della funzione f(x) $= x^{3} + 3x^{2} + 4,$ e si calcoli l’area della regione finita di
piano compresa tra la funzione, l’asse delle ascisse, l’asse delle ordinate e la retta di equazione $x= -2.$

Studiamo la funzione di equazione

$y=$ f(x) $= x^{3}+ 3x^{2}+ 4$

Abbiamo già detto che, essendo una cubica, è definita, continua e derivabile su tutto $R.$ Inoltre, dai punti
precedenti (notando che $a= 3$ e $c= 4)$ deduciamo che:

$A= (0; c) = A= (0; 4) = m:$ minimo relativo

$B= (-2$
$3$ a; $4$
$27 a^{3} + c) = (-2;8) = M:$ massimo relativo

$F= (-1$
$3$ a; $2$
$27 a^{3} + c) = (-1;6)$

Per tracciare il grafico della funzione basta quindi calcolare i limiti agli estremi del dominio
(osserviamo che, trattandosi di una cubica, non ci sono asintoti di alcun tipo):
$$ 4 lim $$
$x\to -\infty ( x^{3}+ 3x^{2}+ 4) = -\infty$ ,
$$ lim $$
$x\to +\infty ( x^{3}+ 3x^{2}+ 4) = +\infty$

Sessione Straordinaria 2024 - Problema $1$

Il grafico richiesto (con evidenziata la regione di cui si chiede l’area) è quindi il seguente:

L’area $S$ della regione richiesta si ottiene calcolando il seguente integrale:

$\int (x^{3} + 3x^{2} + 4)$
$-2$
$dx= [1$
$4 x^{4} + x^{3} + 4x]$
$-2$
$= 0 -(4 -8 -8) = 12 =$ Area richiesta

$d)$
Si dimostri infine che la funzione f(x) è simmetrica rispetto al suo punto di flesso. Successivamente, si
applichi alla funzione f(x) la traslazione di vettore $v⃗(\alpha , \beta$ ). Determinare $\alpha , \beta \in R,$ affinché la funzione
traslata risulti dispari.

$y=$ f(x) $= x^{3}+ 3x^{2}+ 4 , F= (-1; 6)$

Ricordiamo che tutte le cubiche sono simmetriche rispetto al punto di flesso (che esiste sempre ed è unico).

Dimostriamolo nel nostro caso. Le equazioni della simmetria rispetto al punto di coordinate (a; $b)$ sono:

{x' $= 2a-x$
$y' = 2b-y$ ; da cui: ${x= 2a-x'$
$y= 2b-y' ; {x= -2 -x'$
$y= 12 -y' ; {x\to -2 -x$
$y\to 12 -y$ ;

Sostituendo nell’equazione della funzione otteniamo:

$12 -y= (-2 -x)3 + 3(-2 -x)2 + 4 = -8 -12x-6x^{2} -x^{3} + 3(4 + 4x+ x^{2}) + 4 =$

$= -x^{3} -3x^{2} + 8 ; -y= -x^{3} -3x^{2} -4; y= x^{3} + 3x^{2} + 4 =$ f(x) $c. v. d.$

Sessione Straordinaria 2024 - Problema $1$

Per rispondere alla seconda parte della domanda osserviamo che, affinché la funzione sia dispari il suo
grafico deve essere simmetrico rispetto all’origine degli assi cartesiani, pertanto (essendo il grafico della
funzione simmetrico rispetto al flesso) la traslazione deve portare il flesso nell’origine. Cioè il punto $(-1; 6)$
deve andare nel punto $(0; 0).$ La traslazione che $fa$ ciò ha vettore di componenti:

$\alpha = 0- (-1) = 1$ e $\beta = 0 -6 = -6.$

Il vettore richiesto è quindi: $v⃗(\alpha , \beta ) = v⃗(1, -6)$

Rappresentiamo graficamente quanto verificato:

Con la collaborazione di Angela Santamaria
