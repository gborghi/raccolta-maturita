---
tipo: soluzione
prova_stem: 2023_liceo_scientifico_2023_problema2_8
pdf: Prova_Maturita_2023.pdf
source: text
title: 2023 Liceo Scientifico — Problema 2 — Svolgimento
---


Liceo Scientifico 2023 - Problema $2$

LICEO SCIENTIFICO 2023 - PROBLEMA $2$

Fissato un parametro reale a, con $a\ne 0,$ si consideri la funzione $fa$ così definita:

fa(x) $= x^{2} -ax$
$x^{2} -a$
il cui grafico sarà indicato con $\Omega$ a.

a)

Al variare del parametro a, determinare il dominio di $fa$ , studiarne le eventuali discontinuità e scrivere le
equazioni di tutti i suoi asintoti.

Dominio e discontinuità: $x^{2} -a\ne 0, x^{2} \ne$ a Dobbiamo distinguere due casi.

$Caso1, a< 0,$ il dominio è tutto $R$ (la funzione è continua su tutto $R).$

Caso $2: a> 0,$ il dominio è $x\ne \pm \sqrt{a}.$ In tal caso la funzione è discontinua per $x= \pm \sqrt{a};$ studiamo la
specie di discontinuità.

Punto $x= \sqrt{a}.$ Risulta:

$$ lim $$
$x\to \sqrt{a}$
$x^{2}-ax$
$x^{2}-a=$ [
$a-a\sqrt{a}$
] ; se $a\ne 1$ tale limite è infinito (discontinuità di seconda specie), se $a=1$ il limite si
presenta nella forma indeterminata [
$0];$ per $a=1$ abbiamo:
$$ lim $$
$x\to 1$
$x^{2} -x$
$x^{2} -1 =$ lim
$x\to 1$
$x(x-1)$
$(x+ 1)(x-1) =$ lim
$x\to 1$
$x+ 1 = 1$
$2$ (discontinuità eliminabile, terza specie)

Punto $x= -\sqrt{a}$ . Risulta:

$$ lim $$
$x\to -\sqrt{a}$
$x^{2}-ax$
$x^{2}-a=$ [
$a+a\sqrt{a}$
] $= \infty$ perchè il numeratore è sempre maggiore di zero (discontinuità di

seconda specie).

Asintoti:

Per $a<0$ il dominio è tutto $R,$ quindi non ci sono asintoti verticali ed essendo lim
$x\to \pm \infty$
$x^{2}-ax$
$x^{2}-a= 1$ abbiamo
l’asintoto orizzontale $y=1$ per $x\to \pm \infty$ .

Per $a>0$ c’è ancora l’asintoto orizzontale $y=1$ visto per $a<0$ ma potrebbero esserci asintoti verticali. In base
allo studio della continuità fatto sopra, abbiamo l’asintoto verticale $x= \sqrt{a}$ se $a\ne 1$ e l’asintoto verticale
$x= -\sqrt{a}$ per ogni $a> 0.$

Liceo Scientifico 2023 - Problema $2$

$b)$

Mostrare che, per $a\ne 1,$ tutti i grafici $\Omega$ a intersecano il proprio asintoto orizzontale in uno stesso punto e
condividono la stessa retta tangente nell’origine.

Abbiamo visto che per ogni a non nullo le curve hanno l’asintoto orizzontale $y= 1.$ Cerchiamo le
intersezioni di tale asintoto con il grafico delle funzioni.

$y= 1$
$y=$
$x^{2}-ax$
$x^{2}-a$
$x^{2}-ax$
$x^{2}-a= 1 , x^{2} -ax= x^{2} -a$ (con $x^{2} \ne$ a), $-ax= -a: x= 1$ (con $a\ne 1)$

N.B. Se fosse $a= 1,$ avremmo
$x^{2}-x$
$x^{2}-1 = 1,$
$x+1 = 1, x= x+ 1, 0 = 1!$ La curva non interseca il suo asintoto
orizzontale.

Quindi tutti i grafici intersecano il proprio asintoto orizzontale nello stesso punto $(1; 1),$ per $a\ne 1$

Cerchiamo la tangente nell’origine (che appartiene a tutte le curve):

$y' = (2x-a)(x^{2} -a) -(x^{2} -ax)(2x)$
$(x^{2} -a)2$
$= 2x^{3} -2ax-ax2 + a^{2} -2x^{3} + 2ax2$
$(x^{2} -a)2$

$y' = -2ax+ a^{2} + ax2$
$(x^{2} -a)2$
; $y'(0) = a^{2}$
$a^{2} = 1$
Osserviamo che se $a= 1$ risulta:
$y' =$
$-2x+1+x^{2}$
$(x^{2}-1)^{2} =$
$(x-1)^{2}$
$(x^{2}-1)^{2} =$
$(x+1)^{2}$ ed è ancora $y'(0) = 1.$

Quindi nell’origine tutti i grafici condividono la tangente di equazione $y= x$ (per $a\ne 1)$

$c)$

Al variare di $a< 1,$ individuare gli intervalli di monotonia della funzione $fa$ . Studiare la funzione $f-1(x)$ e
tracciarne il grafico $\Omega -1.$

$y' = -2ax+ a^{2} + ax2$
$(x^{2} -a)2$
$> 0,$
$a(x^{2} -2x+$ a) $> 0,$
con $x^{2} -a\ne 0$

Caso $1: 0 < a< 1$

$a(x^{2} -2x+$ a) $> 0$ se $x^{2} -2x+ a> 0 , x< 1 -\sqrt{1} -a$ vel $x> 1 + \sqrt{1} -a$ . In tali intervalli la
funzione è crescente, nella parte rimanente del dominio è decrescente.

Caso $2: a< 0$

Liceo Scientifico 2023 - Problema $2$

$a(x^{2} -2x+$ a) $> 0$ se $x^{2} -2x+ a< 0: 1 -\sqrt{1} -a< x< 1 + \sqrt{1} -a.$ In tale intervallo la funzione è
crescente, nella parte rimanente del dominio è decrescente.

Dobbiamo ora studiare la funzione per $a= -1.$

$y=$ f(x) $= x^{2} + x$
$x^{2} + 1$

Dominio: $R$ (dove è continua e derivabile)

Asintoto orizzontale $y= 1$

Studio derivata prima (già calcolata con a generico, quindi basta porre in essa $a= -1)$

$y' = -(x^{2} -2x-1)$
$(x^{2} + 1)^{2}$
$= -x^{2} + 2x+ 1$
$(x^{2} + 1)^{2}$
$> 0$ quando $-x^{2} + 2x+ 1 > 0,$
$x^{2} -2x-1 < 0$

$1 -\sqrt{2} < x< 1 + \sqrt{2}:$ in tale intervallo la funzione è crescente, pertanto avremo un minimo relativo (che
sarà anche assoluto) per $x= 1 -\sqrt{2}$ ed un massimo relativo (che sarà anche assoluto) per $x= 1 + \sqrt{2}.$

Studio derivata seconda:

y'' $= 2(x^{3} -3x^{2} -3x+ 1)$
$(x^{2} + 1)^{3}$
$= 2(x+ 1)(x^{2} -4x+ 1)$
$(x^{2} + 1)^{3}$
$> 0$ se $(x+ 1)(x^{2} -4x+ 1) > 0.$

Risolvendo la disequazione si ottiene:

y'' $> 0$ se $-1 < x< 2 -\sqrt{3}$ vel $x> 2 + \sqrt{3}$ . In tali intervalli il grafico volge la concavità verso
l’alto, nella parte rimanente del dominio verso il basso. Perciò si hanno i seguenti punti di flesso:

$x= -1, x= 2 -\sqrt{3} , x= 2 + \sqrt{3}$

Il grafico della funzione è il seguente:

Liceo Scientifico 2023 - Problema $2$

$d)$

Determinare l’area della regione limitata compresa tra il grafico $\Omega -1,$ la retta $ad$ esso tangente
nell’origine e la retta $x= \sqrt{3}.$

Ricordiamo che la tangente nell’origine (trovata per ogni valore di a) ha equazione $y= x.$

L’area richiesta si ottiene calcolando i seguenti integrali:

$Area= b+ a= \int (x^{2} + x$
$x^{2} + 1 -x)$
$$ 1 0 $$
$dx+ \int$
$(x-x^{2} + x$
$x^{2} + 1)$
$$ \sqrt{3} 1 dx $$

Calcoliamo il seguente integrale indefinito:

$$ \int $$
$x^{2} + x$
$x^{2} + 1 dx= \int$
$x^{2} + 1 -1 + x$
$x^{2} + 1$
$dx= \int (1 -$
$1 + x^{2} +$
$1 + x^{2}) dx=$

$= x-arctg(x) + 1$
$2 ln(1 + x^{2}) + c$
Quindi:

$Area= -[x^{2}$
$2 -x+$ arctg(x) $-1$
$2 ln(1 + x^{2})]$
$$ 0 1 $$
$+ [x^{2}$
$2 -x+$ arctg(x) $-1$
$2 ln(1 + x^{2})]$
$$ 1 \sqrt{3} = $$
= -[
$2 -1 + arctg(1) -$
$2 ln (2) -0] +$ [
$2 -\sqrt{3} + arctg(\sqrt{3}) -$
$2 ln(4) -$
$2 + 1 -(arctg(1) +$
$2 ln(2)]=$
$2 -$
$\pi$
$$ 4 + 1 $$
$2 ln (2)) + (2 -\sqrt{3} +$
$\pi$
$3 -$
$2 ln(2) -$
$\pi$
$$ 4) = ( 5 $$
$2 -$
$\pi$
$6 -\sqrt{3}) u^{2} \cong 0.24 u^{2} =$ Area

Con la collaborazione di Angela Santamaria
