---
tipo: soluzione
prova_stem: 2019_sessione_straordinaria_2019_problema1_189
pdf: Prova_Maturita_2019.pdf
source: text
title: 2019 Straordinaria — Problema 1 — Svolgimento
---


Sessione Straordinaria 2019 - Problema $1$

LICEO SCIENTIFICO STRAORDINARIA 2019 - PROBLEMA $1$

Dato $k > 0,$ si consideri la funzione $f: [0, +\infty ) \to R$ così definita:

f(x) = {
$kx$ se $0 \le x\le 1$
$x^{2}$ se $x> 1$

a)
Dimostrare che, qualunque sia $k > 0,$ la funzione $f$ è continua ma non ovunque derivabile.
Studiare l’andamento di tale funzione, specificandone il punto di massimo assoluto. Per quali
valori di $k$ le tangenti destra e sinistra nel punto di non derivabilità formano un angolo acuto $\gamma$ tale
che tan $\gamma = 3$ ?

Per $x\ne 1$ la funzione è continua; analizziamo $x= 1.$

$f(1) = k$ , lim
$x\to 1-f(x) =$ lim
$x\to 1-(kx) = k$ , lim
$x\to 1+$ f(x) = lim
$x\to 1+ ( k$
$x^{2}) = k$

Quindi la funzione è continua per ogni valore di $k.$

Analizziamo la derivabilità.
Anche in questo caso il punto da analizzare è $x= 1,$ essendo la funzione derivabile per $x\ne 1.$
Si ha:
f'(x) = {
$k$ se $0 \le x< 1$
$-2k$
$x^{3}$ se $x> 1$
E risulta:

$$ lim $$
$x\to 1-f'(x) =$ lim
$x\to 1-(k) = k$ , lim
$x\to 1+$ f'(x) = lim
$x\to 1-(-2k$
$x^{3} ) = -2k$

Quindi, essendo $k= -2k$ per $k= 0,$ per $k> 0$ la funzione non è derivabile per $x=1.$

Studio della funzione

f(x) = {
$kx$ se $0 \le x\le 1$
$x^{2}$ se $x> 1$

Per $0 \le x\le 1$ il grafico è un segmento di estremi $(0; 0), (1; k),$ con $k> 0$

Sessione Straordinaria 2019 - Problema $1$

Per $x> 1$ il grafico è parte della funzione potenza $y= k x-2,$ che è del tipo:

Il grafico della funzione $y= f (x)$ è quindi il seguente:

Il punto di massimo assoluto è $M= (1; k).$
Tale punto coincide con il punto di non derivabilità. In base a quanto visto precedentemente
risulta:

$f-$
$'(1) = k$ e $f+$
$'(1) = -2k$

Detto $\gamma$ l’angolo fra le tangenti destra e sinistra in $M$ si ha:

tan $\gamma = | m-m'$
$1 + m m'| = | k+ 2k$
$1 -2k^{2}| = |$
$3k$
$1 -2k^{2}| = 3$ se
$3k$
$1 -2k^{2} = \pm 3$

$3k$
$1 -2k^{2} = 3 , 3k= 3 -6k^{2}$ ,
$2k^{2} + k-1 = 0, k= -1 \pm 3$
: accettabile $k= 1$

$3k$
$1 -2k^{2} = -3,$
$3k= -3 + 6k^{2}, 2k^{2} -k-1 = 0: k= -1$
$2$ non acc. e $k= 1$ accettabile

Pertanto le tangenti destra e sinistra nel punto di non derivabilità formano un angolo acuto $\gamma$ tale
che tan $\gamma = 3$ se $k=$
$2$ o $k= 1.$

Sessione Straordinaria 2019 - Problema $1$

$b)$
Posto $k = 1,$ sia $r$ una retta di equazione $y = t,$ con $0 < t < 1.$ Detti $S$ e $T$ i punti d’intersezione
tra $r$ ed il grafico della funzione $f,$ siano $S'$ e $T'$ le rispettive proiezioni ortogonali sull’asse $x.$
Come deve essere scelto il valore di $t,$ in modo che sia massima l’area del rettangolo SS'T'T?

Se $k= 1$ la funzione $f$ ha equazione:

f(x) = {
$x$ se $0 \le x\le 1$
$x^{2}$ se $x> 1$

Cerchiamo $S$ e $T.$

$S: {y= x$
$y= t ; S= (t; t)$

$T: {y= 1$
$$ x^{2 $$
$y= t$
; $T= ( 1$
$\sqrt{t}; t)$

Area(SS'T'T) $= ( 1$
$\sqrt{t}-t) (t) = t$
$\sqrt{t}-t^{2} = \sqrt{t}-t^{2}$ , con $0 < t< 1$

Quindi dobbiamo cercare il massimo della funzione

$y= \sqrt{t}-t^{2},$ con $0 < t< 1$

Risulta: $y' =$
$2\sqrt{t}-2t\ge 0$ se $1 \ge 4t\sqrt{t}$ ,
$16 \ge t^{3}: t\le \sqrt{1}$
$$ 16 3 $$
, con $0 < t< 1$

Sessione Straordinaria 2019 - Problema $1$

Quindi la funzione è crescente per $0 < t< \sqrt{1}$
$$ 16 3 $$
e decrescente per $\sqrt{1}$
$$ 16 3 $$
$< t< 1,$ risulta quindi massima
per $t= \sqrt{1}$
$$ 16 3 . $$

L’area del rettangolo SS'T'T è massima per $t= \sqrt{1}$
$$ 16 3 . $$

Nel vuoto, si consideri una distribuzione sferica di carica elettrica, positiva e di raggio $R,$ espresso in metri
$(m).$ La densità di carica, indicata con $\rho$ ed espressa in coulomb al metro cubo $(C/m^{3}),$ è uniforme.

$c)$
Indicata con $x$ la distanza di un punto $P$ dal centro della sfera, provare che l’intensità del campo
elettrico generato da tale distribuzione di carica è data da

E(x) = {
$kx$ se $0 \le x\le R$
$$ kR3 $$
$x^{2}$ se $x> R$

dove $k$ è un’opportuna costante, di cui si chiede l’espressione in funzione della densità di carica $\rho$
e la dimensione fisica.

Consideriamo una superficie sferica $S$ di raggio $x$ interna alla sfera che contiene le cariche e $ad$
essa concentrica e applichiamo il teorema di Gauss. Detto $\Phi$ il flusso attraverso $S$ del campo
elettrico E generato dalle cariche interne $ad S$ si ha:

$\Phi = ES = 4\pi x2E$

Per il teorema di Gauss, detta $Q$ la carica totale contenuta in $S$ ed $\varepsilon 0$ la costante dielettrica del
vuoto, si ha poi:
$\Phi = Q$
$\varepsilon 0$

Ma se $V$ è il volume della sfera $S$ risulta:
$Q= \rho V= \rho (4$
$3 \pi x^{3})$

Quindi:
$\Phi = Q$
$\varepsilon 0$
$= \rho V$
$\varepsilon 0$
$\rho (4$
$3 \pi x^{3})$
$\varepsilon 0$
$= 4\pi x^{3}\rho$
$3\varepsilon 0$

Sessione Straordinaria 2019 - Problema $1$

Uguagliando le due espressioni del flusso:

$4\pi x2E= 4\pi x^{3}\rho$
$3\varepsilon 0$
da cui: $E= \rho x$
$3\varepsilon 0$

Per i punti esterni alla distribuzione di cariche, con ragionamento analogo si ha:

$4\pi x2E=$
$4\pi R^{3}\rho$
$3\varepsilon 0$ , tenendo presente che in questo caso $Q= \rho V= \rho$ (
$3 \pi R^{3})$

Quindi per i punti esterni:

$E= \rho R^{3}$
$3\varepsilon 0x^{2}$

Osservato che nel centro della distribuzione di cariche il campo elettrico è nullo e che sulla sua superficie è
dato da $E=$
$\rho R$
$3\varepsilon 0$ (caso particolare del caso relativo $ai$ punti interni quando $x$ tende $ad R),$ si ha:

E(x) = {
$kx$ se $0 \le x\le R$
$$ kR3 $$
$x^{2}$ se $x> R$
avendo posto $k=$
$\rho$
$3\varepsilon 0$ .

Calcoliamo la dimensione fisica di $k:$

[k] =
$[\rho$ ]
$[\varepsilon 0] =$
[Q
V]
$[\varepsilon 0] =$
[Q]
$[L^{3}]$
[ $Q^{2}$
$FL2]$
= [F]
[QL] $= [MLT-2]$
[ITL]
$= [M][T]-3[I]-1 =$ [k]

L’unità di misura di $k$ è:
$$ Kg $$
$s3A$ .

$d)$
Sia $q$ una carica elementare positiva collocata nel centro della sfera. Determinare l’espressione del
lavoro compiuto dalla forza elettrica per portare la carica $q$ a distanza $2R$ dal centro della sfera.
Quale dovrebbe essere il lavoro compiuto dalla stessa forza elettrica per portare la carica $q$ a
distanza infinita dal centro della sfera?

Il lavoro $dL$ (positivo)compiuto dalla forza elettrica (del campo generato dalla distribuzione sferica
di carica positiva) per spostare di un tratto $x$ la carica $q$ (positiva) dal centro della sfera è dato da:

$dL=$ qEdx

Sessione Straordinaria 2019 - Problema $1$

Integrando:

$q\int$
$$ Edx $$
$2R$
$= q\int$ (kx)dx
$$ R 0 $$
$+ q\int$
$(kR3$
$x^{2} ) dx$
$2R$
$= q[1$
$2 kx2]$
$$ 0 R $$
$+ q[-kR3$
x]
$2R$

$2 kqR2 -q($
$2 kR2 -kR2) = qkR2:$ lavoro per portare $q$ dal centro della sfera a distanza $2R.$

Calcoliamo ora il lavoro per portare la carica dal centro della sfera all’infinito:

$L= q\int$ (kx)dx
$$ R 0 $$
$+ q\int$
$(kR3$
$x^{2} ) dx$
$+\infty$
$= q[1$
$2 kx2]$
$$ 0 R $$
$+ q$ lim
$b\to +\infty [-kR3$
x]
$$ R b = $$

$2 qkR2 -qlim$
$b\to +\infty (kR3$
$-kR2) = 1$
$2 kqR2 -0 + kqR2 = 3$
$2 kqR2 = L$

Con la collaborazione di Angela Santamaria e Stefano Scoleri
