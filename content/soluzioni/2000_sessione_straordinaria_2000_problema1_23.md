---
tipo: soluzione
prova_stem: 2000_sessione_straordinaria_2000_problema1_23
pdf: Prova_Maturita_2000.pdf
source: text
title: 2000 Straordinaria — Problema 1 — Svolgimento
---


Sessione straordinaria 2000 - Problema $1$

LICEO SCIENTIFICO STRAORDINARIA 2000 - PROBLEMA $1$

È assegnata, nel piano riferito $ad$ assi cartesiani ortogonali Oxy, la curva $g$ di equazione
$y= 1 -$
$1 + x^{2}$

a)

Il candidato studi e disegni il grafico di $g$ e quello della curva $g^{1}$ simmetrica di $g$ rispetto alla
retta $y = 1.$
$y=$ g(x) $= 1-$
$1+ x^{2}=$
$$ x^{2 $$
$1+ x^{2}$

Dominio: tutto $R.$

Intersezioni con gli assi cartesiani: se $x=0, y=0:$ il grafico passa per $O= (0; 0).$ Se $y=0, x=0.$ Quindi gli
assi cartesiani vengono tagliati solo nell’origine.

Simmetrie notevoli: $g(-x) =$ g(x): la funzione è pari, quindi il suo grafico è simmetrico rispetto
all’asse $y.$

Segno della funzione: $y\ge 0$ per ogni $x$ (in particolare si annulla per $x= 0).$ Il grafico non
è mai al di sotto dell’asse $x.$

Limiti agli estremi del dominio ed eventuali asintoti:

$$ lim $$
$x\to \pm \infty$ g(x) = lim
$x\to \pm \infty$
$$ x^{2 $$
$1+x^{2} = 1:$ la retta di equazione $y= 1$ è asintoto orizzontale per $x\to$
$\pm \infty$ . Non ci sono asintoti verticali né obliqui.

Studio della derivata prima:

g'(x) =
$2x(1+x^{2})-x^{2}(2x)$
$(1+x^{2})^{2}$
$2x$
$(1+x^{2})^{2} \ge 0$ se $x\ge 0:$ la funzione è crescente per $x> 0$ e
decrescente per $x< 0; x= 0$ è punto di minimo relativo (e assoluto).

Studio della derivata seconda:

g''(x) $= 2(1 + x^{2})^{2} -2x[2(1 + x^{2})(2x)]$
$(1 + x^{2})^{4}$
$= ⋯= 2 -6x^{2}$
$(1 + x^{2})^{3} \ge 0$ per $2 -6x^{2} \ge 0,$
$x^{2} \le$
$3, -$
$\sqrt{3} \le x\le$
$\sqrt{3}$ . Quindi:

Sessione straordinaria 2000 - Problema $1$

il grafico di $g$ ha la concavità verso l’alto per -
$\sqrt{3} < x<$
$\sqrt{3}$ e verso il basso per
$x< -$
$\sqrt{3}$ vel $x>$
$\sqrt{3}$ . Punti di flesso $x= \pm$
$\sqrt{3},$ con ordinata: $y= g($
$\sqrt{3}) = ⋯=$
$4,$ perciò:
$F^{1} = (-1$
$$ \sqrt{3} ; 1 4) , $$
$F^{2} = ( 1$
$$ \sqrt{3} ; 1 4) $$

Grafico della funzione:

La simmetria rispetto alla retta di equazione $y= 1$ ha equazioni:

$x' = x$
$y' = 2 -y$ ; {
$x= x'$
$y= 2 -y'$ ; {
$x\to x$
$y\to 2 -y$

$L$ a simmetrica $g^{1}$ ha quindi equazione:

$2 -y=$
$$ x^{2 $$
$1+x^{2} , y= 2 -$
$$ x^{2 $$
$1+x^{2} ; y= g^{1}(x) = 2+x^{2}$
$1+x^{2}$ .
Grafico di $g^{1}:$

$b)$

Il candidato determini $k (k > 0)$ in modo tale che la regione limitata da $g$ e $g^{1}$ e dalle rette
$x= \pm k$ sia equivalente al cerchio di raggio unitario.

Il cerchio di raggio unitario ha area: $\pi$ .

L’area $S$ della regione delimitata da $g$ e $g^{1}$ e dalle rette $x= \pm k$ si ottiene calcolando il
seguente integrale (si noti la simmetria della regione rispetto all’asse $y):$

Sessione straordinaria 2000 - Problema $1$

$S= 2 \int [g^{1}(x) -g(x)]$
$$ k 0 $$
$dx= 2 \int [2 + x^{2}$
$1 + x^{2} -$
$$ x^{2 $$
$1 + x^{2}]$
$$ k 0 $$
$dx= 2 \int$ [
$1 + x^{2}]$
$$ k 0 $$
$dx=$

$= 2 [2 arctg(x)]0$
$k= 4$ arctg(k)

Deve essere $4$ arctg(k) $= \pi$ , arctg(k) =
$\pi$
$4$ : quindi $k= 1.$

$c)$

Il candidato dica in che cosa consista il problema della “quadratura del cerchio” e perché
venga definito un “problema classico”.

Il problema della quadratura del cerchio consiste nella costruzione per via elementare di un
quadrato di area pari a quella del cerchio dato. Si tratta di un problema classico, che si è dimostrato
essere irrisolvibile per via elementare. Ciò equivale a dire che non è possibile costruire, usando
solo riga e compasso, il lato del quadrato equivalente al cerchio. Se per esempio prendiamo il
cerchio di raggio $1,$ la sua area è uguale a $\pi$ , quindi il lato del quadrato equivalente è $\pi$ , che non
può essere costruito per via elementare. La dimostrazione dell'impossibilità di quadrare il cerchio
(per via elementare) è una conseguenza del fatto che $\pi$ è un numero trascendente.

La quadratura del cerchio, assieme al problema della trisezione dell'angolo e a quello della
duplicazione del cubo, è un problema classico della matematica greca (più precisamente della
geometria).

Con la collaborazione di Angela Santamaria
