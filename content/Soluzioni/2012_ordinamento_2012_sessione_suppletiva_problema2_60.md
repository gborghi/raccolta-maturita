---
tipo: soluzione
prova_stem: 2012_ordinamento_2012_sessione_suppletiva_problema2_60
pdf: Prova_Maturita_2012.pdf
source: text
title: Soluzioni — 2012_ordinamento_2012_sessione_suppletiva_problema2_60
---
# Soluzioni — 2012_ordinamento_2012_sessione_suppletiva_problema2_60

## Problema 1

ORDINAMENTO 2012 SESSIONE SUPPLETIVA - PROBLEMA $2$

Si consideri la funzione:

f(x) $= x\sqrt{2} -x$

Si studi la funzione e si tracci il suo grafico $\gamma$ su un piano riferito $ad$ un sistema di assi cartesiani
ortogonali Oxy.

f(x) $= x\sqrt{2}-x$

Dominio:

$2 -x\ge 0 \implies x\le 2 \implies -\infty < x\le 2$

Simmetrie notevoli:

Con il dominio trovato la funzione non può essere né pari né dispari.

Intersezioni con gli assi cartesiani:

Se $x= 0 , y= 0;$ se $y= 0, x= 0$ e $x= 2.$

Segno della funzione:

$y> 0$ se $0 < x< 2$ .

Limiti:

$limx\to -\infty x\sqrt{2} -x= -\infty$ ; non c’è asintoto obliquo, poiché la funzione non è un infinito del

primo ordine.

Derivata prima:

f'(x) =
$4-3x$
$2\sqrt{2}-x\ge 0$ se $4 -3x\ge 0 , x\le$
$3$ : la funzione è crescente per $x<$
$$ 4 3 , $$
decrescente per
$3 < x\le 2$ e ammette un massimo relativo (e assoluto) per $x=$
$3$ , che
vale $f($
$$ 4 3) = 4 $$
$3 \sqrt{2} -$
$$ 4 3 = 4 3 \sqrt{2 $$
$3 \cong 1.1;$ massimo $M=$ (
$$ 4 3 ; 4 3 \sqrt{2} 3) $$

La funzione non è derivabile in $x=2$ e risulta:

$limx\to 2-$
$4-3x$
$2\sqrt{2}-x= -\infty$ : quindi in $x=2$ abbiamo una tangente verticale.

Derivata seconda:

f''(x) =
$3x-8$
$4(2-x)\sqrt{2}-x\ge 0$ se $x\ge$
$3$ ed essendo $x\le 2$ la derivata seconda è sempre
negativa: il grafico volge sempre la concavità verso il basso; non ci sono flessi.

Il grafico della funzione è il seguente:

Si risolva la disequazione:

$x\sqrt{2} -x< 1$

Rappresentiamo nello stesso sistema di riferimento cartesiano le due funzioni

$y= x\sqrt{2} -x$ e $y= 1$

Detti $F$ e $G$ i punti di intersezione tra le due curve, la disequazione è verificata per:

$x< xF , xG< x\le 2$ . Troviamo le ascisse di $F$ e $G:$

$x\sqrt{2} -x= 1;$ posta la condizione $0 < x< 2$ abbiamo:

$x^{2}(2 -x) = 1$ che equivale a: $x^{3} -2x^{2} + 1 = 0$ ; abbassando di grado (mediante la
regola di Ruffini) con la radice $x= 1 = xF,$ abbiamo:

$(x-1)(x^{2} -x-1) = 0;$ l’equazione $x^{2} -x-1 = 0$ ammette le soluzioni:

$x=$
$1\pm \sqrt{5}$
$2$ , di cui è accettabile $x=$
$$ 1+\sqrt{5} 2 $$
$= xG.$

La disequazione è quindi verificata per:

$x< 1 , 1 + \sqrt{5}$
$< x\le 2$

N.B.

Allo stesso risultato si arriva con il metodo algebrico, che equivale a risolvere i sistemi:

${x\le 2$
$x< 0 \implies x< 0$ e

$x\le 2$
$x\ge 0$
$x^{2}(2 -x) < 1$
$\implies$ {
$x\le 2$
$x\ge 0$
$x^{3} -2x^{2} + 1 > 0$
$\implies$ {
$x\le 2$
$x\ge 0$
$(x-1)(x^{2} -x-1) > 0$
da cui

$0 \le x< 1$ ,
$$ 1+\sqrt{5} 2 $$
$< x\le 2$

Si scriva l’equazione della tangente alla curva $\gamma$ nel punto di intersezione con l'asse $y$ e si
calcoli in gradi e primi (sessagesimali) l'ampiezza dell'angolo $\varphi$ che essa forma con la
direzione positiva dell'asse $x.$

Il punto d’intersezione con l’asse $y$ è $O=(0;0).$ La tangente richiesta ha equazione:

$y-f(0) = f'(0)(x-0)$

essendo $f(0) = 0$ e $f'(0) =$
$2\sqrt{2} = \sqrt{2},$ la tangente ha equazione:

$y= \sqrt{2} x$

Cerchiamo l’angolo $\varphi$ che la tangente forma con il semiasse positivo delle $x.$

$tg \varphi = f'(0) = \sqrt{2}$ quindi $\varphi = arctg(2) = 54.736^\circ \cong 54^\circ 44'$

La regione finita di piano delimitata dalla curva $\gamma$ e dall'asse $x$ nel I quadrante è la base
di un solido $S,$ le cui sezioni, ottenute con piani perpendicolari all'asse $x,$ sono tutte
esagoni regolari. Si calcoli il volume di $S.$

Il volume del solido $S$ si ottiene calcolando il seguente integrale:

V(S) $= \int$ A(x)dx
$$ 2 0 $$
, dove A(x) è l’area dell’esagono regolare di lato f(x).

A(x) $= p∙a= 3f(x)$ ∙[f(x) $∙\sqrt{3}$
$$ 2 ] = 3 $$
$2 ∙\sqrt{3}(f(x))$
$$ 2 = 3\sqrt{3} 2 $$
$∙(x\sqrt{2} -x)$
$$ 2 = 3\sqrt{3} 2 $$
$∙x^{2} ∙(2 -x)$

A(x) $= 3\sqrt{3}$
$∙(2x^{2}-x^{3})$

V(S) $= \int$ A(x)dx
$$ 2 0 = 3\sqrt{3 $$
$2 \int (2x^{2} -x^{3})dx= 3\sqrt{3}$
$∙[2$
$3 x^{3} -x^{4}$
$$ 4 ] 0 2 = 3\sqrt{3} 2 $$
$∙[16$
$3 -4 -(0)]$
$$ 2 0 $$

V(S) $= 2\sqrt{3} u^{3} \cong 3.464 u^{3}$

Con la collaborazione di Angela Santamaria, Simona Scoleri e Stefano Scoleri

ORDINAMENTO 2012 - SESSIONE SUPPLETIVA

QUESITO $1$

Si divida il segmento $AB=$ a in due parti $AC$ e $CB,$ in modo che, costruito su $AC$ il
quadrato ACDE e su $CB$ il triangolo equilatero CBF, sia minima l’area del pentagono
ABFDE.

Posto $BC=x$ (con $0 \le x\le$ a) risulta $AC=a-x;$ quindi:

Area(ABFDE) = Area(ACDE) + Area(CBF) + Area(FCD) =

$= (a-x)2 + x^{2} ∙\sqrt{3}$
$$ 4 + 1 $$
$2 (x(a-x)sen30^\circ ) = a^{2} -2ax+ x^{2} + x^{2} ∙\sqrt{3}$
$$ 4 + 1 $$
$4 ax-1$
$4 x^{2} =$

$$ = (\sqrt{3} 4 + 3 $$
$4) x^{2} -7$
$4 ax+ a^{2} = y$

$y= (\sqrt{3}$
$$ 4 + 3 $$
$4) x^{2} -$
$4 ax+ a^{2}$

rappresenta una parabola con la concavità rivolta verso l’alto, quindi il minimo di $y$ si ha
nel vertice, cioè per:

$x= xV= -$
$2a=$
$4a$
$$ \sqrt{3} 2 +3 2 = $$
$3a$
$\sqrt{3}-2 =$
$7a$
$$ 2(\sqrt{3}+3) = $$
$7a(3-\sqrt{3})$
$$ 12 $$
$\cong 0.74$ a.

Il pentagono ABFDE ha quindi area minima quando il lato del triangolo ha lato
$7a(3-\sqrt{3})$
$$ 12 . $$

QUESITO $2$

Data la funzione:
f(x) $= {senx∙ln(sen2x)$ , per $0 < x< \pi$
$0$ , per $x= 0$

si provi che è continua, ma non derivabile, nel punto $x = 0.$

Affinché la funzione sia continua in $x=0$ deve essere:

$$ lim $$
$x\to 0+$ f(x) $= f(0) = 0$

$limx\to 0+ senx∙ln(sen2x) = limx\to 0+ sen2x∙ln(sen2x)$
$2cosx$
$= 0$ poiché $cosx\to 1$ e

$sen2x∙ln(sen2x) \to 0$ (poiché $sen2x\to 0$ e f(x)lnf(x) $\to 0$ se f(x) $\to 0+).$

Quindi la funzione è continua (da destra) in $x=0.$ Dimostriamo che non è derivabile,
dimostrando che non esiste o non è finito il limite:

$$ lim $$
$h\to 0+ f(0 + h) -f(0)$

Risulta:

$$ lim $$
$h\to 0+ f(0 + h) -f(0)$
= lim
$h\to 0+ sen2h∙ln(sen2h) -0$
= lim
$h\to 0+ (sen2h$
) $∙ln(sen2h) =$

$= 2$ ∙lim
$h\to 0+ ln(sen2h) = -\infty$ : quindi la funzione non è derivabile in $x=0.$

Il grafico della funzione è il seguente:

QUESITO $3$

Si scriva l’equazione della tangente al diagramma della funzione:

f(x) $= (x+ 2)ln(e+2x)$
nel punto $P(0,2).$

La tangente in $P$ ha equazione: $y-2 = f'(0)(x-0).$

Riscriviamo la funzione nella seguente forma:

f(x) $= eln[(x+2)ln(e+2x)] = eln(e+2x)∙ln (x+2)$ (N.B. la funzione è definita per $x> -$
$$ e 2 ). $$

f'(x) $= eln(e+2x)∙ln (x+2)$ ∙[
$e+ ex∙ln(x+ 2) + ln(e+ 2x)$
$x+ 2$

$f'(0) = 2$ ∙[
$e∙ln(2) +$
$$ 1 2] = 4 $$
$e∙ln(2) + 1$ quindi la tangente in $P$ ha equazione:

$y-2 =$ [
$e∙ln(2) + 1]$ ∙x $\implies y=$ [
$e∙ln(2) + 1] ∙x+ 2 \cong 2.02 x+ 2$

Questa la situazione grafica:

QUESITO $4$

La superficie piana $S,$ delimitata dalla curva $\gamma$ di equazione $y= 1 +$ tgx e dall'asse $x$
nell'intervallo $0 \le x\le$
$\pi$
$4$ , è la base di un solido $\Sigma$ , le cui sezioni, ottenute con piani
perpendicolari all'asse $x,$ sono tutte triangoli equilateri. Si calcoli il volume di $\Sigma$ .

La superficie $S$ è rappresentata nella seguente figura:

Rappresentiamo più in dettaglio la superficie $S:$

Il volume del solido $\Sigma$ è dato da:
$V= \int$
A(x) $dx$
$\pi /4$

Essendo A(x) l’area del triangolo equilatero di lato f(x), quindi: A(x) = (f(x))
$2 ∙\sqrt{3}$
A(x) $= (1 + tgx)2 ∙\sqrt{3}$
$4 = (1 + 2tgx+ tg2x) ∙\sqrt{3}$

$V= \int$
$A(x)dx=$
$\pi /4$
$$ 0 \int $$
$(1 + 2tgx+ tg2x) ∙\sqrt{3}$
$4 dx= \sqrt{3}$
$\pi /4$
$$ 0 \int $$
$(1 + 2tgx+ tg2x)dx=$
$\pi /4$

$$ = \sqrt{3 $$
$4 \int [(1 + tg2x) + 2tgx]dx=$
$\pi$
$$ 4 0 \sqrt{3 $$
$4 [\int (1 + tg2x)dx+$
$\pi$
$$ 4 0 $$
$\int (2tgx)dx$
$\pi$
$$ 4 0 ] = $$

$$ = \sqrt{3 $$
$4 [\int (1 + tg2x)dx+$
$\pi$
$$ 4 0 $$
$2 \int$ (senx
cosx)dx
$\pi$
$$ 4 0 ] = \sqrt{3 $$
$4 [tgx-2ln(cosx)]0$
$\pi$
$$ 4 = \sqrt{3 $$
$4 [1 -2ln($
$$ \sqrt{2 $$
$2 ) -(0)] =$

$$ = \sqrt{3 $$
$4 [1 -ln(1$
$$ 2)] = \sqrt{3 $$
$4 [1 + ln (2)] u^{3} \cong 0.733 u^{3} = V$

QUESITO $5$

Mentre corre con una velocità costante attraverso il deserto, montando il suo fido
cammello, un capo tuareg vede la cima di una grande palma e dirige direttamente verso di
essa. Al primo avvistamento la cima della palma si presentava con un angolo di
elevazione di $4^\circ$ ; venti minuti più tardi l’angolo di elevazione misura $9^\circ$ . Quanti minuti sono
ancora necessari al tuareg per raggiungere l’albero?

Al primo avvistamento della palma: $BÂD= 4^\circ$ ; da A a $C$ trascorrono $20$ minuti; $BĈD= 9^\circ$ .
Dobbiamo calcolare il tempo in minuti che impiega il capo tuareg per raggiungere l’albero

Indichiamo con $t$ il tempo in minuti impiegato per passare da $C$ a $B.$

$BD= AB∙tg4^\circ = BC∙tg9^\circ$ ; ma, essendo la velocità costante:
$$ CB $$
$t=$
$$ AC $$
$20 \implies CB=$
t∙AC
$$ 20 $$
Ma $AB= AC+ CB= AC+$
t∙AC
$20 = AC(1 +$
$20);$ quindi, da $AB∙tg4^\circ = BC∙tg9^\circ$ :
$AC(1 + t$
$20) ∙tg4^\circ =$ t∙AC
$$ 20 $$
$∙tg9^\circ \implies (20 + t) ∙tg4^\circ =$ t∙ $tg9^\circ \implies$

$t(tg9^\circ -tg4^\circ ) = 20 ∙tg4^\circ \implies t=$
$20 tg4^\circ$
$tg9^\circ -tg4^\circ \cong 15.81$ minuti $\cong 15'49"$

QUESITO $6$

Si determinino le equazioni degli asintoti della curva: $f (x) = \sqrt{x}^{2} + 2x-3 -x$

Cerchiamo il dominio della funzione.

$x^{2} + 2x-3 \ge 0 \implies -\infty < x\le -3, 1 \le x< +\infty$

La funzione può quindi ammettere asintoti orizzontali $e/o$ obliqui.

Risulta:

$limx\to -\infty (\sqrt{x}^{2} + 2x-3 -x) = +\infty$ : quindi per $x\to -\infty$ possiamo avere un asintoto
obliquo.

$$ lim $$
$x\to -\infty (\sqrt{x}^{2} + 2x-3 -x$
) = lim
$x\to -\infty (-x-x$
) $= -2 = m$

$$ lim $$
$x\to -\infty$ (f(x) $-mx) =$ lim
$x\to -\infty [(\sqrt{x}^{2} + 2x-3 -x) + 2x] =$ lim
$x\to -\infty (\sqrt{x}^{2} + 2x-3 + x) =$

= lim
$x\to -\infty$
$(\sqrt{x}^{2} + 2x-3 + x) ∙(\sqrt{x}^{2} + 2x-3 -x)$
$\sqrt{x}^{2} + 2x-3 -x$
= lim
$x\to -\infty$
$x^{2} + 2x-3 -x^{2}$
$-x-x$
= lim
$x\to -\infty$
$2x$
$-2x=$

$= -1 = q$

Quindi per $x\to -\infty$ abbiamo l’asintoto obliquo di equazione: $y= -2x-1.$

Vediamo se c’è asintoto per $x\to +\infty$ .

$$ lim $$
$x\to +\infty (\sqrt{x}^{2} + 2x-3 -x) =$ [F. I. $+ \infty -\infty$ ]

$$ lim $$
$x\to +\infty (\sqrt{x}^{2} + 2x-3 -x) =$ lim
$x\to +\infty$
$(\sqrt{x}^{2} + 2x-3 -x) ∙(\sqrt{x}^{2} + 2x-3 + x)$
$\sqrt{x}^{2} + 2x-3 + x$

= lim
$x\to +\infty$
$x^{2} + 2x-3 -x^{2}$
$+x+ x$
= lim
$x\to -\infty$
$2x$
$2x= 1$

Quindi $x\to +\infty$ abbiamo l’asintoto orizzontale: $y= 1.$

N.B. La funzione di equazione $f (x) = \sqrt{x}^{2} + 2x-3 -x$ equivale a $y= \sqrt{x}^{2} + 2x-3 -x$
che si può trasformare in:

$y+ x\ge 0$
$(y+ x)2 = x^{2} + 2x-3 \implies$ {
$y\ge -x$
$y^{2} + 2xy-2x+ 3 = 0$

che è una parte di iperbole:

QUESITO $7$

Un ottaedro regolare di alluminio (densità $\rho = 2,7 g/cm3),$ avente lo spigolo $l= 5 cm,$
presenta all’interno una cavità di forma cubica. Sapendo che la massa dell’ottaedro è
$m= 155 g,$ si calcoli la lunghezza dello spigolo della cavità.

Ricordiamo che il volume dell’ottaedro regolare è:
V(ottaedro regolare) $= l^{3} ∙\sqrt{2}$

Questa formula si ottiene raddoppiando il volume della piramide regolare retta a base
quadrata con spigoli tutti uguali $ad l;$ infatti:

V(tetraedro) $= 1$
$3 ∙l^{2}$ ∙VO

Ma $VO= \sqrt{VH2} -OH2 = \sqrt (l\sqrt{3}$
$$ 2 ) 2 $$
-(
$$ l 2) 2 = \sqrt{1 $$
$2 l^{2} = l\sqrt{2}$
$2$ quindi:

V(tetraedro) =
$3 ∙l^{2} ∙l\sqrt{2}$
$$ 2 = 1 $$
$6 l^{3}\sqrt{2}$ pertanto:

V(ottaedro regolare) $= 2∙1$
$6l^{3}\sqrt{2}= l^{3}∙\sqrt{2}$

V(ottaedro pieno) $= l^{3} ∙\sqrt{2}$
$= 53 ∙\sqrt{2}$
$cm3 \cong 58,926 cm3$

Siccome $\rho =$
$V V=$
$\rho$ . Quindi:

V(ottaedro vuoto) =
$\rho =$
$155 g$
$2,7 g/cm3 = 57,407 cm3$

V(cavità cubica) = V(ottaedro pieno) $-V(ottaedro$ vuoto) $= 58.926 cm3 -57,407 cm3 =$

$= 1,518 cm3$

Quindi lo spigolo della cavità è:

spigolo cavità $= \sqrt{1}.518$
$cm\cong 1,149 cm$

QUESITO $8$

Quante diagonali ha un poligono convesso di $n$ lati?

Le diagonali uscenti da un vertice sono $n-3$ (cioè tante quanti sono i vertici meno il
vertice stesso e i due adiacenti).

Il numero delle diagonali uscenti da $n$ vertici sono quindi $n∙(n-3).$ Ma ogni diagonale
contiene due vertici, quindi il numero delle diagonali è:

numero $diagonali= n∙(n-3)$

Questo numero può essere determinato anche come numero dei segmenti ottenuti con le
combinazioni semplici di $n$ oggetti (gli $n$ vertici) a due a due, diminuito del numero dei
$segment1$ che si ottengono congiungendo due vertici consecutivi, che sono pari al numero
dei lati $(n).$ Quindi:

numero $diagonali= Cn,2 -n= (n$
$2) -n= n(n-1)$
$2!$
$-n= n(n-1) -2n$
$= n(n-3)$

QUESITO $9$

Si calcoli il valore medio della funzione:
$y= 1$
$$ x^{2 $$

nell’intervallo $a\le x\le b,$ con $0 <$ a $< b$ , e si dimostri che esso è uguale alla media
geometrica tra i due valori che la funzione assume nei due estremi dell’intervallo.

Il valor medio richiesto è dato da:

$\int$ f(x)dx
$$ b a $$
$b-a$
$$ = \int 1 $$
$x^{2} dx$
$$ b a $$
$b-a$
$b-a\int$
$x^{2} dx$
$$ b a = 1 $$
$b-a[-1$
x]
$$ a b = 1 $$
$b-a∙[-1$
$b-(-1$
a)] =

$b-a[1$
$a-1$
b] =
$b-a∙b-a$
$$ ab = 1 ab $$

I valori assunti agli estremi dell’intervallo sono: f(a) =
$a^{2}$ e f(b) =
$b^{2}$ . La loro media
geometrica è:

$\sqrt{f}(a)f(b) = \sqrt$
$a^{2}$ ∙
$b^{2} =$
$ab=$
$\int$ f(x)dx
$$ b a $$
$b-a$

QUESITO $10$

Data la funzione:
f(x) $= x^{2} -x-4$
$x-1$

si verifichi che esiste un solo punto ξ interno all’intervallo chiuso $[-1,0],$ tale che la tangente
al diagramma in questo punto è parallela alla corda congiungente i due punti estremi del
diagramma.

Si tratta di verificare che la funzione soddisfa il teorema di Lagrange nell’intervallo $[-1,0],$
cioè che la funzione è continua nell’intervallo chiuso $[-1,0]$ e derivabile nell’intervallo aperto
$(-1,0);$ in tal caso esiste almeno un punto ξ interno all’intervallo dato tale che:

$f(0)-f(-1)$
$0-(-1)$
$4-1$
$1 = 3 =$ f'(ξ) (∗)

La funzione è continua per ogni $x$ diverso da $1,$ quindi lo è nell’intervallo $[-1,0].$
Risulta:

f'(x) =
$(2x-1)(x-1) -(x^{2} -x-4)(1)$
$(x-1)^{2}$
$= x^{2} -2x+ 5$
$(x-1)^{2}$

Che esiste per ogni $x$ diverso da $1,$ quindi la funzione è derivabile in $(-1,0).$

Per il Teorema di Lagrange esiste quindi almeno un punto dell’intervallo aperto $(-1,0)$ in
cui vale la (∗).

f'(x) =
$x^{2}-2x+5$
$(x-1)^{2} = 3$ se:

$x^{2} -2x+ 5 = 3(x-1)^{2} \implies 2x^{2} -4x-2 = 0 \implies x^{2} -2x-1 = 0$

$x= 1 \pm \sqrt{2}$ di cui è accettabile la sola soluzione $x= 1 -\sqrt{2} =$ ξ

Con la collaborazione di Angela Santamaria, Simona Scoleri e Stefano Scoleri
