---
tipo: soluzione
prova_stem: 2011_pni_2011_sessione_suppletiva_problema2_80
pdf: Prova_Maturita_2011.pdf
source: text
title: 2011 PNI Suppletiva — Problema 2 — Svolgimento
---


PNI 2011 SESSIONE SUPPLETIVA - PROBLEMA $2$

Si consideri la funzione:
f(x) $= (3 -x)\sqrt{x}+ 3$

Si studi tale funzione e si tracci il suo grafico $\gamma$ , su un piano riferito $ad$ un sistema di assi
cartesiani ortogonali Oxy.

f(x) $= (3-x)\sqrt{x}+ 3$

Dominio: $x\ge -3 \implies -3 \le x< +\infty$

Simmetrie notevoli:

Visto il dominio, la funzione non può essere pari né dispari.

Intersezioni con gli assi cartesiani:

Se $x= 0 , y= \sqrt{3};$ se $y= 0, x= 3$ e $x= -3$

Segno della funzione:

$y\ge 0$ se $x\le 3$ .

Limiti:

$limx\to +\infty (3 -x)\sqrt{x}+ 3 = -\infty$ ; non c’è asintoto obliquo perché la funzione non è un

infinito del primo ordine per $x$ che tende a più infinito.

Derivata prima:

f'(x) $= D((3 -x)\sqrt{x}+ 3) = -\sqrt{x}+ 3 + (3 -x)$ ∙
$2\sqrt{x}+ 3$
$= -2x-6 + 3 -x$
$2\sqrt{x}+ 3$
$= -3x-3$
$2\sqrt{x}+ 3$

f'(x) $\ge 0$ se $-3x-3 \ge 0, x\le -1$ :

la funzione cresce per $-3 \le x< -1$ e decresce per $-1 < x< +\infty$ .

$x= -1$ punto di massimo relativo (e assoluto) con valore $f(-1) = 4\sqrt{2}.$

Derivata seconda:

f''(x) $= D(-3x-3$
$2\sqrt{x}+ 3$
) $= -3x-15$
$4\sqrt{x+ 3}^{3} \ge 0$ se $x\le -5$ ,
cioè mai nel dominio

Quindi il grafico volge la sempre la concavità verso il basso. Non ci sono flessi.

Il grafico della funzione è il seguente:

Si scriva l’equazione della tangente $t$ alla curva $\gamma$ nel punto di intersezione con l’asse $y$ e si
calcoli l’area del triangolo che essa forma con gli assi cartesiani.

Il punto di intersezione con l’asse delle $y$ è $A= (0; 3\sqrt{3}).$ La tangente in A è:

$y-3\sqrt{3} = f'(0) ∙(x-0) \implies y-3\sqrt{3} = -3$
$$ 2\sqrt{3 $$
∙x $\implies y= -\sqrt{3}$
$2 x+ 3\sqrt{3}$

Il triangolo ABC che la tangente in A al grafico della funzione forma con gli assi cartesiani
ha area:

A(ABC) = AC∙BC
$= 3\sqrt{3} ∙6$
$u^{2} = 9\sqrt{3} u^{2} \cong 15.59 u^{2}$

Si calcoli il volume del cono $S$ generato da una rotazione attorno all’asse $x$ del
succitato triangolo e il volume del solido S’ generato dalla rotazione attorno all’asse $x$
della porzione di piano, situata nel I quadrante, limitata dalla curva $\gamma$ e dagli assi
cartesiani.

Il volume del cono $S$ è dato da:

V(cono $S) = 1$
$3 \pi R2h= 1$
$3 \pi ∙AC2 ∙BC= 1$
$3 \pi ∙(3\sqrt{3})$
$2 ∙6 = 54 \pi u^{3}$

Il volume del solido S’ si ottiene calcolando il seguente integrale:

V(S') $= \pi \int f^{2}(x)dx$
$$ b a $$
$= \pi \int [(3 -x)\sqrt{x}+ 3]$
$2dx= \pi \int (3 -x)2(x+ 3) dx=$
$$ 3 0 3 0 $$
$= \pi \int (x^{3} -3x^{2} -9x+ 27) dx=$
$$ 3 0 $$
$\pi [x^{4}$
$4 -x^{3} -9$
$2 x^{2} + 27x]$
$$ 0 3 = 135 $$
$4 \pi u^{3}$

Si scelga a caso un punto all’interno del cono $S.$ Si determini la probabilità che tale punto
risulti esterno al solido S’.

$p=$ V(cono $S) -V(S')$
V(cono $S)$
$= 1 -$
V(S')
V(cono $S) = 1 -$
$$ 135 $$
$4 \pi$
$54 \pi = 1 -135$
$4 ∙54 = 1 -5$

$8 = 0.375 = 37.5% = p$

Con la collaborazione di Angela Santamaria, Simona Scoleri e Stefano Scoleri

PNI 2011 - SESSIONE SUPPLETIVA

QUESITO $1$

Si $sa$ che certi uccelli, durante la migrazione, volano $ad$ un’altezza media di $260$ metri.
Un’ornitologa osserva uno stormo di questi volatili, mentre si allontana da lei in linea retta,
con un angolo di elevazione di $30^\circ$ . Se un minuto più tardi tale angolo si è ridotto a $20^\circ$ ,
con che velocità si stanno spostando gli uccelli?

Risulta: $AB=$
$$ BD $$
sen $300 = 2BD= 520 m , AC=$
$$ CK $$
sen $20^\circ =$
$$ 260 $$
$0.3420 m= 760.23 m$

Per il teorema del coseno si ha:

$BC2 = AB2 + AC2 -2 ∙AB∙AC∙cos(10^\circ ) =$ (5202 $+ 760.232 -1040 ∙760.23 ∙0.98) m^{2} \cong$

$\cong 73523.24 m^{2} \implies BC= \sqrt{73523}.24 m^{2} \cong 271.15 m$

Quindi la velocità media degli uccelli è data da:

v(uccelli) =
$$ BC $$
$1 min= 271.15 m$
$60 s$
$\cong 4.52 m/s$

QUESITO $2$

La funzione:
f(x) =
(e
$x-1)$

non è definita nel punto $x = 0,$ che è per essa un punto di discontinuità. Si precisi il tipo di
questa discontinuità, dopo aver esaminato il limite della f(x) per $x$ tendente a zero da
sinistra e per $x$ tendente a zero da destra.

$limx\to 0-$
(e
$x-1)$
$2 = 1 limx\to 0+$
(e
$x-1)$
$2 = 0+$ : discontinuità di prima specie con salto $1.$

Il grafico della funzione è il seguente:

QUESITO $3$

La retta di equazione $x= 8$ seca la parabola di equazione $x= y^{2} - 4y+ 3$ nei punti A e
$B.$ Fra i rettangoli inscritti nel segmento parabolico di base $AB$ si determini quello che
genera il cilindro di volume massimo in una rotazione di $180^\circ$ intorno all’asse della
parabola.

Indichiamo con $C$ il generico punto della parabola di ordinata $y\ge 2$ ed ascissa $x\le 8$ .

$C= (x; y),$ con $xV\le x\le 8$ , dove $xV= -1$ è l’ascissa del vertice della parabola, risulta:

$CH= yc-yH= y-2 , CD= 8 -xC= 8 -x= 8 -(y^{2} - 4y+ 3) = 5 -y^{2} + 4y$

Il cilindro richiesto ha volume:

V(cilindro) $= \pi R2h= \pi ∙CH2 ∙CD= \pi (y-2)^{2}(5 -y^{2} + 4y)$

Il volume è massimo quando lo è: $z= (y-2)^{2}(5 -y^{2} + 4y)$ con $2 \le y\le 5$

Essendo $z$ una funzione continua in un intervallo chiuso e limitato essa ammette massimo
e minimo assoluto (per il teorema di Weierstrass), che sono da ricercarsi tra i valori agli
estremi dell’intervallo ed i valori nei punti che annullano la derivata prima.

$f(2) = f(5) = 0$

$z' = 2(y-2)(5 -y^{2} + 4y) + (y-2)^{2}(-2y+ 4) = 0$ se:

$y= 0 , y=$
$4-3\sqrt{2}$
$\cong -0.121$ :non accettabile , $y=$
$$ 4+3\sqrt{2} 2 $$
$\cong 4.121$ :accettabile

Il massimo del volume si ha per $y=$
$$ 4+3\sqrt{2} 2 $$
e vale: V(max) $= 63.617 u^{3}$

QUESITO $4$

Si determini il campo di esistenza della funzione:

$f (x) = (3$ cos $x + sen2 x - 3)cos x$

Che cosa succederebbe se l’esponente fosse senx ?

La funzione (che ha periodo $T= 2\pi$ ) è definita se:

$3$ cos $x + sen2 x - 3 > 0 \cup {3$ cos $x + sen2 x - 3 = 0$
$cosx> 0$

$3$ cos $x + sen2 x - 3 > 0 , 3 cosx+ 1 -cos2 x-3 > 0 , cos2 x-3 cosx+ 2 < 0$ ,

$(1 < cosx< 2 ) \implies$ ∄ $x$

${3$ cos $x + sen2 x - 3 = 0$
$cosx> 0$
$\implies {cos2 x-3 cosx+ 2 = 0$
$cosx> 0$
$\implies$ {cos $x= 1$
$cosx> 0$ ,
$x= 2k\pi$

Quindi il campo di esistenza della funzione è: $x= 2k\pi$ , con $k\in Z.$

Notiamo che la funzione equivale a:

f(x) $= 0$ per $x= 2k\pi$ , con $k\in Z$

Se l’esponente fosse sen $x,$ la funzione non sarebbe mai definita. Infatti:

${3$ cos $x + sen2 x - 3 = 0$
$senx> 0$
$\implies {cos2 x-3 cosx+ 2 = 0$
$senx> 0$
$\implies$ {cos $x= 1$
$senx> 0$ ,
∄ $x$

QUESITO $5$

Si calcoli il valore medio della funzione $f (x) = ex(x^{2} + x+ 1),$ nell’intervallo $0 \le x \le 1.$

Il valor medio richiesto è dato da:

$\int$ f(x)dx
$$ b a $$
$b-a$
$\int ex(x^{2} + x+ 1)dx$
$$ 1 0 $$
$1 -0$
$= \int ex(x^{2} + x+ 1)dx$
$$ 1 0 $$

Cerchiamo una primitiva di $ex(x^{2} + x+ 1)$ integrando per parti:

$\int ex(x^{2} + x+ 1)dx= \int (ex)'(x^{2} + x+ 1)dx= ex(x^{2} + x+ 1) -\int ex(2x+ 1)dx=$

$= ex(x^{2} + x+ 1) -2 \int exxdx-\int exdx= ex(x^{2} + x+ 1) -2 \int$ (ex)'x $dx-ex=$

$= ex(x^{2} + x+ 1) -2 [xex-\int$ exdx] $-ex= ex(x^{2} + x+ 1) -2xex+ 2ex-ex+ k=$

$= ex(x^{2} -x+ 2) + k$

Quindi:

$\int$ f(x)dx
$$ b a $$
$b-a$
$= \int ex(x^{2} + x+ 1)dx= [ex(x^{2} -x+ 2)]0$
$1 = 2e-2 = 2(e-1)$
$$ 1 0 $$
$\cong 3.44$

QUESITO $6$

Si determini un numero positivo $N$ tale che, per $x > N,$ la funzione f(x) $= 20,3x$ è sempre
maggiore della funzione g(x) $= x^{30}.$

Dobbiamo trovare un numero positivo $N$ tale che, per ogni $x>N,$ risulti sempre:

$20,3 x> x^{30}$

Rappresentiamo in modo qualitativo nello stesso sistema di riferimento le due funzioni.
Notiamo che, per valori positivi della $x,$ le due curve si incontrano una prima volta nei
pressi $dell’1;$ siccome f(x) è un infinito di ordine superiore rispetto a g(x), il grafico della $f$
$ad$ un certo punto dovrà essere al di sopra del grafico della $g.$

Per $x=10: f(10) = 8, g(10) =$ 1010 : è ancora maggiore la $g.$

Per $x=100: f(100) = 230 \cong 1.07 ∙109, g(10) = 10030$ : è ancora maggiore la $g.$

Per $x=1000: f(1000) =$ 2300 $\cong 2.04 ∙1090, g(10) =$ 1090 : è maggiore la $f.$

Quindi, per $x>1000$ risulterà sempre
$20,3 x> x^{30}$

Un valore di $N$ per cui la funzione f(x) $= 20,3x$ è sempre maggiore della funzione
g(x) $= x^{30}$ è quindi $N=$ 1000.

QUESITO $7$

Tenuto conto che:
$\pi$
$2 -1 = \int \sqrt{1} -x$
$1 + x dx$
$$ 1 0 $$

si calcoli un’approssimazione di
$\pi$
$2$ , utilizzando uno dei metodi di integrazione numerica
studiati.

Posto f(x) $= \sqrt$
$1-x$
$1+x$ , consideriamo l’intervallo $[0; 1]$ e dividiamolo in $n$ parti; poniamo
$h=$
$1-0$
$n=$
$n.$

Utilizzando, per esempio, la formula dei trapezi, l’integrale dato può essere approssimato
mediante la formula:

$\int f(x)dx\cong b-a$
$[f(x^{0}) +$ f(xn)
$+ f(x^{1}) + f(x^{2}) + ⋯+ f(xn-1)]$
$$ b a $$

Nel nostro caso, ponendo per esempio $n=5,$ abbiamo $h=$
$$ 1 5 $$

$x^{0} = 0, x^{1} = 1$
$5, x^{2} = 2$
$5, x^{3} = 3$
$5 , x^{4} = 4$
$5 , x^{5} = 1$

Quindi si ha la seguente approssimazione:

$\int \sqrt{1} -x$
$1 + x dx$
$$ 1 0 $$
$\cong 1$
$5 ∙[f(0) + f(1)$
$+ f(1$
$5) + f(2$
$5) + f(3$
$5) + f(4$
$5)] \cong 0.5609$

Quindi:
$\pi$
$2 -1 \cong 0.5609$ da cui
$\pi$
$2 \cong 1.5609$

Notiamo che il valore esatto di
$\pi$
$2$ è: $1.57079$ ... . .

QUESITO $8$

La regione del I quadrante delimitata dall’ellisse di equazione
$$ x^{2 $$
$4 + y^{2} = 1$ e dagli assi
cartesiani è la base di un solido $F$ le cui sezioni, ottenute con piani perpendicolari all’asse
$y,$ sono tutte quadrati. Si calcoli il volume di $F.$

Il volume del solido $F$ si ottiene calcolando il seguente integrale:

V(F) $= \int$ A(y)dy
$$ 1 0 $$

dove A(y) è l’area del quadrato di lato $AB= xA$ ; quindi A(y) $= x^{2} = 4(1 -y^{2}).$

V(F) $= \int$ A(y)dy
$$ 1 0 $$
$= \int 4(1 -y^{2})dy= 4 ∙[y-y^{3}$
$$ 3 ] 0 1 $$
$= 4$ ∙
$$ 1 0 $$
$(1 -1$
$$ 3) = 8 $$
$3 u^{3} \cong 2.667 u^{3}$

QUESITO $9$

Un bersaglio è costituito da tre cerchi concentrici, i cui raggi misurano rispettivamente $5, 3$
e $1.$ Un arciere ha probabilità $1⁄2$ di colpire il bersaglio. Qual è la probabilità che lo colpisca
in un punto appartenente al cerchio di raggio $3$ ma non a quello di raggio $1$ ?

L’area favorevole è data dall’area della corona circolare di raggi $1$ e $3,$ l’area possibile (con
probabilità $1⁄2)$ è data dall’area del cerchio di raggio $5.$ Quindi:

$p= 1$
$2$ ∙area favorevole
area possibile
$2 ∙\pi ∙(32 -12)$
$\pi ∙52$
$2 ∙8$
$$ 25 = 4 $$
$25 = 0.16 = 16%$

QUESITO $10$

Sia $P$ un punto fissato su una circonferenza; quale è la probabilità che prendendo su
questa due punti a caso A e $B,$ l’angolo AP̂B sia acuto? Si illustri il ragionamento seguito.

Fissato $P,$ consideriamo la tangente in $P$ alla circonferenza e su di essa un punto $D.$

Deve essere $\alpha = |\gamma -\beta | <$
$\pi$

Gli angoli $\gamma$ e $\beta$ sono legati alla scelta di A e $B$ ed hanno le limitazioni:

$0 \le \gamma \le \pi 0 \le \beta \le \pi$

La possibilità di scelta per A e $B$ equivale quindi all’area del quadrato di area $\pi 2;$ le scelte
di A e $B$ favorevoli equivalgono all’area della parte del quadrato di lato $\pi$ che soddisfa la
condizione: $|\gamma -\beta | <$
$\pi$

L’area favorevole è data da:

A(poligono EOHGCF) = A(quadrato) $-2Area(HBG) = \pi 2 -2$ ∙(
$\pi$
$2 ∙\pi$
$$ 2 2 ) = 3 $$
$4 \pi 2$

La probabilità richiesta è quindi data da:

$p=$
$4 \pi 2$
$\pi 2 = 3$

Con la collaborazione di Angela Santamaria, Simona Scoleri e Stefano Scoleri
