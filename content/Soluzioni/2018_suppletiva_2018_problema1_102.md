---
tipo: soluzione
prova_stem: 2018_suppletiva_2018_problema1_102
pdf: Prova_Maturita_2018.pdf
source: text
title: 2018 Suppletiva — Problema 1 — Svolgimento
---


Suppletiva 2018 - Problema $1$

SESSIONE SUPPLETIVA - 2018 - PROBLEMA $1$

$y= k|sen(x)|$ con $x\in [0; 3\pi$ ],
$k> 0$

La funzione data ha periodo $\pi$ . Consideriamo la parte di grafico in $[0; \pi$ ] e la sua
simmetrica rispetto alla bisettrice $y=x:$ essa rappresenta la prima parte della cornice
sull’asse $y.$ I due grafici si intersecano sulla suddetta bisettrice. La funzione $y=$
$k|sen(x)|$ ha il primo massimo in (
$\pi$
$2 ; k)$ e quindi incontra la sua simmetrica in tale punto
se $k=$
$\pi$

Suppletiva 2018 - Problema $1$

L’area della quattro regioni da rivestire è uguale a $8$ volte l’area $S$ della regione
compresa fra i grafici delle curve $y=$
$\pi$
$2$ sen $x$ e $y= x$ in $[0;$
$\pi$

Risulta:

$S= \int$ (
$\pi$
$2$ sen $x-x)$
$\pi$
$$ 2 0 $$
$dx= [-$
$\pi$
$2$ cos $x-$
$2 x^{2}]$
$\pi$
$2 = (-$
$\pi 2$
$\pi$
$2) dm2$

Quindi l’area A della regione da colorare (approssimiamo $\pi$ con $3.14)$ è:

$A= 8S= 8 (-\pi 2$
$8 + \pi$
$2) = (4\pi -\pi 2) dm2 \cong (2.70) dm2$

Suppletiva 2018 - Problema $1$

I primi archi della decorazione sull’asse $x$ e sull’asse $y,$ si devono incontrare solo
nell’origine degli assi e sono simmetrici rispetto alla retta $y=x.$ Essi sono tangenti
nell’origine se entrambi sono tangenti alla retta $y=x,$ quindi l’arco sull’asse $x$ deve avere
equazione $y=$ sen x:quindi $k= 1.$ Se $k> 1$ i due grafici si incontrano in un altro punto
oltre all’origine; per incontrarsi solo nell’origine deve quindi essere $k\le 1$ . In particolare
per $k= 0$ non c’è alcuna decorazione e lo specchio è tangente al quadrato.

Esempi con $k=1, k>1, 0<k<1:$

Esprimiamo in funzione di $k\in [0; 1]$ l’area della parte di cornice compresa fra i lati e le
quattro curve goniometriche. Tale area è $12$ volte l’area $R$ della regione compresa fra il
grafico di $y= k$ sen $x$ e l’asse delle $x$ nell’intervallo $[0; \pi$ ]. Risulta:

$R= \int (k$ sen $x)$
$\pi$
$dx= k[-cos x]0$
$\pi = k(1 + 1) = (2k) dm2$

Quindi l’area richiesta è:

$Area= 12R= (24k) dm2$

Suppletiva 2018 - Problema $1$

La circonferenza contorno dello specchio ha centro nel centro del quadrato, che ha
coordinate $C=$ (
$2 \pi$ ;
$2 \pi$ ) e raggio uguale
$2 \pi -k$ , essendo $k$ l’ordinata del massimo della
funzione $y= k$ sen $x$ nell’intervallo $[\pi ; 2\pi$ ]. Quindi:

Area(specchio) $= \pi (3$
$2 \pi -k$ )
$$ 2 dm2 $$

L’area minima (per la fig. $3)$ dello specchio si avrà in corrispondenza del massimo valore
di $k,$ che è $1.$ Approssimando $\pi$ con $3.14$ si ha:

Area(minima) $= \pi (3$
$2 \pi -1$ )
$dm2 \cong 43.22 dm2$

L’area massima dello specchio si avrà in corrispondenza del minimo valore di $k,$ che è $0:$

Area(massima) $= \pi (3$
$2 \pi$ )
$dm2 = 9\pi 3$
$\cong 69.66 dm2$

L’area da verniciare è data dall’area del quadrato meno l’area dello specchio meno l’area
della regione compresa fra le quattro curve ed i lati del quadrato:

Area(da verniciare) $= [(3\pi )^{2} -\pi (3$
$2 \pi -1$ )
$-24] dm2 \cong 21.52 dm2$

Siccome con $1$ litro di vernice si coprono $6 m^{2} = 600 dm2,$ con $1 ml$ si copriranno $0.6 dm2$
E quindi con $125 ml$ si copriranno $(0.6 )(125)dm2 = 75 dm2.$
Per passare due mani di vernice occorre ricoprire $21.52 ∙2 dm2 = 43.04 dm2$

Quindi la quantità di vernice a disposizione è sufficiente.

Suppletiva 2018 - Problema $1$

La quantità di vernice richiesta in funzione di $k$ è:

Area(k) $= 2 [(3\pi )^{2} -\pi$ (
$2 \pi -k$ )
$-24k]$ che è massima se è massima

y(k) $= (3\pi )^{2} -\pi (3$
$2 \pi -k$ )
$-24k= -\pi k^{2} + (3\pi 2 -24)k-9\pi 3$
$4 + 9\pi 2$

Il grafico di questa funzione è una parabola con la concavità verso il basso, quindi il suo
massimo si avrà nel vertice (se incluso nelle limitazioni di $k),$ cioè in:

$k= -b$
$2a= -3\pi 2 -24$
$-2\pi$
$= 3\pi 2 -24$
$2\pi$
$\cong 0.89,$
che è compreso nei limiti di $k.$

La quantità di vernice richiesta è massima se $k=$
$3\pi 2-24$
$2\pi$
$\cong 0.89.$

Con la collaborazione di Angela Santamaria
