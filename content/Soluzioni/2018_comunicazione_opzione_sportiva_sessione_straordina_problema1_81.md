---
tipo: soluzione
prova_stem: 2018_comunicazione_opzione_sportiva_sessione_straordina_problema1_81
pdf: Prova_Maturita_2018.pdf
source: text
title: >-
  Soluzioni —
  2018_comunicazione_opzione_sportiva_sessione_straordina_problema1_81
---
# Soluzioni — 2018_comunicazione_opzione_sportiva_sessione_straordina_problema1_81

## Problema 1

Comunicazione Opzione sportiva
Sessione straordinaria 2018 - Problema $1$

SCIENTIFICO COMUNICAZIONE OPZIONE SPORTIVA
SESSIONE STRAORDINARIA 2018

PROBLEMA $1$

Due archi illimitati di due parabole (con assi paralleli all’asse $y$ di un sistema di
riferimento), ciascuno dei quali contenente il vertice della parabola, sono disposti in modo
da passare per tutti e quattro i quadranti del piano e raccordarsi nell’origine, formando il
grafico $\Gamma$ di una funzione:

f(x) $= {f^{1}(x)$ se $x< 0$
$f^{2}(x)$ se $x\ge 0$

che è derivabile in $R.$ La figura $1$ sottostante ne fornisce un possibile esempio.

Il grafico $\Gamma$ interseca l’asse $x$ nell’origine O e nei punti A di ascissa $xA< 0$ e $B$ di ascissa
$xB> 0$ .

Dimostra che, se la funzione $f$ soddisfa le richieste precedenti, si ha:

$f^{1}(x) = ax2 + bx , f^{2}(x) = cx2 + bx$

dove a, $b, c$ sono opportuni coefficienti reali non nulli. Dimostra che le rette tangenti a $\Gamma$ in
A e in $B$ sono parallele.

Le due parabole passano per l’origine, quindi le loro equazioni mancano del termine noto.

Comunicazione Opzione sportiva
Sessione straordinaria 2018 - Problema $1$

Il punto A ha coordinate $A= (-$
a; $0)$ , quindi $xA= -$
$a< 0,$ quindi
$a> 0$ da cui
$b> 0,$ poiché, essendo la concavità della parabola di sinistra rivolta verso l’alto, è $a> 0.$

Il punto $B$ ha coordinate $B= (-$
$c; 0)$ , con $xB= -$
$c> 0,$ quindi
$c< 0$ da cui,
essendo la concavità della parabola di destra rivolta verso il basso, è $c< 0$ e $b> 0.$

Poiché la funzione $f$ deve essere derivabile nell’origine, indicati con $b^{1}$ e $b^{2}$ i coefficienti
di $x$ nelle equazioni di $f^{1}$ ed $f^{2}$ deve essere:

$(f^{1}')-(0) = (f^{2}')+(0),$ da cui: $(2ax+ b^{1})^{0} = (2cx+ b^{2})^{0} \Rightarrow b^{1} = b^{2} = b$

Quindi è verificato che $f^{1}(x) = ax2 + bx , f^{2}(x) = cx2 + bx,$ con $a> 0, b> 0, c< 0$ .

La tangente a $\Gamma$ in A ha coefficiente angolare $mA= f^{1}$
' (-
a) $= -2b+ b= -b$ .
La tangente a $\Gamma$ in $B$ ha coefficiente angolare $mB= f^{2}$
' (-
$c) = -2b+ b= -b$ .

Le rette tangenti a $\Gamma$ in A e in $B$ sono quindi parallele.

Considerando le funzioni g(x) = f'(x), F(x) $= \int$ f(t)
$$ x 0 $$
$dt$ mostra, con le opportune
argomentazioni, che il grafico di $g$ è costituito da due semirette che intersecano l’asse $x$
nei punti di ascisse
$$ xA $$
$2$ e
$$ xB $$
$2$ e che il grafico di $F$ ha due punti stazionari di ascisse $xA$ e
$xB$ e due punti di flesso di ascisse
$$ xA $$
$2$ e
$$ xB 2 . $$

Se $x< 0$ g(x) $= f^{1}$
'(x) $= 2ax+ b$
Se $x> 0$ g(x) $= f^{2}$
'(x) $= 2cx+ b$
Se $x= 0$ g(x) $= b$

Quindi:

g(x) = {
$2ax+ b$ se $x< 0$
$b$ se $x= 0$
$2cx+ b$ se $x> 0$
: che sono due semirette uscenti dal punto $(0; b)$

Se $y= 0$ si ha: $x= -$
$2a=$
$$ xA $$
$2$ e $x= -$
$2c=$
$$ xB 2 . $$

N.B.

Allo stesso risultato si può pervenire graficamente osservando che, essendo la $f$ formata
da due parabole (quindi funzioni quadratiche), la sua derivata sarà formata da due rette
(funzioni lineari); siccome la derivata della $f$ è nulla nei vertici (punti a tangenti orizzontali)
e tali vertici hanno ascisse
$$ xA $$
$2$ e
$$ xB $$
$2$ , possiamo concludere che il grafico di $g$ è costituito da

Comunicazione Opzione sportiva
Sessione straordinaria 2018 - Problema $1$

due semirette che intersecano l’asse $x$ nei punti di ascisse
$$ xA $$
$2$ e
$$ xB 2 . $$

Analizziamo ora la funzione F(x) $= \int$ f(t)
$$ x 0 $$
$dt.$ Essendo $F$ continua e derivabile con
derivata uguale $ad$ f(x): F'(x) = f(x) (per il teorema fondamentale del calcolo integrale),
risulta F'(x) $= 0$ dove f(x) $= 0,$ quindi per $x= xA$ e $x= xB:$

il grafico di $F$ ha due punti stazionari di ascisse $xA$ e $xB.$

Per determinare i flessi di $F$ studiamo il segno della derivata seconda:

F''(x) = f'(x) $> 0 (f$ crescente) se
$$ xA $$
$2 < x<$
$$ xB $$
$2$ :grafico di $F$ con concavità verso l'alto
F''(x) = f'(x) $< 0 (f$ decrescente) se $x< xA$
$2$ vel $x> xB$
$2$ :
grafico di $F$ con concavità verso l'alto.

Quindi $F$ ha due punti di flesso di ascisse
$$ xA $$
$2$ e
$$ xB 2 $$

Determina le espressioni di $f^{1}(x)$ e $f^{2}(x)$ in modo tale che la retta tangente a $\Gamma$ nel
punto A abbia equazione $y= -2x- 8$ e l’area della superficie limitata compresa tra $\Gamma$ e
l’asse $x$ contenuta nel semipiano $x> 0$ sia pari a $9$ volte quella dell’analoga superficie
contenuta nel semipiano $x< 0.$

Affinché $f^{1}(x)$ abbia tangente in A di equazione $y= -2x- 8$ deve essere:

$mA= f^{1}$
' (-
a) $= -b= -2,$ quindi $b= 2$ .

Siccome la retta passa per $A= (-$
a; $0),$ deve essere: $0 = -2 (-$
a) $-8,$

$a= 4, a=$
$$ b 4 = 1 2 $$

Quindi: $f^{1}(x) =$
$2 x^{2} + 2x$

L’area della superficie limitata compresa tra $\Gamma$ e l’asse $x$ contenuta nel semipiano $x < 0$ è :

$-\int f^{1}$
$$ 0 xA $$
$(x) dx= -\int (1$
$2 x^{2} + 2x)$
$-4$
$dx= -[1$
$6 x^{3} + x^{2}]$
$-4$
$= -(32$
$3 -16) = 16$

L’area della superficie limitata compresa tra $\Gamma$ e l’asse $x$ contenuta nel semipiano $x > 0$ è :

$$ \int $$
$f^{2}(x)$
$$ xB 0 $$
$dx= \int$
$(cx2 + 2x)$
$-2$
$$ c 0 $$
$dx= [1$
$3 cx3 + x^{2}]$
$-2$
$$ c = 1 $$
$3 c(-8$
$c^{3}) + 4$
$c^{2} = -8$
$3c^{2} + 4$
$c^{2} = 4$
$3c^{2}$

Comunicazione Opzione sportiva
Sessione straordinaria 2018 - Problema $1$

Affinché l’area della superficie limitata compresa tra $\Gamma$ e l’asse $x$ contenuta nel semipiano
$x > 0$ sia pari a $9$ volte quella dell’analoga superficie contenuta nel semipiano $x < 0$ deve
essere:

$$ \int $$
$f^{2}(x)$
$$ xB 0 $$
$dx= 9 (-\int$
$$ f^{1} 0 xA $$
$(x) dx)$ ,
$3c^{2} = 9$ (
$$ 16 $$
$3 ) , c^{2} =$
$36 , c= -$
$6$ (ricordiamo che $c< 0).$

Quindi: $f^{2}(x) = -$
$6 x^{2} + 2x$

Osserviamo che le aree delle due superfici (segmenti parabolici) potrebbero essere
calcolate anche mediante il teorema di Archimede.

Dimostra che anche il rapporto tra le aree dei triangoli formati dalle rette tangenti al
grafico $\Gamma$ nei punti A, O e $B$ e l’asse delle ascisse è pari a $9.$

Osserviamo che i due triangoli AOC e BOD sono simili per il terzo criterio di similitudine
essendo congruenti gli angoli ACO e BDO (alterni interni nelle parallele $AC$ e $BD$ con la
trasversale $CD)$ e gli angoli OAC e OBD (alterni interni nelle parallele $AC$ e $BD$ con la
trasversale $AB).$

$A= (-4; 0)$ e $B= (12; 0).$

Il rapporto di similitudine è $k=$
$$ OB $$
$OA=$
$$ 12 $$
$4 = 3$ . Il rapporto fra le aree è $k^{2} = 32 = 9.$
Quindi:
Area(OBD) $= 9Area(OAC).$

Con la collaborazione di Angela Santamaria
