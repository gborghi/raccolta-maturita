---
tipo: soluzione
prova_stem: 2023_sessione_straordinaria_2023_problema1_47
pdf: Prova_Maturita_2023.pdf
source: text
title: Soluzioni — 2023_sessione_straordinaria_2023_problema1_47
---
# Soluzioni — 2023_sessione_straordinaria_2023_problema1_47

## Problema 1

Sessione Straordinaria 2023 - Problema $1$

LICEO SCIENTIFICO STRAORDINARIA 2023 - PROBLEMA $1$

Si consideri la famiglia di funzione fn(x) $= 2 -$
$x+$
$xn$ con $n\in N$ e $n> 1.$

a)

Verificare che tutte le curve rappresentate dalle funzioni della famiglia fn(x) passano per uno
stesso punto e scrivere le sue coordinate. Determinare, in funzione del parametro $n,$ le
ascisse degli estremi e dei flessi e calcolarne il limite, con $n\to \infty$ . Scrivere le equazioni degli
asintoti e tracciare i grafici delle funzioni $fn$ , evidenziando le differenze tra i casi in cui $n$ è
pari da quelli in cui $n$ è dispari.

$y=$ fn(x) $= 2-$
$x+$
$xn$ con $n\in N$ e $n> 1$

Con $n=2$ abbiamo: $y= 2 -$
$x+$
$x^{2}$ e con $n=3: y= 2 -$
$x+$
$x^{3}$ . Intersechiamo queste due curve:

$y= 2 -3$
$x+ 3$
$$ x^{2 $$
$y= 2 -3$
$x+ 3$
$$ x^{3 $$
$\Rightarrow 2 -3$
$x+ 3$
$x^{2} = 2 -3$
$x+ 3$
$x^{3}$ ,
$x^{2} = + 3$
$x^{3} , x= 1$ ,
quindi: $y= 2$

Verifichiamo che il punto $(1; 2)$ appartiene a tutte le curve:

$2 = 2 -3$
$$ 1 + 3 $$
$1n , 2 = 2$ : verificato per ogni $n.$

Quindi tutte le curve passano per il punto di coordinate $(1; 2).$

Cerchiamo le ascisse degli estremi e dei flessi delle curve in funzione di $n.$

$y= 2 -3$
$x+ 3$
$$ xn $$

Dominio: $x\ne 0$ (per tali valori la generica funzione è continua e derivabile due volte, quindi nelle ascisse
degli estremi si deve annullare la derivata prima e nelle ascisse dei flessi la derivata seconda).

$y' = 3$
$x^{2} -3n$
$xn+1 = 0, xn-1 -n= 0,$
$xn-1 = n$
$y' \ge 0,$
$xn-1-n$
$xn+1$
$\ge 0;$ se $n$ è dispari $y' \ge 0$ se $xn-1 -n\ge 0, x\le -$
$\sqrt{n}$
$n-1$
vel $x\ge$
$\sqrt{n}$
$n-1$

-
$\sqrt{n}$
$n-1$
$\sqrt{n}$
$n-1$

$N +$
$D +$
y’ +
$M m$

Sessione Straordinaria 2023 - Problema $1$

Quindi per $n$ dispari la funzione cresce se $x< -$
$\sqrt{n}$
$n-1$
, decresce per -
$\sqrt{n}$
$n-1$
$< x< 0$ e per
$0 < x<$
$\sqrt{n}$
$n-1$
, cresce per $x>$
$\sqrt{n}$
$n-1$
. Perciò $x= -$
$\sqrt{n}$
$n-1$
è punto di massimo relativo e $x=$
$\sqrt{n}$
$n-1$
è punto
di minimo relativo.

Per $n$ pari $y' \ge 0,$
$xn-1-n$
$xn+1$
$\ge 0$ per $x< 0$ e per $x\ge$
$\sqrt{n}$
$n-1$
perciò:
la funzione cresce se $x< 0,$ decresce per $0 < x<$
$\sqrt{n}$
$n-1$
e cresce per $x>$
$\sqrt{n}$
$n-1$
. Pertanto $x=$
$\sqrt{n}$
$n-1$
è
punto di minimo relativo.

$\sqrt{n}$
$n-1$

$N -$
$D -$
y’ +
∄ $m$
Riepilogando:
se $n$ è pari un estremo per $x=$
$\sqrt{n}$
$n-1$
(minimo relativo)
se $n$ è dispari abbiamo due estremi di ascisse: $x= -$
$\sqrt{n}$
$n-1$
(punto di massimo relativo) e $x=$
$\sqrt{n}$
$n-1$
(punto
di minimo relativo).

Calcoliamo i limiti richiesti:
Per $n$ pari: lim
$n\to \infty$
$\sqrt{n}$
$n-1$
= lim
$n\to \infty (n$
$n-1) : F.$ I $\infty 0;$ lim
$n\to \infty$ (e)ln $n$
$n-1 =$ lim
$n\to \infty$ (e)
$n-1 ln n=$ lim
$n\to \infty$ e(ln $n$
$n-1) = e^{0} = 1$
(ricordiamo che $n-1$ è infinito di ordine superiore rispetto a $ln n$ )

Per $n$ dispari: lim
$n\to \infty (\pm$
$\sqrt{n}$
$n-1$
) $= \pm 1$ (i passaggi sono uguali a quelli fatti per $n$ pari).

y'' $= -6$
$x^{3} + 3n(n+ 1)$
$xn+2$
$-2(xn-1) + n(n+ 1) = 0,$
$xn-1 = n(n+ 1)$

y'' $\ge 0$ se
$-2(xn-1)+n(n+1)$
$xn+2$
$\ge 0;$ se $n$ è pari y'' $\ge 0$ per $-2(xn-1) + n(n+ 1) \ge 0 : x\le$
$\sqrt{n}(n+1)$
$n-1$

(con $x\ne 0)$ perciò $x=$
$\sqrt{n}(n+1)$
$n-1$
è punto di flesso:

$\sqrt{n}(n+1)$
$n -1$

y’’ +
∄ $F$

Se $n$ è dispari il numeratore è positivo per -
$\sqrt{n}(n+1)$
$n-1$
$< x<$
$\sqrt{n}(n+1)$
$n-1$

ed il denominatore è positivo per $x> 0$ perciò y'' $> 0$ se $x< -$
$\sqrt{n}(n+1)$
$n-1$
e per
$0 < x<$
$\sqrt{n}(n+1)$
$n-1$
-
$\sqrt{n}(n+1)$
$n-1$
$\sqrt{n}(n+1)$
$n-1$

$N -$
$D -$
y’’ +
$F^{1}$ ∄ $F^{2}$

Sessione Straordinaria 2023 - Problema $1$

Pertanto il grafico volge la concavità verso l’alto se $x< -$
$\sqrt{n}(n+1)$
$n-1$
e $0 < x<$
$\sqrt{n}(n+1)$
$n-1$
e verso il
basso se -
$\sqrt{n}(n+1)$
$n-1$
$< x< 0$ e $x>$
$\sqrt{n}(n+1)$
$n-1$
; perciò $x= \pm$
$\sqrt{n}(n+1)$
$n-1$
sono punti di flesso.
Riepilogando:
se $n$ è pari abbiamo un flesso per $x=$
$\sqrt{n}(n+1)$
$n-1$

se $n$ è dispari abbiamo due flessi di ascisse: $x= \pm$
$\sqrt{n}(n+1)$
$n-1$

Calcoliamo ora limiti richiesti:
Per $n$ pari: lim
$n\to \infty$
$\sqrt{n}(n+1)$
$n-1$
= lim
$n\to \infty$ (
$n(n+1)$
$$ 2 1 $$
$n-1) : F.$ I $\infty 0;$ lim
$n\to \infty (e)lnn(n+1)$
$$ 2 1 $$
$n-1$
= lim
$n\to \infty$ (e)
$n-1 lnn(n+1)$
$$ 2 = lim $$
$n\to \infty$ e
$lnn(n+1)$
$n-1$
$= e^{0} = 1$
$(n-1$ è infinito di ordine superiore rispetto a $ln$
$n(n+1)$
Per $n$ dispari: lim
$n\to \infty (\pm$
$\sqrt{n}(n+1)$
$n-1$
) $= \pm 1$ (i passaggi sono uguali a quelli fatti per $n$ pari).

Cerchiamo ora gli asintoti
Siccome lim
$x\to \pm \infty (2 -$
$x+$
$xn) = 2,$ tutte le curve hanno l’asintoto orizzontale di equazione $y= 2.$
Non ci sono quindi asintoti obliqui.
Cerchiamo gli eventuali asintoti verticali:
$$ lim $$
$x\to 0 (2 -3$
$x+ 3$
$xn) =$ lim
$x\to 0(2 -3x-1 + 3x-n-1) =$ lim
$x\to 0(2 -3x-1 + 3x-n) =$ lim
$x\to 0(3x-n) = \infty$
(siccome $-n< -1$ per ogni $n,$ essendo $n> 1, x-n$ è infinito di ordine superiore rispetto a $x-1$ per $x\to 0).$
In particolare, per $n$ pari sia il limite destro che il limite sinistro per $x\to 0$ è $+\infty$ , se $n$ è dispari il limite destro è $+\infty$ ,
quello sinistro $-\infty$ .
Tutte le curve hanno l’asintoto verticale di equazione $x= 0.$

Quanto ricavato sul dominio, sugli estremi, sui flessi e sugli asintoti ci permette di tracciare un grafico qualitativo
delle funzioni.

Grafico delle funzioni per $n$ pari (per esempio $n=2):$

Sessione Straordinaria 2023 - Problema $1$

Grafico per $n$ dispari (per esempio $n=3):$

$b)$

Si assuma $n=3,$ studiare la funzione $f^{3}(x)$ e tracciare un suo grafico rappresentativo,
dimostrando che ammette un unico zero di segno negativo. Discutere, al variare del
parametro $k\in R,$ il numero e il segno delle soluzioni dell’equazione $f^{3}(x) = k.$

Per $n=3$ si ha:
$y= 2 -3$
$x+ 3$
$$ x^{3 $$

Sfruttando quanto abbiamo già detto nel caso generale abbiamo:

Dominio: $x\ne 0$

$$ lim $$
$x\to \pm \infty (2 -$
$x+$
$x^{3}) = 2:$ asintoto orizzontale di equazione $y= 2.$

Non ci sono quindi asintoti obliqui.

La funzione cresce se $x< -\sqrt{3}$ , decresce per $-\sqrt{3} < x< 0$ e per
$0 < x< \sqrt{3},$ cresce per $x> \sqrt{3}$ . Perciò $x= -\sqrt{3}$ è punto di massimo relativo e $x= \sqrt{3}$ è punto di
minimo relativo (ordinata del massimo: $2 +$
$\sqrt{3} -$
$$ 3 3\sqrt{3} = 2 + 2 $$
$\sqrt{3}$ , ordinata del minimo
$2 -$
$$ 3 \sqrt{3} + 3 $$
$3\sqrt{3} = 2 -$
$$ 2 \sqrt{3}). $$

In base a quanto visto nel caso generale, con $n=3$ abbiamo: il grafico volge la concavità verso l’alto se
$x< -\sqrt{6}$ e $0 < x< \sqrt{6}$ e verso il basso se $-\sqrt{6} < x< 0$ e $x> \sqrt{6}$ ; perciò $x= \pm \sqrt{6}$ sono punti di
flesso, con ordinate rispettivamente:

se $x= \sqrt{6} , y= 2 -$
$$ 3 \sqrt{6} + 3 $$
$6\sqrt{6} = 2 -$
$2\sqrt{6} \cong 0.98$

se $x= -\sqrt{6} , y= 2 +$
$\sqrt{6} -$
$$ 3 6\sqrt{6} = 2 + 5 $$
$2\sqrt{6} \cong 3.02$

Sessione Straordinaria 2023 - Problema $1$

Il grafico della funzione in oggetto è quindi il seguente:

Notiamo che il grafico taglia l’asintoto orizzontale in due punti; troviamone le ascisse:

$2 -3$
$x+ 3$
$x^{3} = 2 , 3$
$x^{3} = 3$
$x , x^{2} = 1: x= \pm 1.$

Il grafico della funzione taglia l’asse delle $x$ in un solo punto (di ascissa negativa, compresa fra $-1$ e $0),$
quindi la funzione ha un unico zero di segno negativo.

Discutiamo il numero ed il segno delle soluzioni dell’equazione $f^{3}(x) = k.$

Ciò equivale a studiare il numero ed il segno delle ascisse dei punti di intersezione fra i grafici delle
funzioni di equazioni $y= f^{3}(x)$ ed $y= k$ (rette parallele all'asse $x).$
Osservando il grafico di $y= f^{3}(x)$ e ricordando che l’ordinata del massimo relativo è $2 +$
$\sqrt{3}$ e quella del
minimo relativo $2 -$
$\sqrt{3}$ , si deduce che l’equazione $f^{3}(x) = k$ ha:

$1$ soluzione (negativa) per $k< 2 -$
$$ 2 \sqrt{3} - $$
$1$ soluzione negativa ed una positiva (doppia) per $k= 2 -$
$$ 2 \sqrt{3} - $$
$3$ soluzioni (una negativa ed una positiva) per $2 -$
$\sqrt{3} < x< 2$
$2$ soluzioni (una negativa e due positive) per $k= 2$
$3$ soluzioni (due negative ed una positiva) per $2 < k< 2 +$
$$ 2 \sqrt{3} - $$
$1$ soluzione negativa (doppia) ed una positiva per $k= 2 +$
$$ 2 \sqrt{3} - $$
$1$ soluzione positiva per $k> 2 +$
$$ 2 \sqrt{3 $$

Sessione Straordinaria 2023 - Problema $1$

$c)$
Si consideri la funzione g(x) $= 2 -$
$x$ e verificare che, $\forall x> 0,$ vale la disuguaglianza
fn(x) > g(x) indipendentemente dal valore di $n.$ Si consideri l’integrale
I(t) $= \int$ (fn(x) $-g(x))$
$$ t 1 dx $$
che esprime l’area della regione delimitata dai grafici delle funzioni $fn$ e $g$ e dalle rette di
equazioni $x= 1$ e $x= t, t> 1.$ Si calcolino I(t) e il lim
$t\to +\infty$ I(t) , fornendo
un’interpretazione geometrica del risultato ottenuto.

Verifichiamo che fn(x) > g(x) $\forall x> 0$ indipendentemente dal valore di $n.$

$2 -3$
$x+ 3$
$xn> 2 -3$
$x$ ,
$xn> 0 \forall x> 0$ indipendentemente da $n: c. v. d.$

Calcoliamo ora il seguente integrale:
I(t) $= \int$ (fn(x) $-g(x))$
$$ t 1 $$
$dx= \int (2 -3$
$x+ 3$
$xn-(2 -3$
$x))$
$$ t 1 $$
$dx= \int ( 3$
$xn)$
$$ t 1 $$
$dx= \int (3x-n)$
$$ t 1 $$
$dx=$
$= [3x-n+1$
$-n+ 1]$
$$ 1 t $$
$= 3t-n+1$
$-n+ 1 -$
$-n+ 1 =$ I(t)
$$ lim $$
$t\to +\infty (3t-n+1$
$-n+ 1 -$
$-n+ 1) =$
$n-1 =$ lim
$t\to +\infty$ I(t)
Significato geometrico di tale limite: rappresenta l’area (finita per ogni $n>1)$ della regione aperta (con
$x>1)$ delimitata dai grafici delle due funzioni e dalla retta di equazione $x = 1.$

$d)$
Calcolare il lim
$x\to \infty$
$fn(x)-2$
$g(x)-2$
e verificare che il risultato non dipende da $n\in N, n> 1.$

$$ lim $$
$x\to \infty$
$fn(x)-2$
$g(x)-2 =$ lim
$x\to \infty$
$2-3$
$x+ 3$
$xn-2$
$2-3$
$x-2$
= lim
$x\to \infty$
$-3$
$x+ 3$
$$ xn $$
$-3$
= lim
$x\to +\infty$
$-3$
$-3$
$= 1 =$ lim
$x\to \infty$
$fn(x)-2$
$g(x)-2$

(N.B. Per $x\to \infty$
$xn$ è infinitesimo di ordine superiore rispetto a -
$x$ , essendo $n>1).$

Con la collaborazione di Angela Santamaria
