---
tipo: soluzione
prova_stem: 2023_sessione_ordinaria_2023_calendario_australe_2_prova_22
pdf: Prova_Maturita_2023.pdf
source: text
title: 2023 Ordinaria Australe 2 — Prova — Svolgimento
---

## Problema 1

Sessione ordinaria 2023

Ministero dell’istruzione e del merito
$LI23 -$ SCIENTIFICO OPZIONE SCIENZE APPLICATE QUADRIENNALE
Disciplina: MATEMATICA

Il candidato risolva uno dei due problemi e risponda a $4$ quesiti del questionario.
PROBLEMA $1$
Si consideri la funzione f(x) $= x\alpha$ ∙ $ex$ , definita nell’insieme dei numeri reali.
$1.$ Determinare il valore minimo del parametro $\alpha$ , intero positivo, in modo che la funzione
f(x) $= x\alpha$ ∙ $ex$ abbia un minimo assoluto nell’origine del sistema di riferimento.
$2.$ Posto $\alpha = 2,$ tracciare il grafico rappresentativo $\Gamma$ della funzione di equazione $y=$ f(x),
individuando asintoti, estremi e flessi.
$3.$ Determinare l’area della regione del $II$ quadrante delimitata dal grafico $\Gamma$ , dall’asse delle
ordinate e dalla retta tangente a $\Gamma$ nel suo punto di massimo relativo.
$4.$ Al variare del parametro $k\in R,$ determinare il numero di soluzioni dell’equazione f(x) $= k.$
Specificare per quali valori del parametro l’equazione ammette due soluzioni concordi.

PROBLEMA $2$
Si consideri la famiglia di funzioni fk(x) $= kx4 + x^{3} + 2kx2$ con $k\ge 0.$
$1.$ Al variare del parametro $k,$ studiare la monotonia, specificando la natura dei punti
stazionari.
$2.$ Tracciare il grafico $\gamma 1$ della funzione corrispondente al valore $k=$
$8$ determinando, in
particolare, le coordinate dei due punti di flesso $F^{1}$ e $F^{2}.$ Scrivere le equazioni delle rette
$t^{1}$ e $t^{2}$ tangenti a $\gamma 1$ in $F^{1}$ e $F^{2}.$
$3.$ Scrivere l’equazione $y=$ g(x) della funzione simmetrica di $f$ rispetto all’asse delle ascisse
e se ne tracci il grafico rappresentativo $\gamma 2$ nel medesimo piano cartesiano in cui è tracciato
$\gamma 1.$ Si indichi con $R$ la regione delimitata da $\gamma 1, \gamma 2$ e dalla retta $x= 1$ e se ne calcoli l’area.
$4.$ Calcolare, al variare del parametro $k,$ i seguenti limiti:
$$ lim $$
$x\to 0$
fk(x)
senk(x) lim
$x\to \infty$
fk(x)
$|senk(x)|$

Sessione ordinaria 2023

Ministero dell’istruzione e del merito

$LI23 -$ SCIENTIFICO OPZIONE SCIENZE APPLICATE QUADRIENNALE
Disciplina: MATEMATICA

QUESITI
$1.$ Data una circonferenza di centro O, siano $PA$ e $PB$ i segmenti di tangente alla circonferenza
condotti da un suo punto esterno $P.$ Dimostrare che il quadrilatero AOBP ha le diagonali
perpendicolari.
$2.$ È data un’urna contenente $10$ palline bianche e $6$ palline nere. Calcolare la probabilità che:
• Estraendo una pallina, sia di colore nero;
• Estraendo due palline contemporaneamente, siano entrambe di colore nero;
• Estraendo due palline contemporaneamente, siano di colore diverso.
$3.$ Determinare le coordinate della proiezione ortogonale $H$ del punto $A(4, -1,1)$ sul piano
$\pi : 3x-2y+ z= 1.$ Determinare le equazioni cartesiane del luogo geometrico dei punti di $\pi$
che hanno distanza $3\sqrt{2}$ da A.
$4.$ È data la parabola di equazione f(x) $= -3x^{2} + 6x.$ Determinare le coordinate del punto $P$
appartenente alla porzione di curva f(x) nel I quadrante tale che sia massimo il prodotto delle
distanze di $P$ dagli assi cartesiani.
$5.$ Gli angoli di un triangolo, inscritto in una circonferenza di raggio $7 m,$ hanno ampiezze $\alpha , \beta$ e
$\gamma$ . Sapendo che $\alpha =$
$\pi$
$6$ e che $cos\beta =$
$7,$ determinare il valore di $sen\gamma$ e l’area del triangolo ABC.
$6.$ Assegnata la curva $y= ax3 + bx2 + cx+ d,$ determinare i valori dei parametri reali in modo
che passi per $A(-1, -3), B(1, -1)$ e risulti tangente in $C(0,1)$ alla retta $t: y= 2x+ 1.$
Determinare infine le coordinate dell’ulteriore punto $Q$ di intersezione tra la curva e la retta $t.$
$7.$ Dimostrare che la regione finita di piano contenuta nel primo quadrante, delimitata dagli assi
cartesiani e dalla curva $y=$
$k-x$
$x^{2}+k^{2}$ con $k> 0,$ ha area costante pari a
$\pi$
$4 -$
$ln 2$
$8.$ Data la funzione f(x) =
$1+be-cx$ con $a> 0, b> 0$ e $c> 0,$ determinare a, $b, c$ sapendo che
f(x) ha come asintoto orizzontale la retta $y= 5$ e che il grafico di f(x) è tangente, in $x= 0,$
alla retta di equazione $y=$
$5 x+ 1.$

_______________________________________
Durata massima della prova: $6$ ore.
È consentito l’uso di calcolatrici scientifiche $e/o$ grafiche purché non siano dotate di capacità di calcolo simbolico.
(Nota MIM $n.$ 9305 del $20$ marzo 2023).
È consentito l’uso del dizionario bilingue $(italiano-lingua$ del paese di provenienza) per i candidati di madrelingua non
italiana.
Non è consentito lasciare l’Istituto prima che siano trascorse $3$ ore dalla consegna della traccia.

Australe $2$ Sessione Ordinaria 2023 Problema $1$

LICEO SCIENTIFICO AUSTRALE $2$ ORDINARIA 2023 - PROBLEMA $1$

Si consideri la funzione f(x) $= x\alpha$ ∙ex , definita nell’insieme dei numeri reali.

Determinare il valore minimo del parametro $\alpha$ , intero positivo, in modo che la funzione f(x) $= x\alpha$ ∙ex
abbia un minimo assoluto nell’origine del sistema di riferimento.

La funzione è definita e continua e derivabile su tutto l’asse reale.

$Pe$ stabilire che $x= 0$ è punto di minimo assoluto calcoliamo i limiti agli estremi del dominio e studiamo la
derivata prima.

$$ lim $$
$x\to -\infty x\alpha ∙ex=$ lim
$x\to -\infty$
$x\alpha$
$e-x=$ [
$\infty$
$\infty$ ] $= 0 (e-x$ è infinito di ordine superiore rispetto $ad x\alpha$ per $x\to -\infty$ )

$$ lim $$
$x\to +\infty x\alpha ∙ex= +\infty$

Studio della derivata prima:

f'(x) $= \alpha x\alpha -1 ∙ex+ x\alpha ∙ex= x\alpha -1 ∙ex(\alpha + x)$

Essendo $\alpha$ intero positivo (quindi $1, 2, 3,$ ...) abbiamo i seguenti casi:

$- \alpha$ pari (quindi $\alpha -1$ dispari): $x\alpha -1 ∙ex(\alpha + x) \ge 0$ se $x\alpha -1(\alpha + x) \ge 0$ che equivale a dire

$x(\alpha + x) \ge 0: x\le -\alpha$ vel $x\ge 0:$ la funzione cresce per $x< -\alpha$ , decresce per $-\alpha < x< 0$ e cresce
per $x> 0.$ Essendo lim
$x\to -\infty x\alpha ∙ex= 0$ , ed $f(0) = 0$ e lim
$x\to +\infty x\alpha ∙ex= +\infty$ : la funzione ha in $x= 0$ un
punto di minimo assoluto. Il grafico è del tipo:

Siccome $\alpha = 2, 4, 6,$ ...: il minimo valore di $\alpha$ per cui la funzione ammette un minimo assoluto nell’origine
degli assi cartesiani è $\alpha =2.$

Australe $2$ Sessione Ordinaria 2023 Problema $1$

$\alpha$ dispari (quindi $\alpha -1$ pari):
Se $\alpha = 1,$ la funzione ha equazione f(x) = x∙ex ed è:
f'(x) $= x\alpha -1 ∙ex(\alpha + x) = ex(1 + x) > 0$ se $x> -1:$ la funzione è quindi decrescente per $x< -1$
e crescente per $x> -1:$ nell’origine non c’è un minimo assoluto. Il grafico è del tipo:

Se $\alpha = 3, 5, 7,$ ... abbiamo:
$x\alpha -1 ∙ex(\alpha + x) = 0$ se $x= 0$ e $x= -\alpha$ ed è $x\alpha -1 ∙ex(\alpha + x) > 0$ se $x> -\alpha$ con $x\ne 0$ . Quindi la
funzione decresce se $x< -\alpha$ , cresce se $x> -\alpha$ e siccome la derivata si annulla per $x= 0$ in tale punto
abbiamo un flesso a tangente orizzontale (e non un minimo assoluto). Il grafico è del tipo:

Riepilogando: il valore minimo del parametro $\alpha$ , intero positivo, in modo che la funzione f(x) $= x\alpha$ ∙ex
abbia un minimo assoluto nell’origine del sistema di riferimento è $2.$

Posto $\alpha = 2,$ tracciare il grafico rappresentativo $\Gamma$ della funzione di equazione $y=$ f(x), individuando
asintoti, estremi e flessi.

Dobbiamo studiare la funzione di equazione: f(x) $= x^{2}∙ex$

Australe $2$ Sessione Ordinaria 2023 Problema $1$

Dominio: tutto $R.$

$Pari/Dispari: f(-x) = x^{2} ∙e-x:$ né pari né dispari.

Intersezioni con gli assi cartesiani: se $x= 0, y= 0.$ Se $y= 0. x^{2} ∙ex= 0,$ quindi $x= 0$ (doppia).

Segno della funzione:

f(x) $> 0$ se $x^{2} ∙ex> 0,$ quindi per ogni $x \ne 0$

Limiti agli estremi del dominio:

$$ lim $$
$x\to -\infty x^{2} ∙ex=$ lim
$x\to -\infty$
$$ x^{2 $$
$e-x= 0+ (e-x$ è infinito di ordine superiore rispetto a $x^{2}$ per $x\to -\infty$ ): quindi

$y= 0$ è asintoto orizzontale per $x\to -\infty$ ).

$$ lim $$
$x\to +\infty x^{2} ∙ex= +\infty$ .

Asintoti:

Abbiamo detto che c’è un asintoto orizzontale per $x\to -\infty$ . Non ci sono asintoti verticali perché la
funzione è continua su tutto $R.$ Potrebbe esserci asintoto obliquo per $x\to +\infty$ ); verifichiamolo:

$$ lim $$
$x\to +\infty$
$x^{2}∙ex$
= lim
$x\to +\infty x∙ex= +\infty$ : non c’è asintoto obliquo.

Monotonia:

f'(x) $= D(x^{2}$ ∙ex) $= 2xex+ x2ex= xex(2 + x) = 0$ se $x= 0$ e $x= -2$ (punti stazionari).

f'(x) $> 0$ se $xex(2 + x) > 0, x(2 + x) > 0: x< -2$ vel $x> 0.$ Quindi:

se $x< -2$ la funzione cresce, se $-2 < x< 0$ la funzione decresce, se $x> 0$ la funzione cresce.

Pertanto $x= -2$ è punto di massimo relativo: $M= (-2;$
$e^{2}). x= 0$ punto di minimo relativo (ed anche
assoluto): $m= (0; 0).$

Concavità:

f''(x) $= D(2xex+ x2ex) = 2ex+ 2xex+ 2xex+ x2ex) = ex(x^{2} + 4x+ 2) > 0$ se $x^{2} + 4x+ 2 > 0.$

Risulta $x^{2} + 4x+ 2 = 0$ se $x= -2 \pm \sqrt{2}.$ Quindi $x^{2} + 4x+ 2 > 0$ se $x< -2 -\sqrt{2}$ vel $x> -2 + \sqrt{2}$

Pertanto: se $x< -2 -\sqrt{2}$ il grafico volge la concavità verso l’alto, se $-2 -\sqrt{2} < x< -2 + \sqrt{2}$ il grafico
volge la concavità verso il basso, se $x> -2 + \sqrt{2}$ il grafico volge la concavità verso l’alto. Quindi:

$x= -2 \pm \sqrt{2}$ sono punti di flesso. E risulta: se $x= -2 -\sqrt{2}, y= (-2 -\sqrt{2})$
$2e-2-\sqrt{2} \cong 0.4$
Se $x= -2 + \sqrt{2}, y= (-2 + \sqrt{2})$
$2e-2+\sqrt{2} \cong 0.2.$

Il grafico è quindi il seguente:

Australe $2$ Sessione Ordinaria 2023 Problema $1$

Determinare l’area della regione del $II$ quadrante delimitata dal grafico $\Gamma$ , dall’asse delle ordinate e
dalla retta tangente a $\Gamma$ nel suo punto di massimo relativo.
Rappresentiamo la regione richiesta (in azzurro):

L’area richiesta A si ottiene sottraendo all’area del rettangolo di base $2$ e altezza
$e^{2}$ l’area della regione del
secondo quadrante delimitata dal grafico della funzione, dall’asse $x$ e dalla retta $x= -2.$ Quindi:

$A= (2) ( 4$
$e^{2}) -\int x^{2}$ ∙ex
$-2$
$$ dx $$

Cerchiamo una primitiva di $x^{2}$ ∙ex integrando due volte per parti:

$\int x^{2} ∙exdx= \int x^{2}$ ∙(ex)' $dx= x^{2} ∙ex-\int 2xexdx= x^{2} ∙ex-[\int 2x(ex)' dx]=$

$= x^{2} ∙ex-(2xex-\int 2exdx) = x^{2} ∙ex-(2xex-2ex) + C= x^{2} ∙ex-2xex+ 2ex+ C=$
$= ex(x^{2} -2x+ 2) + C.$ Quindi:

$A= (2) ( 4$
$e^{2}) -\int x^{2}$ ∙ex
$-2$
$dx= 8$
$e^{2} -[ex(x^{2} -2x+ 2)]-2$
$$ 0 = 8 $$
$e^{2} -[2 -(e-2(10)] = 8$
$e^{2} -2 + 10$
$e^{2} =$

$$ = (18 $$
$e^{2} -2) u^{2} \cong 0.44 u^{2} =$ A

Australe $2$ Sessione Ordinaria 2023 Problema $1$

Al variare del parametro $k\in R,$ determinare il numero di soluzioni dell’equazione f(x) $= k.$
Specificare per quali valori del parametro l’equazione ammette due soluzioni concordi.

Discutiamo il numero di soluzioni dell’equazione: $x^{2}∙ex= k.$
Il numero delle soluzioni corrisponde al numero delle intersezioni tra i grafici di
$y=$ f(x) $= x^{2}$ ∙ex e $y= k.$

Rappresentiamo nello stesso piano cartesiano la funzione ed alcune rette del tipo $y= k:$

Se $k< 0:$ nessuna soluzione.
Se $k= 0:$ due soluzioni coincidenti (nulle).
Se $0 < k<$
$e^{2}: 3$ soluzioni distinte, di cui due negative ed una positiva.
Se $k=$
$e^{2}:$ tre soluzioni di cui due coincidenti e negative (precisamente uguali a $-2)$ ed una positiva.
Se $k>$
$e^{2}:$ una soluzione positiva.
Specificare per quali valori del parametro l’equazione ammette due soluzioni concordi
Ci sono due soluzioni distinte e concordi (in particolare negative) se $0 < k<$
$e^{2}.$ Se $k=$
$e^{2}$ ci sono due
soluzioni concordi (negative) e coincidenti (uguali a $-2).$

Con la collaborazione di Angela Santamaria

Australe $2$ Sessione Ordinaria 2023 Problema $2$

LICEO SCIENTIFICO AUSTRALE $2$ ORDINARIA 2023 - PROBLEMA $2$

Si consideri la famiglia di funzioni fk(x) $= kx4 + x^{3} + 2kx2$ con $k\ge 0.$

Al variare del parametro $k,$ studiare la monotonia, specificando la natura dei punti stazionari

La funzione, razionale intera, è definita, continua e derivabile su tutto l’asse reale.
Studiamo la derivata prima:

f'(x) $= 4kx3 + 3x^{2} + 4kx= x(4kx2 + 3x+ 4k)$

Se $k= 0:$ f'(x) $= x(3x) = 3x^{2} \ge 0$ per ogni $x,$ in paricolare f'(x) $= 0$ per $x= 0,$ che è
punto stazionario. Essendo per $x\ne 0$ f'(x) $> 0,$ la funzione è sempre crescente. Quindi:

Se $k= 0$ la funzione è sempre crescente ed ha in $x= 0$ un punto stazionario che è punto di flesso a
tangente orizzontale.

Se $k> 0: x(4kx2 + 3x+ 4k) \ge 0;$ il discriminante dell’equazione $4kx2 + 3x+ 4k= 0$ è:
$\Delta = 9 -64k^{2}.$ Si hanno i seguenti casi:
o Se $9-64k^{2}< 0,$ quindi $k< -$
$8$ vel $k>$
$8$ (quindi $k>$
$8$ perché $k> 0)$
$4kx2 + 3x+ 4k> 0$ per ogni $x$ . Pertanto:
f'(x) $\ge 0$ per $x\ge 0$ (e la derivata si annulla per $x= 0).$ Pertanto:
▪
Se $k>$
$8$ la funzione è decrescente per $x< 0,$ crescente per $x> 0$ ed ha un punto
stazionario in $x= 0$ che è un punto di minimo relativo (ed anche assoluto).

Australe $2$ Sessione Ordinaria 2023 Problema $2$

o Se $9-64k^{2}> 0,$ cioè -
$8 < k<$
$8$ , equivalente a $0< k<$
$8$ , risulta:
$4kx2 + 3x+ 4k\ge 0$ per $x\le$
$-3-\sqrt{9}-64k^{2}$
$8k$
$= x^{1}$ vel $x\ge$
$-3+\sqrt{9}-64k^{2}$
$8k$
$= x^{2}.$
E risulta
$-3-\sqrt{9}-64k^{2}$
$8k$
$< 0.$ Confrontiamo
$-3+\sqrt{9}-64k^{2}$
$8k$
con $0.$ Si ha:

$-3 + \sqrt{9} -64k^{2}$
$8k$
$\ge 0$ se $-3 + \sqrt{9} -64k^{2} \ge 0, \sqrt{9} -64k^{2} \ge 3,$

$-64k^{2} \ge 0$ MAI. Pertanto
$-3+\sqrt{9}-64k^{2}$
$8k$
$< 0$ sempre. Ne consegue che se
$0 < k<$
$8 , 4kx2 + 3x+ 4k\ge 0$ per $x\le$
$-3-\sqrt{9}-64k^{2}$
$8k$
vel $x\ge$
$-3+\sqrt{9}-64k^{2}$
$8k$
(con entrambi
i valori negativi).

$-3 -\sqrt{9} -64k^{2}$
$8k$

$-3 + \sqrt{9} -64k^{2}$
$8k$

$m^{1} M m^{2}$

In conclusione, per $0< k<$
$$ 3 8: $$

se $x<$
$-3-\sqrt{9}-64k^{2}$
$8k$
funzione decrescente, per
$-3-\sqrt{9}-64k^{2}$
$8k$
$< x<$
$-3+\sqrt{9}-64k^{2}$
$8k$
crescente
$$ per $$
$-3+\sqrt{9}-64k^{2}$
$8k$
$< x< 0$ decrescente , per $x> 0$ crescente.
La funzione ha quindi tre punti stazionari:
$x=$
$-3-\sqrt{9}-64k^{2}$
$8k$
(minimo relativo), $x=$
$-3+\sqrt{9}-64k^{2}$
$8k$
(massimo relativo)
ed $x= 0$ (minimo relativo).

$4kx2 + 3x+ 4k$

f'(x)

Australe $2$ Sessione Ordinaria 2023 Problema $2$

o Se $9-64k^{2}= 0 , k= \pm$
$8$ , quindi (essendo $k> 0) k=$
$8$ risulta:
f'(x) $= 4kx3 + 3x^{2} + 4kx= x(3$
$2 x^{2} + 3x+ 3$
$$ 2) = 3 $$
$2 x(x^{2} + 2x+ 1)$
ed è: f'(x) =
$2 x(x^{2} + 2x+ 1) =$
$2 x(x+ 1)^{2} \ge 0$ per $x\ge 0$ , con f'(x) $= 0$ per
$x= 0$ e $x= -1.$

In questo caso, quindi, $k=$
$8$ , la funzione decresce per $x< 0,$ cresce per $x> 0 :x= 0$
è punto di minimo relativo (ed anche assoluto). Inoltre $x= -1$ è punto di flesso a tangente
orizzontale.

Riepilogando:

Se $k= 0$ la funzione è sempre crescente ed ha in $x= 0$ un punto stazionario che è punto di flesso a
tangente orizzontale.

Se $0< k<$
$8$ la funzione è decrescente se $x<$
$-3-\sqrt{9}-64k^{2}$
$8k$
crescente per
$-3-\sqrt{9}-64k^{2}$
$8k$
$< x<$
$-3+\sqrt{9}-64k^{2}$
$8k$
, decrescente per
$-3-\sqrt{9}-64k^{2}$
$8k$
$< x< 0$ e crescente per $x> 0;$ ha
quindi tre punti stazionari:

$x=$
$-3-\sqrt{9}-64k^{2}$
$8k$
(minimo relativo), $x=$
$-3+\sqrt{9}-64k^{2}$
$8k$
(massimo relativo)
ed $x= 0$ (minimo relativo).

Se $k=$
$8$ , la funzione decresce per $x< 0,$ cresce per $x> 0 :x= 0$
è punto di minimo relativo (ed anche assoluto). Inoltre $x= -1$ è punto di flesso a tangente
orizzontale.

Se $k>$
$8$ la funzione è decrescente per $x< 0,$ crescente per $x> 0$ ed ha un punto stazionario in
$x= 0$ che è un punto di minimo relativo (ed anche assoluto).

Tracciare il grafico $\gamma 1$ della funzione corrispondente al valore $k=$
$8$ determinando, in particolare, le
coordinate dei due punti di flesso $F^{1}$ e $F^{2}$ . Scrivere le equazioni delle rette $t^{1}$ e $t^{2}$ tangenti a $\gamma 1$ in $F^{1}$ e $F^{2}.$

Per $k=$
$8$ la funzione ha equazione: $y=$ f(x) =
$8x^{4}+ x^{3}+$
$4x^{2}$

Dominio: tutto $R.$

$Pari/Dispari: f(-x) =$
$8 x^{4} -x^{3} +$
$4 x^{2}:$ né pari né dispari.

Intersezioni con gli assi cartesiani: se $x= 0, y= 0.$ Se $y= 0.:$
$8 x^{4} + x^{3} +$
$4 x^{2} = 0,$

Australe $2$ Sessione Ordinaria 2023 Problema $2$

$3x^{4} + 8x^{3} + 6x^{2} = 0 , x^{2}(3x^{2} + 8x+ 6) = 0$ solo se $x= 0$ (che è soluzione doppia, quindi il grafico è
tangente all’asse delle $x$ nell’origine degli assi), perché il discriminante di $3x^{2} + 8x+ 6 = 0$ è negativo.

Segno della funzione:

f(x) $\ge 0$ se $x^{2}(3x^{2} + 8x+ 6) \ge 0:$ la funzione è sempre positiva per ogni $x\ne 0$ (in $x= 0,$ come già
detto si annulla).

Limiti agli estremi del dominio:

$$ lim $$
$x\to \mp \infty$ (
$8 x^{4} + x^{3} +$
$4 x^{2}) =$ lim
$x\to \mp \infty$ (
$8 x^{4}) = +\infty$ . Notiamo che non ci sono asintoti orizzontali né obliqui.

Monotonia:

f'(x) =
$2 x^{3} + 3x^{2} +$
$2 x=$
$2 x(x^{2} + 2x+ 1) =$
$2 x(x+ 1)^{2} = 0$ se $x= 0$ e $x= -1.$ Risulta:

Se $x< -1:$ f'(x) $< 0:$ funzione decrescente.
Se $x= -1:$ f'(x) $= 0: x= -1$ punto stazionario
Se $-1 < x< 0:$ f'(x) $< 0:$ funzione decrescente (quindi $x= -1$ è punto di flesso a tangente orizzontale)
Se $x= 0:$ f'(x) $= 0: x= 0$ punto stazionario.
Se $x> 0:$ f'(x) $> 0:$ funzione crescente, $x= 0$ punto di minimo relativo (ed anche assoluto): $m= (0; 0)$

Concavità:

f''(x) =
$2 x^{2} + 6x+$
$2 \ge 0$ se $3x^{2} + 4x+ 1 \ge 0; x1,2 =$
$-2\pm 1$
$3$ ; quindi $x^{1} = -1$ e $x^{2} = -$
$$ 1 3. $$
Pertanto: f''(x) $\ge 0$ se $x\le -1$ vel $x\ge -$
$3.$ Per quanto riguarda la concavità:

se $x< -1:$ concavità verso l’alto
se $-1 < x< -$
$3:$ concavità verso il basso ( $x= -1$ punto di flesso)
se -
$3 < x< 0:$ concavità verso l’alto $(x= -$
$3$ punto di flesso)
Se $x> 0:$ concavità verso l’alto.

$L$ funzione ha quindi due punti di flesso:

se $x= -1, y=$
$8 -1 +$
$$ 3 4 = 1 $$
$8: F^{1} = (-1;$
$$ 1 8) $$

se $x= -$
$3: y=$
$$ 3 8 ( 1 $$
$81) -$
$$ 1 27 + 1 12 = 11 $$
$216: F^{2} = (-$
$$ 1 3 ; 11 216). $$

Cerchiamo le equazioni delle tangenti inflessionali. Risulta: $f'(-1) = 0$ ed $f' (-$
$3) = -$
$$ 1 2 ( 4 $$
$9) = -$
$$ 2 9. $$
Pertanto, indicando con $t^{1}$ la tangente in $F^{1}$ e con $t^{2}$ la tangente in $F^{2},$ abbiamo:

$t^{1}: y-1$
$$ 8 = 0, $$
$y= 1$

$t^{2}: y-$
$$ 11 $$
$216 = -$
$9 (x+$
$3) , y= -$
$9 x-$
$$ 5 216 $$

Australe $2$ Sessione Ordinaria 2023 Problema $2$

Il grafico è quindi il seguente:

Scrivere l’equazione $y=$ g(x) della funzione simmetrica di $f$ rispetto all’asse delle ascisse e se ne
tracci il grafico rappresentativo $\gamma 2$ nel medesimo piano cartesiano in cui è tracciato $\gamma 1.$ Si indichi con $R$
la regione delimitata da $\gamma 1, \gamma 2$ e dalla retta $x= 1$ e se ne calcoli l’area.

L’equazione della funzione $g$ è la seguente: $y=$ g(x) $= -f(x) = -($
$8 x^{4} + x^{3} +$
$4 x^{2}).$ Il suo grafico,
insieme a quello della funzione $f$ è il seguente:

Australe $2$ Sessione Ordinaria 2023 Problema $2$

L’area della regione $R$ è data da:
Area(R) $= 2 \int$ f(x)
$$ 1 0 $$
$dx= 2 \int (3$
$8 x^{4} + x^{3} + 3$
$4 x^{2})$
$$ 1 0 $$
$dx= 2$ [ $3$
$40 x^{5} + 1$
$4 x^{4} + 1$
$4 x^{3}]$
$$ 0 1 = $$

$$ = 2 [ 3 40 + 1 4 + 1 $$
$4 -(0)] = 46$
$40 u^{2} \cong 1.15 u^{2} =$ Area(R)

Calcolare, al variare del parametro $k,$ i seguenti limiti:
$$ lim $$
$x\to 0$ fk(x)
senk(x) lim
$x\to \infty$
fk(x)
$|senk(x)|$

Calcoliamo il primo limite, ricordando che $k\ge 0.$
Notiamo che la funzione $y=$
fk(x)
senk(x) ha il numeratore che è sempre definito
(fk(x) $= kx4 + x^{3} + 2kx2$ con $k\ge 0).$ Il denominatore non è definito (dato che $k\ge 0$ è un numero
reale qualsiasi) quando sen(x) $\le 0$ (si pensi a (-
$$ 1 2) 1 $$
$2 = \sqrt -$
$$ 1 2 ). $$
Dovendo calcolare il limite per $x\to 0,$ non ci interessa che la funzione sia definita in $x= 0.$ Per avere
sen(x) $> 0$ supponiamo che il limite sia per $x\to 0+,$ così il denominatore è definito in un intorno destro
“piccolo a piacere” di $x= 0, 0$ escluso, che è quello che ci interessa per poter calcolare il limite.
Ricordiamo che per $x\to 0+,$ risulta sin (x)~x e quindi (dato che stiamo considerando sen(x) $> 0),$ si
ha senk(x)~xk. Pertanto:
$$ lim $$
$x\to 0+$
fk(x)
senk(x) = lim
$x\to 0+$
$kx4+x^{3}+2kx2$
senk(x)
= lim
$x\to 0+$
$2kx2$
$xk$ .

Si hanno i seguenti casi:
$1)$ Se $k= 2$ il limite è $4$
$2)$ Se $0 \le k< 2$ il limite è $0+$ (il numeratore è infinitesimo di ordine superiore)
$3)$ Se $k> 2$ il limite è $+\infty$ (il numeratore è infinitesimo di ordine inferiore)
Calcoliamo il secondo limite, ricordando che $k\ge 0.$

In questo caso le cose sono più complicate. Infatti, come nel caso precedente, per poter calcolare senk(x)
per ogni numero reale deve essere sen $(x) > 0,$ cioè $2h\pi < x< \pi + 2h\pi$ , con $h\in Z.$ Quindi la
funzione di cui dobbiamo calcolare il limite non esiste in TUTTO un intorno di $+\infty$ (cioè per ogni
numero reale $x> N)$ né in TUTTO un intorno di $-\infty$ (cioè per ogni numero reale $x< -N).$

Australe $2$ Sessione Ordinaria 2023 Problema $2$

Questo fatto pone dei grossi problemi se vogliamo applicare la definizione “semplificata” di limite che
viene normalmente data dai libri di testo usati nei Licei. Infatti, di solito, prima di dare la definizione di
limite per $x\to c,$ si dice che $c$ deve essere (ovviamente) un punto di accumulazione per il dominio $D$
della funzione, con $D\subseteq R,$ e si aggiunge che la funzione sia definita in un intorno I di $c$ (al più escluso
$c).$
Quando si dà la definizione di limite per $x\to +\infty$ si suppone che la funzione sia definita in un intorno I
di $+\infty$ (quindi in un intervallo del tipo $x> N)$ ed analogamente, quando si dà la definizione di limite per
$x\to -\infty$ si suppone che la funzione sia definita in un intorno I di $-\infty$ (quindi in un intervallo del tipo
$x< -N).$ Quando infine si da la definizione di limite per $x\to \infty$ si suppone che la funzione sia definita
in un intorno di $\infty$ (cioè per tutti gli $x$ che soddisfano una disequazione del tipo $|x| > N,$ con $N> 0,$ che
equivale a $x< -N \cup x> N).$
Secondo la definizione che abbiamo richiamato (quella data normalmente nei Licei), il lim
$x\to \infty$
$kx4+x^{3}+2kx2$
$|senk(x)|$
NON ESISTE, perché non esiste un intorno I di $\infty$ in cui la funzione sia sempre definita.
La definizione più generale di limite (che non viene normalmente utilizzata al Liceo), prevede che la
funzione sia definita in $I(\infty ) \cap D,$ cioè nei punti di un intorno di $\infty$ che appartengono al dominio $D$ della
funzione. Le due definizioni che si possono dare (limite finito e limite infinito) sono del tipo seguente.
Definizione generale di limite finito all’infinto:
Data una funzione $y=$ f(x), definita in un in un sottoinsieme $D$ di numeri reali $(D\subseteq R),$ si dice che
f(x) $\to l$ per $x\to \infty$ , e si scrive lim
$x\to \infty$ f(x) $= l,$ se:
$\forall \varepsilon > 0 \exists I\varepsilon (\infty )/ \forall x\in I\cap D$ si ha: $|f(x) -l| < \varepsilon$
Detto a parole: fissato un $\varepsilon > 0$ (piccolo quanto si vuole, ma questa frase, di uso comune, è superflua),
esiste in corrispondenza di esso un numero reale $N> 0$ tale che, per ogni $x$ del dominio $D$ della
funzione per cui si abbia $|x| > N$ (equivalente a $x< -N \cup X> N)$ risulti: $|f(x) -l| < \varepsilon$
(equivalente a $l-\varepsilon <$ f(x) $< l+ \varepsilon$ ).
Definizione generale di limite infinito all’infinto:
Data una funzione $y=$ f(x), definita in un in un sottoinsieme $D$ di numeri reali $(D\subseteq R),$ si dice che
f(x) $\to \infty$ per $x\to \infty$ , e si scrive lim
$x\to \infty$ f(x) $= \infty$ , se:
$\forall M> 0 \exists IM(\infty )/ \forall x\in I\cap D$ si ha: $|f(x)| > M$
Detto a parole: fissato un $M> 0$ (grande quanto si vuole, ma anche questa frase, di uso comune, è
superflua), esiste in corrispondenza di esso un numero reale $N> 0$ tale che, per ogni $x$ del dominio $D$
della funzione per cui si abbia $|x| > N$ (equivalente a
$x< -N \cup X> N)$ risulti: $|f(x)| > M$ (equivalente a f(x) $< -M \cup$ f(x) $> M).$
Fatta questa doverosa estensione della definizione di limite, per calcolare lim
$x\to \infty$
$kx4+x^{3}+2kx2$
$|senk(x)|$ analizziamo
due casi, ricordando che il dominio $D$ della funzione è tutto l’asse reale privato degli (infiniti)
intervalli del tipo $\pi + 2h\pi \le x\le 2\pi + 2h\pi$ , con $h\in Z$ e $h\ge 0$ e $-\pi -2h\pi \le x\le -2h\pi$ , con
$h\in Z$ e $h\ge 0$ e che in $D$ risulta sen(x) $> 0$ affinché abbia senso la potenza $ad$ esponente reale
senk(x).

Australe $2$ Sessione Ordinaria 2023 Problema $2$

Primo caso: $k> 0$
In questo caso, essendo sen $x> 0$ risulta $0 <$ sen $x\le 1$ e quindi $0 < |senk(x)| \le 1$
$$ lim $$
$x\to \infty$
$kx4+x^{3}+2kx2$
$|senk(x)|$
= lim
$x\to \infty$
$$ kx4 $$
$|senk(x)| =$ [
$+\infty$
quantità che oscilla fra $0$ escluso ed $1$ compreso] $= +\infty$ .

N.B. Siccome $0 < |senk(x)| \le 1$ risulta
$$ kx4 $$
$|senk(x)| \ge kx4$ che tende a $+\infty$ , quindi anche
$$ kx4 $$
$|senk(x)| \to +\infty$ .

In conclusione: lim
$x\to \infty$
$kx4+x^{3}+2kx2$
$|senk(x)|$
$= +\infty$ (per ogni $k> 0).$

Secondo caso $k= 0$

Anche in questo caso, essendo sen $x> 0$ risulta $0 <$ sen $x\le 1$ e quindi senk(x) $= sen0(x) = 1.$

Il limite diventa: lim
$x\to \infty$
$$ x^{3 $$
$sen0(x) =$ lim
$x\to \infty x^{3} = \infty$

In conclusione: se $k= 0$ il limite è $\infty$ (precisamente $-\infty$ se $x\to -\infty$ e $+\infty$ se $x\to +\infty$ ).

Osservazione sul secondo limite

Se al denominatore invece di $|senk(x)|$ ci fosse stato $|sen(x)|k$ la discussione sarebbe stata più semplice,
perché avremmo evitato i casi in cui sen(x) $< 0,$ quindi il dominio $D$ sarebbe stato tutto l’asse reale
privato dei punti $x= h\pi$ , con $h\in Z.$ Sarebbe stato comunque necessario lo stesso utilizzare la
definizione “generale” di limite, perché anche in tal caso la funzione non sarebbe stata definita in
TUTTO un intorno di $\infty$ .

Con la collaborazione di Angela Santamaria

## Problema 2

Sessione ordinaria 2023

Ministero dell’istruzione e del merito
$LI23 -$ SCIENTIFICO OPZIONE SCIENZE APPLICATE QUADRIENNALE
Disciplina: MATEMATICA

Il candidato risolva uno dei due problemi e risponda a $4$ quesiti del questionario.
PROBLEMA $1$
Si consideri la funzione f(x) $= x\alpha$ ∙ $ex$ , definita nell’insieme dei numeri reali.
$1.$ Determinare il valore minimo del parametro $\alpha$ , intero positivo, in modo che la funzione
f(x) $= x\alpha$ ∙ $ex$ abbia un minimo assoluto nell’origine del sistema di riferimento.
$2.$ Posto $\alpha = 2,$ tracciare il grafico rappresentativo $\Gamma$ della funzione di equazione $y=$ f(x),
individuando asintoti, estremi e flessi.
$3.$ Determinare l’area della regione del $II$ quadrante delimitata dal grafico $\Gamma$ , dall’asse delle
ordinate e dalla retta tangente a $\Gamma$ nel suo punto di massimo relativo.
$4.$ Al variare del parametro $k\in R,$ determinare il numero di soluzioni dell’equazione f(x) $= k.$
Specificare per quali valori del parametro l’equazione ammette due soluzioni concordi.

PROBLEMA $2$
Si consideri la famiglia di funzioni fk(x) $= kx4 + x^{3} + 2kx2$ con $k\ge 0.$
$1.$ Al variare del parametro $k,$ studiare la monotonia, specificando la natura dei punti
stazionari.
$2.$ Tracciare il grafico $\gamma 1$ della funzione corrispondente al valore $k=$
$8$ determinando, in
particolare, le coordinate dei due punti di flesso $F^{1}$ e $F^{2}.$ Scrivere le equazioni delle rette
$t^{1}$ e $t^{2}$ tangenti a $\gamma 1$ in $F^{1}$ e $F^{2}.$
$3.$ Scrivere l’equazione $y=$ g(x) della funzione simmetrica di $f$ rispetto all’asse delle ascisse
e se ne tracci il grafico rappresentativo $\gamma 2$ nel medesimo piano cartesiano in cui è tracciato
$\gamma 1.$ Si indichi con $R$ la regione delimitata da $\gamma 1, \gamma 2$ e dalla retta $x= 1$ e se ne calcoli l’area.
$4.$ Calcolare, al variare del parametro $k,$ i seguenti limiti:
$$ lim $$
$x\to 0$
fk(x)
senk(x) lim
$x\to \infty$
fk(x)
$|senk(x)|$

Sessione ordinaria 2023

Ministero dell’istruzione e del merito

$LI23 -$ SCIENTIFICO OPZIONE SCIENZE APPLICATE QUADRIENNALE
Disciplina: MATEMATICA

QUESITI
$1.$ Data una circonferenza di centro O, siano $PA$ e $PB$ i segmenti di tangente alla circonferenza
condotti da un suo punto esterno $P.$ Dimostrare che il quadrilatero AOBP ha le diagonali
perpendicolari.
$2.$ È data un’urna contenente $10$ palline bianche e $6$ palline nere. Calcolare la probabilità che:
• Estraendo una pallina, sia di colore nero;
• Estraendo due palline contemporaneamente, siano entrambe di colore nero;
• Estraendo due palline contemporaneamente, siano di colore diverso.
$3.$ Determinare le coordinate della proiezione ortogonale $H$ del punto $A(4, -1,1)$ sul piano
$\pi : 3x-2y+ z= 1.$ Determinare le equazioni cartesiane del luogo geometrico dei punti di $\pi$
che hanno distanza $3\sqrt{2}$ da A.
$4.$ È data la parabola di equazione f(x) $= -3x^{2} + 6x.$ Determinare le coordinate del punto $P$
appartenente alla porzione di curva f(x) nel I quadrante tale che sia massimo il prodotto delle
distanze di $P$ dagli assi cartesiani.
$5.$ Gli angoli di un triangolo, inscritto in una circonferenza di raggio $7 m,$ hanno ampiezze $\alpha , \beta$ e
$\gamma$ . Sapendo che $\alpha =$
$\pi$
$6$ e che $cos\beta =$
$7,$ determinare il valore di $sen\gamma$ e l’area del triangolo ABC.
$6.$ Assegnata la curva $y= ax3 + bx2 + cx+ d,$ determinare i valori dei parametri reali in modo
che passi per $A(-1, -3), B(1, -1)$ e risulti tangente in $C(0,1)$ alla retta $t: y= 2x+ 1.$
Determinare infine le coordinate dell’ulteriore punto $Q$ di intersezione tra la curva e la retta $t.$
$7.$ Dimostrare che la regione finita di piano contenuta nel primo quadrante, delimitata dagli assi
cartesiani e dalla curva $y=$
$k-x$
$x^{2}+k^{2}$ con $k> 0,$ ha area costante pari a
$\pi$
$4 -$
$ln 2$
$8.$ Data la funzione f(x) =
$1+be-cx$ con $a> 0, b> 0$ e $c> 0,$ determinare a, $b, c$ sapendo che
f(x) ha come asintoto orizzontale la retta $y= 5$ e che il grafico di f(x) è tangente, in $x= 0,$
alla retta di equazione $y=$
$5 x+ 1.$

_______________________________________
Durata massima della prova: $6$ ore.
È consentito l’uso di calcolatrici scientifiche $e/o$ grafiche purché non siano dotate di capacità di calcolo simbolico.
(Nota MIM $n.$ 9305 del $20$ marzo 2023).
È consentito l’uso del dizionario bilingue $(italiano-lingua$ del paese di provenienza) per i candidati di madrelingua non
italiana.
Non è consentito lasciare l’Istituto prima che siano trascorse $3$ ore dalla consegna della traccia.

Australe $2$ Sessione Ordinaria 2023 Problema $1$

LICEO SCIENTIFICO AUSTRALE $2$ ORDINARIA 2023 - PROBLEMA $1$

Si consideri la funzione f(x) $= x\alpha$ ∙ex , definita nell’insieme dei numeri reali.

Determinare il valore minimo del parametro $\alpha$ , intero positivo, in modo che la funzione f(x) $= x\alpha$ ∙ex
abbia un minimo assoluto nell’origine del sistema di riferimento.

La funzione è definita e continua e derivabile su tutto l’asse reale.

$Pe$ stabilire che $x= 0$ è punto di minimo assoluto calcoliamo i limiti agli estremi del dominio e studiamo la
derivata prima.

$$ lim $$
$x\to -\infty x\alpha ∙ex=$ lim
$x\to -\infty$
$x\alpha$
$e-x=$ [
$\infty$
$\infty$ ] $= 0 (e-x$ è infinito di ordine superiore rispetto $ad x\alpha$ per $x\to -\infty$ )

$$ lim $$
$x\to +\infty x\alpha ∙ex= +\infty$

Studio della derivata prima:

f'(x) $= \alpha x\alpha -1 ∙ex+ x\alpha ∙ex= x\alpha -1 ∙ex(\alpha + x)$

Essendo $\alpha$ intero positivo (quindi $1, 2, 3,$ ...) abbiamo i seguenti casi:

$- \alpha$ pari (quindi $\alpha -1$ dispari): $x\alpha -1 ∙ex(\alpha + x) \ge 0$ se $x\alpha -1(\alpha + x) \ge 0$ che equivale a dire

$x(\alpha + x) \ge 0: x\le -\alpha$ vel $x\ge 0:$ la funzione cresce per $x< -\alpha$ , decresce per $-\alpha < x< 0$ e cresce
per $x> 0.$ Essendo lim
$x\to -\infty x\alpha ∙ex= 0$ , ed $f(0) = 0$ e lim
$x\to +\infty x\alpha ∙ex= +\infty$ : la funzione ha in $x= 0$ un
punto di minimo assoluto. Il grafico è del tipo:

Siccome $\alpha = 2, 4, 6,$ ...: il minimo valore di $\alpha$ per cui la funzione ammette un minimo assoluto nell’origine
degli assi cartesiani è $\alpha =2.$

Australe $2$ Sessione Ordinaria 2023 Problema $1$

$\alpha$ dispari (quindi $\alpha -1$ pari):
Se $\alpha = 1,$ la funzione ha equazione f(x) = x∙ex ed è:
f'(x) $= x\alpha -1 ∙ex(\alpha + x) = ex(1 + x) > 0$ se $x> -1:$ la funzione è quindi decrescente per $x< -1$
e crescente per $x> -1:$ nell’origine non c’è un minimo assoluto. Il grafico è del tipo:

Se $\alpha = 3, 5, 7,$ ... abbiamo:
$x\alpha -1 ∙ex(\alpha + x) = 0$ se $x= 0$ e $x= -\alpha$ ed è $x\alpha -1 ∙ex(\alpha + x) > 0$ se $x> -\alpha$ con $x\ne 0$ . Quindi la
funzione decresce se $x< -\alpha$ , cresce se $x> -\alpha$ e siccome la derivata si annulla per $x= 0$ in tale punto
abbiamo un flesso a tangente orizzontale (e non un minimo assoluto). Il grafico è del tipo:

Riepilogando: il valore minimo del parametro $\alpha$ , intero positivo, in modo che la funzione f(x) $= x\alpha$ ∙ex
abbia un minimo assoluto nell’origine del sistema di riferimento è $2.$

Posto $\alpha = 2,$ tracciare il grafico rappresentativo $\Gamma$ della funzione di equazione $y=$ f(x), individuando
asintoti, estremi e flessi.

Dobbiamo studiare la funzione di equazione: f(x) $= x^{2}∙ex$

Australe $2$ Sessione Ordinaria 2023 Problema $1$

Dominio: tutto $R.$

$Pari/Dispari: f(-x) = x^{2} ∙e-x:$ né pari né dispari.

Intersezioni con gli assi cartesiani: se $x= 0, y= 0.$ Se $y= 0. x^{2} ∙ex= 0,$ quindi $x= 0$ (doppia).

Segno della funzione:

f(x) $> 0$ se $x^{2} ∙ex> 0,$ quindi per ogni $x \ne 0$

Limiti agli estremi del dominio:

$$ lim $$
$x\to -\infty x^{2} ∙ex=$ lim
$x\to -\infty$
$$ x^{2 $$
$e-x= 0+ (e-x$ è infinito di ordine superiore rispetto a $x^{2}$ per $x\to -\infty$ ): quindi

$y= 0$ è asintoto orizzontale per $x\to -\infty$ ).

$$ lim $$
$x\to +\infty x^{2} ∙ex= +\infty$ .

Asintoti:

Abbiamo detto che c’è un asintoto orizzontale per $x\to -\infty$ . Non ci sono asintoti verticali perché la
funzione è continua su tutto $R.$ Potrebbe esserci asintoto obliquo per $x\to +\infty$ ); verifichiamolo:

$$ lim $$
$x\to +\infty$
$x^{2}∙ex$
= lim
$x\to +\infty x∙ex= +\infty$ : non c’è asintoto obliquo.

Monotonia:

f'(x) $= D(x^{2}$ ∙ex) $= 2xex+ x2ex= xex(2 + x) = 0$ se $x= 0$ e $x= -2$ (punti stazionari).

f'(x) $> 0$ se $xex(2 + x) > 0, x(2 + x) > 0: x< -2$ vel $x> 0.$ Quindi:

se $x< -2$ la funzione cresce, se $-2 < x< 0$ la funzione decresce, se $x> 0$ la funzione cresce.

Pertanto $x= -2$ è punto di massimo relativo: $M= (-2;$
$e^{2}). x= 0$ punto di minimo relativo (ed anche
assoluto): $m= (0; 0).$

Concavità:

f''(x) $= D(2xex+ x2ex) = 2ex+ 2xex+ 2xex+ x2ex) = ex(x^{2} + 4x+ 2) > 0$ se $x^{2} + 4x+ 2 > 0.$

Risulta $x^{2} + 4x+ 2 = 0$ se $x= -2 \pm \sqrt{2}.$ Quindi $x^{2} + 4x+ 2 > 0$ se $x< -2 -\sqrt{2}$ vel $x> -2 + \sqrt{2}$

Pertanto: se $x< -2 -\sqrt{2}$ il grafico volge la concavità verso l’alto, se $-2 -\sqrt{2} < x< -2 + \sqrt{2}$ il grafico
volge la concavità verso il basso, se $x> -2 + \sqrt{2}$ il grafico volge la concavità verso l’alto. Quindi:

$x= -2 \pm \sqrt{2}$ sono punti di flesso. E risulta: se $x= -2 -\sqrt{2}, y= (-2 -\sqrt{2})$
$2e-2-\sqrt{2} \cong 0.4$
Se $x= -2 + \sqrt{2}, y= (-2 + \sqrt{2})$
$2e-2+\sqrt{2} \cong 0.2.$

Il grafico è quindi il seguente:

Australe $2$ Sessione Ordinaria 2023 Problema $1$

Determinare l’area della regione del $II$ quadrante delimitata dal grafico $\Gamma$ , dall’asse delle ordinate e
dalla retta tangente a $\Gamma$ nel suo punto di massimo relativo.
Rappresentiamo la regione richiesta (in azzurro):

L’area richiesta A si ottiene sottraendo all’area del rettangolo di base $2$ e altezza
$e^{2}$ l’area della regione del
secondo quadrante delimitata dal grafico della funzione, dall’asse $x$ e dalla retta $x= -2.$ Quindi:

$A= (2) ( 4$
$e^{2}) -\int x^{2}$ ∙ex
$-2$
$$ dx $$

Cerchiamo una primitiva di $x^{2}$ ∙ex integrando due volte per parti:

$\int x^{2} ∙exdx= \int x^{2}$ ∙(ex)' $dx= x^{2} ∙ex-\int 2xexdx= x^{2} ∙ex-[\int 2x(ex)' dx]=$

$= x^{2} ∙ex-(2xex-\int 2exdx) = x^{2} ∙ex-(2xex-2ex) + C= x^{2} ∙ex-2xex+ 2ex+ C=$
$= ex(x^{2} -2x+ 2) + C.$ Quindi:

$A= (2) ( 4$
$e^{2}) -\int x^{2}$ ∙ex
$-2$
$dx= 8$
$e^{2} -[ex(x^{2} -2x+ 2)]-2$
$$ 0 = 8 $$
$e^{2} -[2 -(e-2(10)] = 8$
$e^{2} -2 + 10$
$e^{2} =$

$$ = (18 $$
$e^{2} -2) u^{2} \cong 0.44 u^{2} =$ A

Australe $2$ Sessione Ordinaria 2023 Problema $1$

Al variare del parametro $k\in R,$ determinare il numero di soluzioni dell’equazione f(x) $= k.$
Specificare per quali valori del parametro l’equazione ammette due soluzioni concordi.

Discutiamo il numero di soluzioni dell’equazione: $x^{2}∙ex= k.$
Il numero delle soluzioni corrisponde al numero delle intersezioni tra i grafici di
$y=$ f(x) $= x^{2}$ ∙ex e $y= k.$

Rappresentiamo nello stesso piano cartesiano la funzione ed alcune rette del tipo $y= k:$

Se $k< 0:$ nessuna soluzione.
Se $k= 0:$ due soluzioni coincidenti (nulle).
Se $0 < k<$
$e^{2}: 3$ soluzioni distinte, di cui due negative ed una positiva.
Se $k=$
$e^{2}:$ tre soluzioni di cui due coincidenti e negative (precisamente uguali a $-2)$ ed una positiva.
Se $k>$
$e^{2}:$ una soluzione positiva.
Specificare per quali valori del parametro l’equazione ammette due soluzioni concordi
Ci sono due soluzioni distinte e concordi (in particolare negative) se $0 < k<$
$e^{2}.$ Se $k=$
$e^{2}$ ci sono due
soluzioni concordi (negative) e coincidenti (uguali a $-2).$

Con la collaborazione di Angela Santamaria

Australe $2$ Sessione Ordinaria 2023 Problema $2$

LICEO SCIENTIFICO AUSTRALE $2$ ORDINARIA 2023 - PROBLEMA $2$

Si consideri la famiglia di funzioni fk(x) $= kx4 + x^{3} + 2kx2$ con $k\ge 0.$

Al variare del parametro $k,$ studiare la monotonia, specificando la natura dei punti stazionari

La funzione, razionale intera, è definita, continua e derivabile su tutto l’asse reale.
Studiamo la derivata prima:

f'(x) $= 4kx3 + 3x^{2} + 4kx= x(4kx2 + 3x+ 4k)$

Se $k= 0:$ f'(x) $= x(3x) = 3x^{2} \ge 0$ per ogni $x,$ in paricolare f'(x) $= 0$ per $x= 0,$ che è
punto stazionario. Essendo per $x\ne 0$ f'(x) $> 0,$ la funzione è sempre crescente. Quindi:

Se $k= 0$ la funzione è sempre crescente ed ha in $x= 0$ un punto stazionario che è punto di flesso a
tangente orizzontale.

Se $k> 0: x(4kx2 + 3x+ 4k) \ge 0;$ il discriminante dell’equazione $4kx2 + 3x+ 4k= 0$ è:
$\Delta = 9 -64k^{2}.$ Si hanno i seguenti casi:
o Se $9-64k^{2}< 0,$ quindi $k< -$
$8$ vel $k>$
$8$ (quindi $k>$
$8$ perché $k> 0)$
$4kx2 + 3x+ 4k> 0$ per ogni $x$ . Pertanto:
f'(x) $\ge 0$ per $x\ge 0$ (e la derivata si annulla per $x= 0).$ Pertanto:
▪
Se $k>$
$8$ la funzione è decrescente per $x< 0,$ crescente per $x> 0$ ed ha un punto
stazionario in $x= 0$ che è un punto di minimo relativo (ed anche assoluto).

Australe $2$ Sessione Ordinaria 2023 Problema $2$

o Se $9-64k^{2}> 0,$ cioè -
$8 < k<$
$8$ , equivalente a $0< k<$
$8$ , risulta:
$4kx2 + 3x+ 4k\ge 0$ per $x\le$
$-3-\sqrt{9}-64k^{2}$
$8k$
$= x^{1}$ vel $x\ge$
$-3+\sqrt{9}-64k^{2}$
$8k$
$= x^{2}.$
E risulta
$-3-\sqrt{9}-64k^{2}$
$8k$
$< 0.$ Confrontiamo
$-3+\sqrt{9}-64k^{2}$
$8k$
con $0.$ Si ha:

$-3 + \sqrt{9} -64k^{2}$
$8k$
$\ge 0$ se $-3 + \sqrt{9} -64k^{2} \ge 0, \sqrt{9} -64k^{2} \ge 3,$

$-64k^{2} \ge 0$ MAI. Pertanto
$-3+\sqrt{9}-64k^{2}$
$8k$
$< 0$ sempre. Ne consegue che se
$0 < k<$
$8 , 4kx2 + 3x+ 4k\ge 0$ per $x\le$
$-3-\sqrt{9}-64k^{2}$
$8k$
vel $x\ge$
$-3+\sqrt{9}-64k^{2}$
$8k$
(con entrambi
i valori negativi).

$-3 -\sqrt{9} -64k^{2}$
$8k$

$-3 + \sqrt{9} -64k^{2}$
$8k$

$m^{1} M m^{2}$

In conclusione, per $0< k<$
$$ 3 8: $$

se $x<$
$-3-\sqrt{9}-64k^{2}$
$8k$
funzione decrescente, per
$-3-\sqrt{9}-64k^{2}$
$8k$
$< x<$
$-3+\sqrt{9}-64k^{2}$
$8k$
crescente
$$ per $$
$-3+\sqrt{9}-64k^{2}$
$8k$
$< x< 0$ decrescente , per $x> 0$ crescente.
La funzione ha quindi tre punti stazionari:
$x=$
$-3-\sqrt{9}-64k^{2}$
$8k$
(minimo relativo), $x=$
$-3+\sqrt{9}-64k^{2}$
$8k$
(massimo relativo)
ed $x= 0$ (minimo relativo).

$4kx2 + 3x+ 4k$

f'(x)

Australe $2$ Sessione Ordinaria 2023 Problema $2$

o Se $9-64k^{2}= 0 , k= \pm$
$8$ , quindi (essendo $k> 0) k=$
$8$ risulta:
f'(x) $= 4kx3 + 3x^{2} + 4kx= x(3$
$2 x^{2} + 3x+ 3$
$$ 2) = 3 $$
$2 x(x^{2} + 2x+ 1)$
ed è: f'(x) =
$2 x(x^{2} + 2x+ 1) =$
$2 x(x+ 1)^{2} \ge 0$ per $x\ge 0$ , con f'(x) $= 0$ per
$x= 0$ e $x= -1.$

In questo caso, quindi, $k=$
$8$ , la funzione decresce per $x< 0,$ cresce per $x> 0 :x= 0$
è punto di minimo relativo (ed anche assoluto). Inoltre $x= -1$ è punto di flesso a tangente
orizzontale.

Riepilogando:

Se $k= 0$ la funzione è sempre crescente ed ha in $x= 0$ un punto stazionario che è punto di flesso a
tangente orizzontale.

Se $0< k<$
$8$ la funzione è decrescente se $x<$
$-3-\sqrt{9}-64k^{2}$
$8k$
crescente per
$-3-\sqrt{9}-64k^{2}$
$8k$
$< x<$
$-3+\sqrt{9}-64k^{2}$
$8k$
, decrescente per
$-3-\sqrt{9}-64k^{2}$
$8k$
$< x< 0$ e crescente per $x> 0;$ ha
quindi tre punti stazionari:

$x=$
$-3-\sqrt{9}-64k^{2}$
$8k$
(minimo relativo), $x=$
$-3+\sqrt{9}-64k^{2}$
$8k$
(massimo relativo)
ed $x= 0$ (minimo relativo).

Se $k=$
$8$ , la funzione decresce per $x< 0,$ cresce per $x> 0 :x= 0$
è punto di minimo relativo (ed anche assoluto). Inoltre $x= -1$ è punto di flesso a tangente
orizzontale.

Se $k>$
$8$ la funzione è decrescente per $x< 0,$ crescente per $x> 0$ ed ha un punto stazionario in
$x= 0$ che è un punto di minimo relativo (ed anche assoluto).

Tracciare il grafico $\gamma 1$ della funzione corrispondente al valore $k=$
$8$ determinando, in particolare, le
coordinate dei due punti di flesso $F^{1}$ e $F^{2}$ . Scrivere le equazioni delle rette $t^{1}$ e $t^{2}$ tangenti a $\gamma 1$ in $F^{1}$ e $F^{2}.$

Per $k=$
$8$ la funzione ha equazione: $y=$ f(x) =
$8x^{4}+ x^{3}+$
$4x^{2}$

Dominio: tutto $R.$

$Pari/Dispari: f(-x) =$
$8 x^{4} -x^{3} +$
$4 x^{2}:$ né pari né dispari.

Intersezioni con gli assi cartesiani: se $x= 0, y= 0.$ Se $y= 0.:$
$8 x^{4} + x^{3} +$
$4 x^{2} = 0,$

Australe $2$ Sessione Ordinaria 2023 Problema $2$

$3x^{4} + 8x^{3} + 6x^{2} = 0 , x^{2}(3x^{2} + 8x+ 6) = 0$ solo se $x= 0$ (che è soluzione doppia, quindi il grafico è
tangente all’asse delle $x$ nell’origine degli assi), perché il discriminante di $3x^{2} + 8x+ 6 = 0$ è negativo.

Segno della funzione:

f(x) $\ge 0$ se $x^{2}(3x^{2} + 8x+ 6) \ge 0:$ la funzione è sempre positiva per ogni $x\ne 0$ (in $x= 0,$ come già
detto si annulla).

Limiti agli estremi del dominio:

$$ lim $$
$x\to \mp \infty$ (
$8 x^{4} + x^{3} +$
$4 x^{2}) =$ lim
$x\to \mp \infty$ (
$8 x^{4}) = +\infty$ . Notiamo che non ci sono asintoti orizzontali né obliqui.

Monotonia:

f'(x) =
$2 x^{3} + 3x^{2} +$
$2 x=$
$2 x(x^{2} + 2x+ 1) =$
$2 x(x+ 1)^{2} = 0$ se $x= 0$ e $x= -1.$ Risulta:

Se $x< -1:$ f'(x) $< 0:$ funzione decrescente.
Se $x= -1:$ f'(x) $= 0: x= -1$ punto stazionario
Se $-1 < x< 0:$ f'(x) $< 0:$ funzione decrescente (quindi $x= -1$ è punto di flesso a tangente orizzontale)
Se $x= 0:$ f'(x) $= 0: x= 0$ punto stazionario.
Se $x> 0:$ f'(x) $> 0:$ funzione crescente, $x= 0$ punto di minimo relativo (ed anche assoluto): $m= (0; 0)$

Concavità:

f''(x) =
$2 x^{2} + 6x+$
$2 \ge 0$ se $3x^{2} + 4x+ 1 \ge 0; x1,2 =$
$-2\pm 1$
$3$ ; quindi $x^{1} = -1$ e $x^{2} = -$
$$ 1 3. $$
Pertanto: f''(x) $\ge 0$ se $x\le -1$ vel $x\ge -$
$3.$ Per quanto riguarda la concavità:

se $x< -1:$ concavità verso l’alto
se $-1 < x< -$
$3:$ concavità verso il basso ( $x= -1$ punto di flesso)
se -
$3 < x< 0:$ concavità verso l’alto $(x= -$
$3$ punto di flesso)
Se $x> 0:$ concavità verso l’alto.

$L$ funzione ha quindi due punti di flesso:

se $x= -1, y=$
$8 -1 +$
$$ 3 4 = 1 $$
$8: F^{1} = (-1;$
$$ 1 8) $$

se $x= -$
$3: y=$
$$ 3 8 ( 1 $$
$81) -$
$$ 1 27 + 1 12 = 11 $$
$216: F^{2} = (-$
$$ 1 3 ; 11 216). $$

Cerchiamo le equazioni delle tangenti inflessionali. Risulta: $f'(-1) = 0$ ed $f' (-$
$3) = -$
$$ 1 2 ( 4 $$
$9) = -$
$$ 2 9. $$
Pertanto, indicando con $t^{1}$ la tangente in $F^{1}$ e con $t^{2}$ la tangente in $F^{2},$ abbiamo:

$t^{1}: y-1$
$$ 8 = 0, $$
$y= 1$

$t^{2}: y-$
$$ 11 $$
$216 = -$
$9 (x+$
$3) , y= -$
$9 x-$
$$ 5 216 $$

Australe $2$ Sessione Ordinaria 2023 Problema $2$

Il grafico è quindi il seguente:

Scrivere l’equazione $y=$ g(x) della funzione simmetrica di $f$ rispetto all’asse delle ascisse e se ne
tracci il grafico rappresentativo $\gamma 2$ nel medesimo piano cartesiano in cui è tracciato $\gamma 1.$ Si indichi con $R$
la regione delimitata da $\gamma 1, \gamma 2$ e dalla retta $x= 1$ e se ne calcoli l’area.

L’equazione della funzione $g$ è la seguente: $y=$ g(x) $= -f(x) = -($
$8 x^{4} + x^{3} +$
$4 x^{2}).$ Il suo grafico,
insieme a quello della funzione $f$ è il seguente:

Australe $2$ Sessione Ordinaria 2023 Problema $2$

L’area della regione $R$ è data da:
Area(R) $= 2 \int$ f(x)
$$ 1 0 $$
$dx= 2 \int (3$
$8 x^{4} + x^{3} + 3$
$4 x^{2})$
$$ 1 0 $$
$dx= 2$ [ $3$
$40 x^{5} + 1$
$4 x^{4} + 1$
$4 x^{3}]$
$$ 0 1 = $$

$$ = 2 [ 3 40 + 1 4 + 1 $$
$4 -(0)] = 46$
$40 u^{2} \cong 1.15 u^{2} =$ Area(R)

Calcolare, al variare del parametro $k,$ i seguenti limiti:
$$ lim $$
$x\to 0$ fk(x)
senk(x) lim
$x\to \infty$
fk(x)
$|senk(x)|$

Calcoliamo il primo limite, ricordando che $k\ge 0.$
Notiamo che la funzione $y=$
fk(x)
senk(x) ha il numeratore che è sempre definito
(fk(x) $= kx4 + x^{3} + 2kx2$ con $k\ge 0).$ Il denominatore non è definito (dato che $k\ge 0$ è un numero
reale qualsiasi) quando sen(x) $\le 0$ (si pensi a (-
$$ 1 2) 1 $$
$2 = \sqrt -$
$$ 1 2 ). $$
Dovendo calcolare il limite per $x\to 0,$ non ci interessa che la funzione sia definita in $x= 0.$ Per avere
sen(x) $> 0$ supponiamo che il limite sia per $x\to 0+,$ così il denominatore è definito in un intorno destro
“piccolo a piacere” di $x= 0, 0$ escluso, che è quello che ci interessa per poter calcolare il limite.
Ricordiamo che per $x\to 0+,$ risulta sin (x)~x e quindi (dato che stiamo considerando sen(x) $> 0),$ si
ha senk(x)~xk. Pertanto:
$$ lim $$
$x\to 0+$
fk(x)
senk(x) = lim
$x\to 0+$
$kx4+x^{3}+2kx2$
senk(x)
= lim
$x\to 0+$
$2kx2$
$xk$ .

Si hanno i seguenti casi:
$1)$ Se $k= 2$ il limite è $4$
$2)$ Se $0 \le k< 2$ il limite è $0+$ (il numeratore è infinitesimo di ordine superiore)
$3)$ Se $k> 2$ il limite è $+\infty$ (il numeratore è infinitesimo di ordine inferiore)
Calcoliamo il secondo limite, ricordando che $k\ge 0.$

In questo caso le cose sono più complicate. Infatti, come nel caso precedente, per poter calcolare senk(x)
per ogni numero reale deve essere sen $(x) > 0,$ cioè $2h\pi < x< \pi + 2h\pi$ , con $h\in Z.$ Quindi la
funzione di cui dobbiamo calcolare il limite non esiste in TUTTO un intorno di $+\infty$ (cioè per ogni
numero reale $x> N)$ né in TUTTO un intorno di $-\infty$ (cioè per ogni numero reale $x< -N).$

Australe $2$ Sessione Ordinaria 2023 Problema $2$

Questo fatto pone dei grossi problemi se vogliamo applicare la definizione “semplificata” di limite che
viene normalmente data dai libri di testo usati nei Licei. Infatti, di solito, prima di dare la definizione di
limite per $x\to c,$ si dice che $c$ deve essere (ovviamente) un punto di accumulazione per il dominio $D$
della funzione, con $D\subseteq R,$ e si aggiunge che la funzione sia definita in un intorno I di $c$ (al più escluso
$c).$
Quando si dà la definizione di limite per $x\to +\infty$ si suppone che la funzione sia definita in un intorno I
di $+\infty$ (quindi in un intervallo del tipo $x> N)$ ed analogamente, quando si dà la definizione di limite per
$x\to -\infty$ si suppone che la funzione sia definita in un intorno I di $-\infty$ (quindi in un intervallo del tipo
$x< -N).$ Quando infine si da la definizione di limite per $x\to \infty$ si suppone che la funzione sia definita
in un intorno di $\infty$ (cioè per tutti gli $x$ che soddisfano una disequazione del tipo $|x| > N,$ con $N> 0,$ che
equivale a $x< -N \cup x> N).$
Secondo la definizione che abbiamo richiamato (quella data normalmente nei Licei), il lim
$x\to \infty$
$kx4+x^{3}+2kx2$
$|senk(x)|$
NON ESISTE, perché non esiste un intorno I di $\infty$ in cui la funzione sia sempre definita.
La definizione più generale di limite (che non viene normalmente utilizzata al Liceo), prevede che la
funzione sia definita in $I(\infty ) \cap D,$ cioè nei punti di un intorno di $\infty$ che appartengono al dominio $D$ della
funzione. Le due definizioni che si possono dare (limite finito e limite infinito) sono del tipo seguente.
Definizione generale di limite finito all’infinto:
Data una funzione $y=$ f(x), definita in un in un sottoinsieme $D$ di numeri reali $(D\subseteq R),$ si dice che
f(x) $\to l$ per $x\to \infty$ , e si scrive lim
$x\to \infty$ f(x) $= l,$ se:
$\forall \varepsilon > 0 \exists I\varepsilon (\infty )/ \forall x\in I\cap D$ si ha: $|f(x) -l| < \varepsilon$
Detto a parole: fissato un $\varepsilon > 0$ (piccolo quanto si vuole, ma questa frase, di uso comune, è superflua),
esiste in corrispondenza di esso un numero reale $N> 0$ tale che, per ogni $x$ del dominio $D$ della
funzione per cui si abbia $|x| > N$ (equivalente a $x< -N \cup X> N)$ risulti: $|f(x) -l| < \varepsilon$
(equivalente a $l-\varepsilon <$ f(x) $< l+ \varepsilon$ ).
Definizione generale di limite infinito all’infinto:
Data una funzione $y=$ f(x), definita in un in un sottoinsieme $D$ di numeri reali $(D\subseteq R),$ si dice che
f(x) $\to \infty$ per $x\to \infty$ , e si scrive lim
$x\to \infty$ f(x) $= \infty$ , se:
$\forall M> 0 \exists IM(\infty )/ \forall x\in I\cap D$ si ha: $|f(x)| > M$
Detto a parole: fissato un $M> 0$ (grande quanto si vuole, ma anche questa frase, di uso comune, è
superflua), esiste in corrispondenza di esso un numero reale $N> 0$ tale che, per ogni $x$ del dominio $D$
della funzione per cui si abbia $|x| > N$ (equivalente a
$x< -N \cup X> N)$ risulti: $|f(x)| > M$ (equivalente a f(x) $< -M \cup$ f(x) $> M).$
Fatta questa doverosa estensione della definizione di limite, per calcolare lim
$x\to \infty$
$kx4+x^{3}+2kx2$
$|senk(x)|$ analizziamo
due casi, ricordando che il dominio $D$ della funzione è tutto l’asse reale privato degli (infiniti)
intervalli del tipo $\pi + 2h\pi \le x\le 2\pi + 2h\pi$ , con $h\in Z$ e $h\ge 0$ e $-\pi -2h\pi \le x\le -2h\pi$ , con
$h\in Z$ e $h\ge 0$ e che in $D$ risulta sen(x) $> 0$ affinché abbia senso la potenza $ad$ esponente reale
senk(x).

Australe $2$ Sessione Ordinaria 2023 Problema $2$

Primo caso: $k> 0$
In questo caso, essendo sen $x> 0$ risulta $0 <$ sen $x\le 1$ e quindi $0 < |senk(x)| \le 1$
$$ lim $$
$x\to \infty$
$kx4+x^{3}+2kx2$
$|senk(x)|$
= lim
$x\to \infty$
$$ kx4 $$
$|senk(x)| =$ [
$+\infty$
quantità che oscilla fra $0$ escluso ed $1$ compreso] $= +\infty$ .

N.B. Siccome $0 < |senk(x)| \le 1$ risulta
$$ kx4 $$
$|senk(x)| \ge kx4$ che tende a $+\infty$ , quindi anche
$$ kx4 $$
$|senk(x)| \to +\infty$ .

In conclusione: lim
$x\to \infty$
$kx4+x^{3}+2kx2$
$|senk(x)|$
$= +\infty$ (per ogni $k> 0).$

Secondo caso $k= 0$

Anche in questo caso, essendo sen $x> 0$ risulta $0 <$ sen $x\le 1$ e quindi senk(x) $= sen0(x) = 1.$

Il limite diventa: lim
$x\to \infty$
$$ x^{3 $$
$sen0(x) =$ lim
$x\to \infty x^{3} = \infty$

In conclusione: se $k= 0$ il limite è $\infty$ (precisamente $-\infty$ se $x\to -\infty$ e $+\infty$ se $x\to +\infty$ ).

Osservazione sul secondo limite

Se al denominatore invece di $|senk(x)|$ ci fosse stato $|sen(x)|k$ la discussione sarebbe stata più semplice,
perché avremmo evitato i casi in cui sen(x) $< 0,$ quindi il dominio $D$ sarebbe stato tutto l’asse reale
privato dei punti $x= h\pi$ , con $h\in Z.$ Sarebbe stato comunque necessario lo stesso utilizzare la
definizione “generale” di limite, perché anche in tal caso la funzione non sarebbe stata definita in
TUTTO un intorno di $\infty$ .

Con la collaborazione di Angela Santamaria

## Quesito 1

Australe $2$ Sessione Ordinaria 2023 Questionario

QUESITO $1$

Data una circonferenza di centro O, siano $PA$ e $PB$ i segmenti di tangente alla circonferenza condotti da un
suo punto esterno $P.$ Dimostrare che il quadrilatero AOBP ha le diagonali perpendicolari.

Osserviamo che i triangoli rettangoli OAP e OBP sono congruenti, avendo l’ipotenusa $OP$ in comune ed i
cateti $OA$ ed $OB$ congruenti perché raggi della stessa circonferenza. Segue che gli angoli corrispondenti
AOP e BOP sono congruenti. Il triangolo AOP è isoscele sulla base $AB (OA$ ed $OB$ congruenti). $OH$ è la
bisettrice dell’angolo al vertice O, pertanto, per una nota proprietà dei triangoli isosceli, $OH$ è anche altezza
relativa alla base $AB:$ pertanto $OP$ ed $AB$ sono perpendicolari c.v.d.

QUESITO $2$

È data un’urna contenente $10$ palline bianche e $6$ palline nere. Calcolare la probabilità che:
•
Estraendo una pallina, sia di colore nero;
•
Estraendo due palline contemporaneamente, siano entrambe di colore nero;
•
Estraendo due palline contemporaneamente, siano di colore diverso.

Calcoliamo la probabilità che, estraendo una pallina, sia di colore nero.

$p=$
$$ 6 16 = 3 $$
$8=0.375 = 37.5$ %

Calcoliamo la probabilità che, estraendo due palline contemporaneamente, siano entrambe di colore
nero.

Il numero di coppie favorevoli è dato da: $C6,2 = (6$
$$ 2) = 15. $$
Il numero di coppie possibili è dato da: $C16,2 = (16$
$$ 2 ) = 120. $$

Quindi: $p=$
$$ 15 120 = 1 $$
$8 = 0.125 = 12.5$ %.

## Quesito 2

Australe $2$ Sessione Ordinaria 2023 Questionario

QUESITO $1$

Data una circonferenza di centro O, siano $PA$ e $PB$ i segmenti di tangente alla circonferenza condotti da un
suo punto esterno $P.$ Dimostrare che il quadrilatero AOBP ha le diagonali perpendicolari.

Osserviamo che i triangoli rettangoli OAP e OBP sono congruenti, avendo l’ipotenusa $OP$ in comune ed i
cateti $OA$ ed $OB$ congruenti perché raggi della stessa circonferenza. Segue che gli angoli corrispondenti
AOP e BOP sono congruenti. Il triangolo AOP è isoscele sulla base $AB (OA$ ed $OB$ congruenti). $OH$ è la
bisettrice dell’angolo al vertice O, pertanto, per una nota proprietà dei triangoli isosceli, $OH$ è anche altezza
relativa alla base $AB:$ pertanto $OP$ ed $AB$ sono perpendicolari c.v.d.

QUESITO $2$

È data un’urna contenente $10$ palline bianche e $6$ palline nere. Calcolare la probabilità che:
•
Estraendo una pallina, sia di colore nero;
•
Estraendo due palline contemporaneamente, siano entrambe di colore nero;
•
Estraendo due palline contemporaneamente, siano di colore diverso.

Calcoliamo la probabilità che, estraendo una pallina, sia di colore nero.

$p=$
$$ 6 16 = 3 $$
$8=0.375 = 37.5$ %

Calcoliamo la probabilità che, estraendo due palline contemporaneamente, siano entrambe di colore
nero.

Il numero di coppie favorevoli è dato da: $C6,2 = (6$
$$ 2) = 15. $$
Il numero di coppie possibili è dato da: $C16,2 = (16$
$$ 2 ) = 120. $$

Quindi: $p=$
$$ 15 120 = 1 $$
$8 = 0.125 = 12.5$ %.

## Quesito 3

Australe $2$ Sessione Ordinaria 2023 Questionario

Calcoliamo la probabilità che estraendo due palline contemporaneamente, siano di colore diverso.

Il numero di coppie favorevoli è dato da: $10 ∙6 = 60.$
Il numero di coppie possibili è dato da: $C16,2 = (16$
$$ 2 ) = 120. $$

Quindi: $p=$
$$ 60 120 = 1 $$
$2 = 0.50 = 50$ %.

QUESITO $3$

Determinare le coordinate della proiezione ortogonale $H$ del punto A $(4, -1, 1)$ sul piano
$\pi : 3x-2y+ z= 1.$ Determinare le equazioni cartesiane del luogo geometrico dei punti di $\pi$ che hanno
distanza $3\sqrt{2}$ da A.

$H$ è l’intersezione fra la normale $n$ al piano $\pi$ passante per A ed il piano stesso. La normale ha vettore
$(3; -2; 1),$ che sono i coefficienti di $x, y$ e $z$ dell’equazione del piano. Essa ha quindi equazioni
parametriche:

$n:$ {
$x= x^{0} + lt$
$y= y^{0} + mt$
$z= z^{0} + nt$
$x= 4 + 3t$
$y= -1 -2t$
$z= 1 + t$

Intersecando col piano $\pi : 3x-2y+ z= 1$ otteniamo:

$3(4 + 3t) -2(-1 -2t) + (1 + t) = 1,14t+ 15 = 1, t= -1:H= (1,1,0)$

Determiniamo le equazioni cartesiane del luogo geometrico dei punti di $\pi$ che hanno distanza $3\sqrt{2}$ da
A.

Il luogo si ottiene intersecando la sfera $S$ di centro A e raggio $3\sqrt{2}$ con $\pi$ , quindi sarà una circonferenza del
piano dato.

$S: (x-4)^{2} + (y+ 1)^{2} + (z-1)^{2} = 18; x^{2} + y^{2} + z^{2} -8x+ 2y-2z= 0$

Il luogo richiesto è quindi la circonferenza (con centro in $H)$ di equazioni:

${x^{2} + y^{2} + z^{2} -8x+ 2y-2z= 0$
$3x-2y+ z= 1$

N.B.

Indicato con $r= HP$
̅̅̅̅ (con $P$ punto della circonferenza appartenente al piano e alla sfera) il raggio della
circonferenza, con $R$ quello della sfera, con $H$ il centro della circonferenza, risulta:

$r^{2} = HP$
$̅̅̅̅2 = AP2$
̅̅̅̅̅ $-AH$
$̅̅̅̅2$

Infatti il triangolo AHP è rettangolo in $H.$ Calcoliamo $AH$
̅̅̅̅, distanza di A dal piano $\pi$ :

$$ AH $$
̅̅̅̅ =
$|ax0 + by0 + cz0 + d|$
$\sqrt{a}^{2} + b^{2} + c^{2}$
$|12 + 2 + 1 -1|$
$$ \sqrt{9} + 4 + 1 = 14 \sqrt{14} = \sqrt{14 $$

## Quesito 4

Australe $2$ Sessione Ordinaria 2023 Questionario

Pertanto:
$r^{2} = AP2$
̅̅̅̅̅ $-AH$
$̅̅̅̅2 = R^{2} -(\sqrt{14})$
$2 = 18 -14 = 4.$

Quindi $r= 2.$

Il luogo geometrico dei punti di $\pi$ che hanno distanza $3\sqrt{2}$ da A è la circonferenza del piano $\pi$ di
centro $H= (1,1,0)eraggio2.$

QUESITO $4$

È data la parabola di equazione f(x) $= -3x^{2} + 6x.$ Determinare le coordinate del punto $P$ appartenente
alla porzione di curva f(x) nel I quadrante tale che sia massimo il prodotto delle distanze di $P$ dagli assi
cartesiani.

La parabola ha asse verticale, vertice $(1; 3),$ taglia l’asse delle $x$ in $(0; 0)$ e $(2; 0),$ e l’asse delle $y$ in $(0; 0).$

Posto $P= (x; -3x^{2} + 6x),$ con $0 \le x\le 2$ ed indicati con A e $B$ le proiezioni di $P$ sugli assi cartesiani,
deve essere:

## Quesito 5

Australe $2$ Sessione Ordinaria 2023 Questionario

$y= PA$
̅̅̅̅ ∙PB
̅̅̅̅ = max

Risulta: $y= |x| ∙|-3x^{2} + 6x| = x(-3x^{2} + 6x),$ con $0 \le x\le 2.$

$y= -3x^{3} + 6x^{2}, y' = -9x^{2} + 12x\ge 0$ se $3x^{2} -4x\le 0: 0 \le x\le 4/3$ . Pertanto:
$y$ cresce per $0 \le x<$
$3 x$ e decresce per
$3 < x\le 2.$ Quindi $y$ è massima se $x=$
$3$ . Per tale valore di $x$ si
ha $y=$
$3 (-$
$$ 16 3 + 8)) = 4 3 ( 8 3) = 32 $$
$9$ . Perciò:

Le coordinate del punto $P$ appartenente alla porzione di curva f(x) nel I quadrante tale che sia massimo il
prodotto delle distanze di $P$ dagli assi cartesiani sono: (
$$ 4 3 ; 8 $$
$3).$ Il massimo prodotto è uguale a
$$ 32 9 . $$

QUESITO $5$

Gli angoli di un triangolo, inscritto in una circonferenza di raggio $7 m,$ hanno ampiezze $\alpha , \beta$ e $\gamma$ .
Sapendo che $\alpha =$
$\pi$
$6$ e che $cos\beta =$
$7$ , determinare il valore di $sen\gamma$ e l’area del triangolo ABC.

Rappresentiamo il triangolo inscritto nella circonferenza:

Dati: $R= 7, \alpha =$
$\pi$
$6$ , cos $\beta =$
$$ 1 7 . $$

Risulta: $\gamma = \pi -$
$\pi$
$6 -\beta =$
$6 \pi -\beta$ , quindi:
sen $\gamma =$ sen $(5$
$6 \pi -\beta ) =$ sen $5$
$6 \pi cos\beta -cos 5$
$6 \pi$ sen $\beta = 1$
$$ 2 (1 7) + \sqrt{3 $$
$2$ sen $\beta$ .

Calcoliamo sen $\beta$ (che è $> 0,$ perché $0 < \beta < \pi /2$ , essendo $cos\beta$ positivo):
sen $\beta = \sqrt{1} -cos2 \beta = \sqrt{1} -(1$
$$ 7) 2 = \sqrt{48} 49 = 4 7 \sqrt{3 $$
Quindi:
sen $\gamma = 1$
$$ 14 + \sqrt{3} 2 (4 7 \sqrt{3}) = 1 14 + 12 14 = 13 $$
$14 =$ sen $\gamma$

Il valore di $sen\gamma$ è
$$ 13 14. $$

## Quesito 6

Australe $2$ Sessione Ordinaria 2023 Questionario

Calcoliamo l’area del triangolo ABC.

Per il Teorema della corda si ha: $BC$
̅̅̅̅ $= 2R$ sen $\alpha = 14$ ∙
$2 m= 7 m= BC$
̅̅̅̅ (a conferma che $BC$ è una
corda che insiste su un angolo alla circonferenza uguale a
$\pi$
$6,$ quindi $BC$
̅̅̅̅ $= R).$
Per lo stesso Teorema si ha:

$$ AB $$
̅̅̅̅ $= 2R$ sen $\gamma = 14 ∙13$
$14 = 13 m$
Quindi:
Area(ABC) $= 1$
$2 AB$
̅̅̅̅ ∙BC
̅̅̅̅ sen $\beta = 1$
$$ 2 (13)(7) (4 $$
$7 \sqrt{3}) = 26 \sqrt{3} m^{2}$

L’area del triangolo ABC è $26 \sqrt{3} m^{2}.$

QUESITO $6$

Assegnata la curva $y= ax3 + bx2 + cx+ d,$ determinare i valori dei parametri reali in modo che passi
per $A(-1, -3), B(1, -1)$ e risulti tangente in $C(0,1)$ alla retta $t: y= 2x+ 1.$ Determinare infine le
coordinate dell’ulteriore punto $Q$ di intersezione tra la curva e la retta $t.$

Passaggio per $A(-1, -3): -3 = -a+ b-c+ d$
Passaggio per $B(1, -1): -1 = a+ b+ c+ d$
Passaggio per $C(0,1): 1 = d$

Per essere tangente a $t: y= 2x+ 1$ in $C(0,1),$ deve essere $f'(0) = 2.$ Ma è: f'(x) $= 3ax2 + 2bx+ c$
quindi: $2 = c.$
Abbiamo quindi il sistema:

$-3 = -a+ b-c+ d$
$-1 = a+ b+ c+ d$
$1 = d$
$2 = c$
$-3 = -a+ b-2 + 1$
$-1 = a+ b+ 2 + 1$
$1 = d$
$2 = c$
$-2 = -a+ b$
$-4 = a+ b$
$1 = d$
$2 = c$
$-3 = b$
$-1 =$ a
$1 = d$
$2 = c$

La curva assegnata ha quindi equazione: $y= -x^{3} -3x^{2} + 2x+ 1.$

Determiniamo le coordinate dell’ulteriore punto $Q$ di intersezione tra la curva e la retta $t.$

$y= 2x+ 1$
$y= -x^{3} -3x^{2} + 2x+ 1$ ; {
$y= 2x+ 1$
$2x+ 1 = -x^{3} -3x^{2} + 2x+ 1$

Risolviamo la seconda equazione:

$x^{3} + 3x^{2} = 0 : x= 0$ (doppia)e $x= -3.$

Con $x= 0$ otteniamo $C(0,1),$ con $x= -3$ otteniamo $Q= (-3; -5)$

L’ulteriore punto $Q$ di intersezione tra la curva e la retta $t$ è quindi: $Q= (-3; -5).$

## Quesito 7

Australe $2$ Sessione Ordinaria 2023 Questionario

QUESITO $7$

Dimostrare che la regione finita di piano contenuta nel primo quadrante, delimitata dagli assi cartesiani
e dalla curva $y=$
$k-x$
$x^{2}+k^{2}$ con $k> 0,$ ha area costante pari a
$\pi$
$4 -$
$ln 2$

La curva taglia l’asse delle ascisse in $x= k> 0$ e l’asse delle ordinate in $y=$
$k$ .
Inoltre $y> 0$ per $x< k.$ La regione richiesta è del tipo:

Quindi l’area si ottiene calcolando il seguente integrale:

$Area= \int$
$k-x$
$x^{2} + k^{2}$
$$ k 0 $$
$dx= \int$
$x^{2} + k^{2}$
$$ k 0 $$
$dx-\int$
$x^{2} + k^{2}$
$$ k 0 dx $$

Cerchiamo una primitiva di $y=$
$x^{2}+k^{2}:$

$$ \int k $$
$x^{2} + k^{2} dx= \int$
$k^{2} (1 + x^{2}$
$k^{2})$
$dx= \int$
$k(1 + (x$
$k)$
$dx= \int$
$$ 1 k $$
$1 + (x$
$k)$
$2 dx=$ arctg $(x$
$k) + c$

Cerchiamo una primitiva di $y=$
$x^{2}+k^{2}$

$$ \int x $$
$x^{2} + k^{2} dx= 1$
$$ 2 \int $$
$2x$
$x^{2} + k^{2} dx= 1$
$2 ln(x^{2} + k^{2}) + h$

Pertanto:

$Area= \int$
$k-x$
$x^{2} + k^{2}$
$$ k 0 $$
$dx= \int$
$x^{2} + k^{2}$
$$ k 0 $$
$dx-\int$
$x^{2} + k^{2}$
$$ k 0 $$
$dx=$ [arctg $(x$
k)]
$$ 0 k $$
$-[1$
$2 ln(x^{2} + k^{2})]$
$$ 0 k = $$

$= arctg(1) -0 -[1$
$2 ln(2k^{2}) -1$
$2 lnk2] = \pi$
$4 -[ln \sqrt{2k^{2}} -ln$ k] $= \pi$
$4 -(ln\sqrt{2} + ln\sqrt{k^{2}} -lnk)) =$

$= \pi$
$4 -(ln\sqrt{2} + ln k-ln k) = \pi$
$4 -ln\sqrt{2} = \pi$
$4 -1$
$2 ln 2 = \pi$
$4 -ln 2$
$2 =$ Area $c. v. d$

## Quesito 8

Australe $2$ Sessione Ordinaria 2023 Questionario

QUESITO $8$

Data la funzione f(x) =
$1+be-cx$ con $a> 0 , b> 0$ e $c> 0,$ determinare a, $b, c$ sapendo che f(x) ha
come asintoto orizzontale la retta $y= 5$ e che il grafico di f(x) è tangente, in $x= 0,$ alla retta di
equazione $y=$
$5 x+ 1.$

Risulta:

$$ lim $$
$x\to -\infty$
$1+be-cx=$ [
$1+be+\infty$ ] = [
$+\infty$ ] $= 0$ ( per $x\to -\infty y= 5$ non è asintoto orizzontale)
$$ lim $$
$x\to +\infty$
$1 + be-cx=$ [
$1 + be-\infty$ ] = [
$1 + 0] =$ a

Quindi, affinché ci sia l’asintoto orizzontale $y= 5,$ deve essere $a= 5$ (l’asintoto orizzontale c’è solo
per $x\to +\infty$ ).

Dall’equazione della retta $y=$
$5 x+ 1$ troviamo che per $x= 0$ si ha $y= 1.$ Quindi il punto di tangenza
è $T= (0; 1).$ Il grafico di f(x) =
$1+be-cx$ deve quindi passare per tale punto: $1 =$
$1+b=$
$1+b$ ,
$1 + b= 5$ ,
quindi $b= 4.$

Perciò la funzione ha equazione del tipo: f(x) =
$1+4e-cx.$

Siccome il grafico di f(x) deve essere tangente alla retta data in $T= (0; 1),$ si ha:
$f'(0) =$
$5 =$ m(retta).
Ma:
f'(x) =
$-5(-4ce-cx)$
$(1+4e-cx)2$ ed $f'(0) =$
$20c$
$$ 25 = 2 $$
$5,$ perciò: $c=$
$$ 1 2. $$

Risulta quindi: $a= 5, b= 4$ e $c=$
$$ 1 2 . $$

Con la collaborazione di Angela Santamaria
