---
tipo: soluzione
prova_stem: 2002_suppletiva_ordinamento_2002_problema1_27
pdf: Prova_Maturita_2002.pdf
source: text
title: 2002 Suppletiva Ordinamento — Problema 1 — Svolgimento
---


Suppletiva Ordinamento 2002 -
Problema $1$

ORDINAMENTO 2002 - SESSIONE SUPPLETIVA - PROBLEMA $1$

Se il polinomio f(x) si divide per $x^{2}- 1$ si ottiene $x$ come quoziente e $x$ come resto.

a)

Determinare f(x).

Risulta:

f(x) $= x(x^{2} -1) + x= x^{3}$

$b)$

Studiare la funzione $y=$
f(x)
$x^{2}-1$ e disegnarne il grafico $G$ in un piano riferito a un
sistema di assi cartesiani ortogonali (Oxy), dopo aver trovato, in particolare, i suoi
punti di massimo, minimo e flesso e i suoi asintoti.

La funzione da studiare ha equazione:

$y=$ f(x)
$x^{2} -1 =$
$$ x^{3 $$
$x^{2} -1 = x(x^{2} -1) + x$
$x^{2} -1$
$= x+$
$x^{2} -1$

N.B. Notiamo che la funzione è nella forma $y= mx+ q+$ g(x) con g(x) $\to 0$ se $x\to \pm \infty$
possiamo quindi affermare che ammette l’asintoto obliquo di equazione $y= x$ (per la
dimostrazione di questa proprietà si veda su matefilia la pagina
$http://www.matefilia.it/argomen/asintoti/asintoti.htm)$

Studiamo la funzione nella forma

$y=$
$$ x^{3 $$
$x^{2} -1$

Dominio:

$x^{2} -1 \ne 0 , x\ne \pm 1 : -\infty < x< -1 \cup -1 < x< 1 \cup 1 < x< +\infty$

Suppletiva Ordinamento 2002 -
Problema $1$

Intersezioni con gli assi cartesiani:

Se $x=0: y=0;$ se $y=0: x=0.$

Simmetrie notevoli:

$f(-x) =$
$-x^{3}$
$x^{2}-1 = -f(x):$ la funzione è dispari; il suo grafico è simmetrico rispetto
all’origine.

Segno della funzione:

$y=$
$$ x^{3 $$
$x^{2} -1 > 0$

$N>0$ se $x>0$
$D>0$ se $x^{2} -1 > 0$ , da cui $x< -1$ vel $x> 1$
Quindi:
$y>0$ se $-1 < x< 0$ vel $x> 1$

Limiti:
$$ lim $$
$x\to \pm \infty$
$$ x^{3 $$
$x^{2} -1 =$ lim
$x\to \pm \infty$
$$ x^{3 $$
$x^{2} =$ lim
$x\to \pm \infty x= \pm \infty$

(abbiamo già evidenziato la presenza dell’asintoto obliquo).

$$ lim $$
$x\to 1\pm$
$$ x^{3 $$
$x^{2} -1 = \pm \infty =$
$$ lim $$
$x\to (-1)\pm$
$$ x^{3 $$
$x^{2} -1$

Abbiamo gli asintoti verticali di equazioni $x= \pm 1$ .

Derivata prima:

$D$ (
$$ x^{3 $$
$x^{2} -1) = x^{4} -3x^{2}$
$(x^{2} -1)^{2} \ge 0$ se $x^{4} -3x^{2} \ge 0, x^{2}(x^{2} -3) \ge 0$
La derivata prima si annulla per $x= 0$ e $x= \pm \sqrt{3}$ , è positiva se $x< -\sqrt{3}$ vel $x> \sqrt{3}$
Quindi la funzione è crescente se $x< -\sqrt{3}$ vel $x> \sqrt{3}$ e decrescente altrove:
$x= 0$ è un punto di flesso a tangente orizzontale; $x= -\sqrt{3}$ è punto di massimo relativo,
con ordinata $y(-\sqrt{3}) = -$
$$ 3\sqrt{3 $$
$2 ; x= \sqrt{3}$ è punto di minimo relativo, con ordinata
$y(\sqrt{3}) =$
$$ 3\sqrt{3} 2 . $$

Derivata seconda:

y'' =
$2x^{3}+6x$
$(x^{2}-1)^{3} \ge 0$ se
$2x(x^{2}+3)$
$x^{2}-1$
$\ge 0$ se
$x^{2}-1 \ge 0$ : il segno della derivata seconda

Suppletiva Ordinamento 2002 -
Problema $1$

coincide con quello della funzione, quindi la concavità è verso l’alto dove la funzione è
positiva $(-1 < x< 0$ vel $x> 1),$ verso il basso altrove. In $x=0,$ come già trovato con lo
studio della derivata prima, abbiamo un flesso (di ordinata $0).$

Il grafico della funzione è il seguente:

$c)$

Trovare l’equazione della retta $t$ tangente a $G$ nel suo punto di ascissa
$$ 1 2 . $$

La retta $t$ ha equazione:

$y-y($
$2) = y'$ (
$2) (x-$
$2);$ risulta:

$y($
$$ 1 2) = ( x^{3 $$
$x^{2}-1)$
$x=1$
$$ 2 = 1 8 1 $$
$4-1 = -$
$8$ ∙
$3 = -$
$6 ; y'$ (
$$ 1 2) = ( $$
$x^{4}-3x^{2}$
$(x^{2}-1)^{2})$
$x=1$
= -
$$ 11 9 $$

Quindi:

$t: y+ 1$
$6 = -11$
$9 (x-1$
$2) , y= -11$
$9 x+ 4$

Suppletiva Ordinamento 2002 -
Problema $1$

$d)$

Determinare le coordinate dei punti comuni alla retta $t$ e alla curva $G.$

Dobbiamo risolvere il seguente sistema:

$y= -11$
$9 x+ 4$
$y=$
$$ x^{3 $$
$x^{2} -1$
$$ ; x^{3 $$
$x^{2} -1 = -11$
$9 x+ 4$
$9 ; 9x^{3} = (x^{2} -1)(-11x+ 4);$

$20x^{3} -4x^{2} -11x+ 4 = 0$

Tale equazione ha la soluzione doppia $x=$
$2$ , poiché $t$ è tangente a $G$ nel punto di
ascissa
$$ 1 2 . $$
Quindi possiamo abbassare di grado due volte l’equazione mediante la regola di Ruffini,
ottenendo:

$20x^{3} -4x^{2} -11x+ 4 = (5 x + 4)(2 x -1)^{2}$

L’equazione quindi ha la soluzione doppia $x=$
$2$ e la soluzione semplice $x= -$
$5 . G$ e $t$ ha
quindi le seguenti intersezioni:

A: {
$x= 1$
$y= -1$
; $B:$ {
$x= -4$
$y= 64$
$$ 45 $$

Suppletiva Ordinamento 2002 -
Problema $1$

e)

Dopo aver determinato i numeri a, $b$ tali che sussista l’identità:

$x^{2}-1=$
$x+ 1+$
$x-1$

calcolare una primitiva della funzione $y=$
f(x)
$x^{2}-1$ .

$$ Da x $$
$x^{2}-1 =$
$x+1 +$
$x-1$ segue:
$x^{2}-1 =$
$a(x-1)+b(x+1)$
$x^{2}-1$
da cui $x= x(a+ b) -a+ b$

e per il Principio di identità dei polinomi deve essere:

{ $a+ b= 1$
$-a+ b= 0$ ; {
$b= 1$
$a= 1$

Quindi:
$x^{2}-1 =$
$$ 1 2 $$
$x+1 +$
$$ 1 2 $$
$x-1$

f(x)
$x^{2}-1 = x+$
$x^{2}-1 = x+$
$$ 1 2 $$
$x+1 +$
$$ 1 2 $$
$x-1$ ; cerchiamone una primitiva:

$\int$ f(x)
$x^{2} -1 dx= \int (x+$
$$ 1 2 $$
$x+ 1 +$
$$ 1 2 $$
$x-1) dx= x^{2}$
$$ 2 + 1 $$
$2 ln|x+ 1| + 1$
$2 ln|x-1| + C=$

$= x^{2}$
$$ 2 + 1 $$
$2 ln|x^{2} -1| + C$

Con la collaborazione di Angela Santamaria
