---
tipo: soluzione
prova_stem: 2011_liceo_della_comunicazione_2011_sessione_ordinaria_problema2_65
pdf: Prova_Maturita_2011.pdf
source: text
title: >-
  Soluzioni —
  2011_liceo_della_comunicazione_2011_sessione_ordinaria_problema2_65
---
# Soluzioni — 2011_liceo_della_comunicazione_2011_sessione_ordinaria_problema2_65

## Problema 1

Liceo della comunicazione 2011
Sessione $Ordinaria-$ Problema $2$

Liceo della comunicazione 2011 - PROBLEMA $2$

Nel piano, riferito $ad$ assi cartesiani Oxy, sono dati i punti: $A(2; 1), B(-2; 1), C(2; 3),$
$D(2; 5), E(6; 5).$

a)

Si verifichi che il quadrilatero convesso ABDE è un parallelogramma del quale $C$ è il
punto d’incontro delle diagonali. Si calcoli l’area del quadrilatero.

I lati opposti $AB$ e $DE,$ entrambi paralleli all’asse $x,$ sono tra loro paralleli; inoltre hanno la
stessa lunghezza (pari per entrambi a $4):$ quindi ABDE è un parallelogramma.

Cerchiamo l’intersezione tra le diagonali. La retta $AD$ ha equazione $x=2;$ la retta $BE$ ha
coefficiente angolare: $m=$
$yE-yB$
$xE-xB =$
$$ 4 8 = 1 $$
$2$ quindi la sua equazione (come retta per $B)$ è:
$y-1 =$
$2 (x+ 2) , y=$
$2 x+ 2$

L’intersezione fra le diagonali è quindi:

$x= 2$
$y= 1$
$2 x+ 2 , C= (2; 3), c. v. d.$

Calcoliamo l’area del quadrilatero:

Area(ABDE) $= AB∙AD= 4 ∙4 u^{2} = 16 u^{2}$

Liceo della comunicazione 2011
Sessione $Ordinaria-$ Problema $2$

$b)$

Si consideri il fascio di curve di equazione

$y= x^{2} + 2x+$ a
$2x-4$

dove a è un parametro reale. Si verifichi che, qualunque sia a, la curva corrispondente
ammette il punto $C$ come centro di simmetria e le rette $AD$ e $BE$ come asintoti.

Le equazioni della simmetria rispetto al punto $C= (2; 3)$ sono:

{x' $= 2a-x$
$y' = 2b-y$ ; {x' $= 4 -x$
$y' = 6 -y$ ; da cui: ${x= 4 -x'$
$y= 6 -y'$ ;

Troviamo la simmetrica della curva data (evitiamo per semplicità gli apici):

$6 -y=$
$(4 -x)2 + 2(4 -x) +$ a
$2(4 -x) -4$
$= x^{2} -10x+ 24 -a$
$4 -2x$
, $y= 6 -x^{2} -10x+ 24 +$ a
$4 -2x$

$y= -x^{2} -2x-a$
$4 -2x$
$= x^{2} + 2x+$ a
$2x-4$

Quindi $C$ è centro di simmetria per ogni a.

La funzione di equazione $y=$
$x^{2}+2x+a$
$2x-4$ ha l’asintoto verticale $x=2$ (infatti il limite per $x$ che
tende a $2$ è infinito): è la retta $AD.$ La funzione è razionale fratta con il grado del
numeratore che supera di $1$ il grado del denominatore, quindi ammette asintoto obliquo, il
cui coefficiente angolare è:

$m=$ lim
$x\to \infty$
f(x)
= lim
$x\to \infty$
$x^{2} + 2x+$ a
$2x^{2} -4x$
$$ = 1 2 $$

$q=$ lim
$x\to \infty$ [f(x) $-mx] =$ lim
$x\to \infty [x^{2} + 2x+$ a
$2x-4$
$-1$
$2$ x] = lim
$x\to \infty [x^{2} + 2x+ a-x^{2} + 2x$
$2x-4$
$$ ] = 4 2 = 2 $$

Quindi, per ogni valore di a, abbiamo l’asintoto obliquo di equazione:

$y=$
$2 x+ 2,$ che coincide con la retta $BE.$

$c)$

Si determini la curva $\lambda$ del fascio passante per il punto $P(0; 1)$ e si verifichi che le rette
$AB$ e $DE$ sono tangenti a $\lambda$ . Si tracci il grafico di $\lambda$ .

Liceo della comunicazione 2011
Sessione $Ordinaria-$ Problema $2$

La curva $\lambda$ si ottiene da $y=$
$x^{2}+2x+a$
$2x-4$ imponendo il passaggio per $P(0; 1):$

$$ 1 = a $$
$-4 , a= -4;$ quindi la curva $\lambda$ ha equazione:

$y=$ f(x) $= x^{2}+ 2x-4$
$2x-4$

Verifichiamo che $AB,$ di equazione $y=1,$ è tangente a $\lambda$ :

$y= 1$
$y=$
$x^{2}+2x-4$
$2x-4$
$x^{2}+2x-4$
$2x-4$
$= 1, x^{2} + 2x-4 = 2x-4 , x= 0$ (doppio)

Quindi $\lambda$ è tangente $ad AB$ in $(0; 1).$

Verifichiamo che $DE,$ di equazione $y=5,$ è tangente a $\lambda$ :

$y= 5$
$y= x^{2} + 2x-4$
$2x-4$
, $x^{2} + 2x-4$
$2x-4$
$= 5, x^{2} + 2x-4 = 10x-20 , x^{2} -8x+ 16 = 0$ ,

$(x-4)^{2} = 0,$
$x= 4$ (doppio)

Quindi $\lambda$ è tangente a $DE$ in $(4; 1).$

Studiamo ora la funzione di equazione:

$y=$ f(x) $= x^{2} + 2x-4$
$2x-4$

La funzione è definita e continua per ogni $x$ diverso da $2.$ La sua equazione può essere
riscritta nella seguente forma:

$y(2x-4) = x^{2} + 2x-4 , x^{2} -2xy+ 2x+ 4y-4 = 0$ che è una conica; senza effettuare
il riconoscimento di tale conica, avendo già verificato che proviene da una funzione che
ha due asintoti possiamo affermare che si tratta di un’iperbole. Per completare lo studio è
sufficiente quindi studiare la derivata prima per determinare il massimo ed il minimo.

f'(x) =
$4x^{2} -16x$
$8x^{2} -32x+ 32 =$
$x^{2} -4x$
$2x^{2} -8x+ 8 = x^{2} -4x$
$2(x-2)^{2} \ge 0$ se $x\le 0 or x\ge 4$
La funzione è quindi crescente per $x<0$ e per $x>4$ e decrescente per $0<x<2$ e $2<x<4:$
$x=0$ è quindi il punto di massimo e $x=4$ il punto di minimo, con ordinate rispettivamente $1$
e $0.$

Il grafico di $\lambda$ è quindi il seguente:

Liceo della comunicazione 2011
Sessione $Ordinaria-$ Problema $2$

Per completare il grafico mancano solo le intersezioni con l’asse $x:$

$x^{2} + 2x-4$
$2x-4$
$x^{2} + 2x-4 = 0$ ,
$x= -1 \pm \sqrt{5}$

$d)$

Si calcoli l’area della regione finita di piano delimitata da $\lambda$ , dalla retta $BE,$ dalla retta di
equazione $x= -2$ e dall’asse $y.$

La regione di cui si chiede l’area è indicata nella seguente figura:

L’area richiesta si ottiene mediante il seguente calcolo integrale:

$Area= \int (1$
$2 x+ 2 -x^{2} + 2x-4$
$2x-4$
$-2$
$dx= \int (x^{2} -2x+ 4x-8 -x^{2} -2x+ 4$
$2(x-2)$
$-2$
$dx=$
$$ \int ( $$
$-4$
$2(x-2))$
$-2$
$dx= -2 \int$ (
$(x-2))$
$-2$
$dx= -2[ln|x-2|]-2$

$= -2(ln2 -ln4) = (2ln2) u^{2} \cong 1.39 u^{2} =$ Area

Con la collaborazione di Angela Santamaria
