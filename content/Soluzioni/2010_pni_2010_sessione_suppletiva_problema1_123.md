---
tipo: soluzione
prova_stem: 2010_pni_2010_sessione_suppletiva_problema1_123
pdf: Prova_Maturita_2010.pdf
source: text
title: Soluzioni — 2010_pni_2010_sessione_suppletiva_problema1_123
---
# Soluzioni — 2010_pni_2010_sessione_suppletiva_problema1_123

## Problema 1

PNI 2010 SESSIONE SUPPLETIVA - PROBLEMA $1$

E’ data una circonferenza di centro O e diametro $AB$
̅̅̅̅ $= 2.$ Sul prolungamento del
diametro $AB,$ dalla parte di $B,$ si prenda un punto $P$ e da esso si conduca una tangente
alla circonferenza.

Detti $T$ il punto di tangenza e $Q$ il punto di intersezione di questa tangente con la tangente
in A alla circonferenza, si calcoli il rapporto:

$TQ2 + TP2$
$$ AP2 , $$

espresso in funzione di $x= BP$
̅̅̅̅, controllando che risulta :

f(x) $= x^{2} + 1$
$x^{2} + 2x$ .

Notiamo che risulta $x> 0$ .

Dalla similitudine fra i triangoli APQ e TPO si ha:

$AQ: AP= OT: TP$

Essendo $AQ= TQ$ e $TP2 = OP2 -OT2 = (1 + x)2 -1 = x^{2} + 2x$ si ha:

$TQ: (2 + x) = 1: \sqrt{x}^{2} + 2x \implies TQ=$
$2 + x$
$\sqrt{x}^{2} + 2x$
$= \sqrt{x}^{2} + 2x$
$\implies TQ2 = x+ 2$

Quindi:

f(x) $= TQ2 + TP2$
$$ AP2 = $$
$x+ 2$
$+ x^{2} + 2x$
$(x+ 2)^{2}$
$= x+ 2 + x(x^{2} + 2x)$
$x(x+ 2)^{2}$
$= (x+ 2)(1 + x^{2})$
$x(x+ 2)^{2}$
$= 1 + x^{2}$
$x(x+ 2)$

Quindi:

f(x) $= x^{2}+ 1$
$x^{2}+ 2x c. v. d$

Prescindendo dalla questione geometrica, si studi la funzione f(x) e se ne tracci il grafico
$\gamma$ .

$y=$ f(x) $= x^{2} + 1$
$x^{2} + 2x$

Dominio:

$x^{2} + 2x\ne 0$ da cui $x\ne -2$ e $x\ne 0 : -\infty < x< -2 , -2 < x< 0 , 0 < x< +\infty$

Simmetrie notevoli:

Visto l’intervallo di studio, non si pone il problema di stabilire se la funzione è pari o

dispari.

Intersezioni con gli assi cartesiani:

Se $x= 0 , y=$ ∄.

Se $y= 0, x^{2} + 1 = 0:$ mai

Quindi non ci sono intersezioni con gli assi cartesiani.

Segno della funzione:

$y> 0$ se
$x^{2}+1$
$x^{2}+2x> 0 \implies x^{2} + 2x> 0, x< -2$ vel $x> 0$

Limiti:

$$ lim $$
$x\to \pm \infty$
$x^{2} + 1$
$x^{2} + 2x= 1 : y= 1$ asintoto orizzontale

$$ lim $$
$x\to (-2)\mp x^{2} + 1$
$x^{2} + 2x=$
$$ lim $$
$x\to (-2)\mp x^{2} + 1$
$x(x+ 2) = \pm \infty : x= -2$ asintoto verticale

$$ lim $$
$x\to 0\mp x^{2} + 1$
$x^{2} + 2x=$ lim
$x\to 0\mp x^{2} + 1$
$x(x+ 2) = \mp \infty : x= 0$ asintoto verticale

Derivata prima:

f'(x) $= D($
$x^{2}+1$
$x^{2}+2x) =$
$2(x^{2}-x-1)$
$x^{2}(x+2)^{2} \ge 0$ se $x^{2} -x-1 \ge 0 : x\le$
$1-\sqrt{5}$
$or x\ge \sqrt{5}+1$

Quindi la funzione è crescente se $x< -2 or -2 < x<$
$1-\sqrt{5}$
$or x> \sqrt{5}+1$
$2$ e
decrescente se

$1-\sqrt{5}$
$< x< 0 or 0 < x< \sqrt{5}+1$
$2$ : quindi abbiamo un punto di massimo relativo in

$x=$
$1-\sqrt{5}$
$2$ ed un punto di minimo relativo in $x=$
$$ 1+\sqrt{5 $$
$2$ ; le ordinate di tali punti sono:

$f(1 -\sqrt{5}$
) $\cong -1.6 , f(1 + \sqrt{5}$
) $\cong +0.62$

Derivata seconda:

f''(x) $= D(2(x^{2} -x-1)$
$x^{2}(x+ 2)^{2} ) = -4x^{3} + 6x^{2} + 12x+ 8$
$x^{3}(x+ 2)^{3}$
$\ge 0$

Siccome $f''(2) \cong 0.05 > 0$ e $f'(3) = -0.003 < 0$ abbiamo un flesso fra $x=2$ e $x=3.$
Tralasciamo lo studio completo della derivata seconda.

Il grafico della funzione è il seguente:

Si calcolino i numeri a, $b, c$ in modo che risulti:

$(1) x^{2} + 1$
$x^{2} + 2x= a+ b$
$x+$
$x+ 2$ .

$x^{2} + 1$
$x^{2} + 2x= a+ b$
$x+$
$x+ 2 = ax(x+ 2) + b(x+ 2) + cx$
$x(x+ 2)$
$\implies$

$x^{2} + 1 = ax2 + (2a+ b+ c)x+ 2b \implies$ {
$a= 1$
$2a+ b+ c= 0$
$2b= 1$

$a= 1$
$b= 1$
$c= -2a-b= -2 -1$
$2 = -5$
$\implies$
$a= 1$
$b= 1$
$c= -5$

Quindi:

f(x) $= x^{2} + 1$
$x^{2} + 2x= 1 + 1$
$2x-$
$2(x+ 2)$

Tenendo presente la scomposizione $(1),$ si calcoli l’area della regione piana, limitata da $\gamma$ ,
dal suo asintoto orizzontale e dalla retta d’equazione $x= 2$ .

Determiniamo l’intersezione A tra la curva e l’asintoto orizzontale:

$y= 1$
$y= x^{2} + 1$
$x^{2} + 2x$
; $x^{2} + 1 = x^{2} + 2x ; xA= 1$

L’area richiesta si ottiene quindi calcolando il seguente integrale:

$Area= \int [1 -f(x)] dx$
$$ 2 1 2 $$
$= \int [1 -(1 + 1$
$2x-$
$2(x+ 2))] dx$
$$ 2 1 2 $$
$= \int [-1$
$2x+$
$2(x+ 2)] dx=$
$$ 2 1 2 $$

$= [-1$
$2$ ln(x) $+ 5$
$2 ln(x+ 2)]1$
$$ 2 2 $$
$= -1$
$2 ln2 + 5$
$2 ln4 -(-1$
$2 ln1$
$$ 2 + 5 $$
$2 ln5$
$2) = -1$
$2 ln2 + 5$
$2 ln4 +$

$2 ln1$
$2 -5$
$2 ln5$
$2 = -1$
$2 ln2 + 5$
$2 ln4 -1$
$2 ln2 -5$
$2 ln5$
$2 = -ln2 + 5$
$2 (ln4 -ln5$

$= -ln2 + 5$
$2 ln8$
$5 \cong 0.48 u^{2} =$ Area

Con la collaborazione di Angela Santamaria, Simona Scoleri e Stefano Scoleri
