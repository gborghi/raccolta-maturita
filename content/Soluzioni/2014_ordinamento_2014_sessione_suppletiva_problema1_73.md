---
tipo: soluzione
prova_stem: 2014_ordinamento_2014_sessione_suppletiva_problema1_73
pdf: Prova_Maturita_2014.pdf
source: text
title: Soluzioni — 2014_ordinamento_2014_sessione_suppletiva_problema1_73
---
# Soluzioni — 2014_ordinamento_2014_sessione_suppletiva_problema1_73

## Problema 1

ORDINAMENTO 2014 SESSIONE SUPPLETIVA - PROBLEMA $1$

Sono dati un quarto di cerchio AOB e la tangente $t ad$ esso in A. Dal punto O si mandi
una semiretta che intersechi l’arco $AB$ e la tangente $t,$ rispettivamente, in $M$ ed $N.$

Posto $AÔM= \alpha$ , si calcoli il rapporto

e lo si esprima in funzione di $x=$ sen

controllando che risulta: f(x) =

$AÔM= \alpha$ , con $0< \alpha <$

Indicato con $R$ il raggio della circonferenza, abbiamo:

$MA= 2R$ sen

$= 2Rx$ (per il teorema della corda)
$MN= ON-OM=$
cos $\alpha -R= R^{1} -cos \alpha$
cos $\alpha$
$= R$
$2sen \alpha$
cos $\alpha$
$2 -sen \alpha$
$= R$
$2sen \alpha$
$1 -2$ sen $\alpha$
$2Rx$
$1 -2x$
Quindi risulta:

$$ MN $$
$MA=$
$2Rx2$
$1 -2x^{2}$
$2Rx$
$1 -2x =$ f(x)

Prescindendo dalla questione geometrica, si studi la funzione f(x) e se ne tracci il grafico
$\gamma$ .

f(x) =
$1 -2x$

Dominio: $1 -2x \ne 0 \implies x\ne \pm \sqrt$
$-\infty < x< -\sqrt{2}$
$2 , -\sqrt{2}$
$2 < x< \sqrt{2}$
$$ 2 , \sqrt{2 $$
$2 < x< +\infty$

Simmetrie notevoli: $f(-x) = -$ f(x), quindi la funzione è dispari (grafico simmetrico rispetto
all’origine).

Intersezioni con gli assi cartesiani: il grafico interseca gli assi nell’origine.

Segno della funzione:

$> 0 ... .... -\infty < x< -\sqrt$
, $0 < x< \sqrt$

Limiti:

lim $\pm$
$1-2x^{2} = 0 (y=0$ asintoto orizzontale; non ci sono asintoti obluiqui)

$$ lim (\sqrt ) $$
$\pm$
$1-2x^{2} = \infty (x= \sqrt$
asintoto verticale)

$$ lim ( \sqrt ) $$
$\pm$
$1-2x^{2} = \infty (x= -\sqrt$
asintoto verticale)

Derivata prima:

$f (x) =$
$2 x + 1$
$(2x -1)$

$f (x) > 0 \forall x$ del dominio: la funzione è sempre crescente.

Derivata seconda:

$f (x) =$

$f (x) > 0$ se

Abbiamo un flesso nell’origine.

Il grafico della funzione è il seguente:

Si scriva l’equazione della tangente a $\gamma$ nel punto di flesso; si scriva poi l’equazione della
circonferenza con il centro nel suddetto punto di flesso e tangente agli asintoti verticali di
$\gamma$ .

La tangente a $\gamma$ nel punto di flesso $O=(0;0)$ ha equazione $y-0 = f'(0)(x-0);$ essendo
$f (0) = 1,$ la tangente ha equazione: $y= x.$

La circonferenza richiesta ha centro in O e raggio $\sqrt$
Equazione circonferenza: $x + y =$

Si determini l’area della regione di piano limitata dalla curva $\gamma$ dall’asse $x$ e dalle rette di
equazioni $x=$

e $x=$

$A= \int$ f(x)dx

$$ = \int x $$
$1 -2x^{2} dx$

$= -1$
$$ 4 \int $$
$-4x$
$1 -2x^{2} dx$

$= -1$
$4 [ln|1 -2x^{2}|]$

$4 ln(14$
$\cong 0.11$

Con la collaborazione di Angela Santamaria, Simona Scoleri e Stefano Scoleri
