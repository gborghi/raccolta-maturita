---
tipo: soluzione
prova_stem: 2010_ordinamento_2010_sessione_suppletiva_problema2_128
pdf: Prova_Maturita_2010.pdf
source: text
title: Soluzioni — 2010_ordinamento_2010_sessione_suppletiva_problema2_128
---
# Soluzioni — 2010_ordinamento_2010_sessione_suppletiva_problema2_128

## Problema 1

ORDINAMENTO 2010 SESSIONE SUPPLETIVA - PROBLEMA $2$

In un sistema di riferimento cartesiano Oxy, si denoti con $\Gamma$ a il grafico della funzione

fa(x) $= (x-a)e^{2}-x$

dove a è un parametro reale positivo ed e è il numero di Nepero.

Si dimostri che, al variare di a, le curve $\Gamma$ a tagliano l’asse delle $x$ secondo lo stesso
angolo $\alpha$ . Si determini l’ampiezza di $\alpha$ in gradi e primi sessagesimali.

Il punto di intersezione con l’asse $x$ ha ascissa: $x=$ a .

Calcoliamo la derivata prima della funzione:

$$ fa $$
'(x) $= e^{2}-x$
$a+ (x-a) ∙[e^{2}-x$
$a∙(-1$
a)] $= e^{2}-x$
$a-1$
$ax∙e^{2}-x$
$a+ e^{2}-x$
$a= 2e^{2}-x$
$a-1$
$ax∙e^{2}-x$

Detto $\alpha$ l’angolo formato tra la tangente alla curva nel punto di ascissa $x=$ a e l’asse
delle $x,$ risulta:

$tg\alpha = fa$
'(a) $= 2e-e= e=$ costante al variare di a .

Essendo $tg\alpha =$ e risulta $\alpha =$ arctg(e) $\cong 69.802^\circ \cong 69^\circ 48'$

Si dimostri che la tangente a $\Gamma$ a nel punto di flesso, descrive, al variare di a, un fascio di
rette parallele. Si determini l’equazione di tale fascio.

Calcoliamo la derivata seconda della funzione:

$$ fa $$
'(x) $= 2e^{2}-x$
$a-1$
$ax∙e^{2}-x$
$a= (2 -x$
a) $e^{2}-x$

$$ fa $$
''(x) $= -1$
$a∙e^{2}-x$
$a+ (2 -x$
a) $∙[e^{2}-x$
$a∙(-1$
a)] $= -3$
$a∙e^{2}-x$
$a+ x$
$a^{2} ∙e^{2}-x$
$a= 1$
$a∙e^{2}-x$
a(x
$a-3) \ge 0$

$$ se x $$
$a-3 \ge 0$ (ricordiamo che $a> 0),$ quindi: $x\ge 3a$ ; abbiamo quindi la concavità
verso il basso se $x< 3a$ e verso l’alto se $x> 3a$ : pertanto $x= 3a$ è punto di flesso.
Il flesso ha dunque coordinate:

$F= (3a; 2a∙e-1) = (3a; 2a$
e)

Cerchiamo la tangente nel punto di flesso.

$$ fa $$
$'(3a) = -$
$e= m=$ costante ; la tangente in $F$ descrive quindi un fascio di rette parallele.

Equazione del fascio:

$y-2a$
$e= -1$
$e(x-3a) \implies ey-2a+ x-3a= 0 \implies x+ ey-5a= 0$

Posto $a= 1,$ si studi $f^{1}(x)$ e si tracci $\Gamma 1$ .

Per a $= 1$ abbiamo:

$y= f^{1}(x) = (x-1)e^{2}-x$

Studiamo la funzione di equazione:

$y=$ f(x) $= (x-1)e^{2}-x$

Dominio: $-\infty \le x\le +\infty$

Simmetrie notevoli:

$f(-x) = (-x-1)e^{2}+x\ne$ f(x): la funzione non è pari
$f(-x) \ne -f(x):$ la funzione non è dispari

Intersezioni con gli assi cartesiani:

Se $x= 0 , y= -e^{2}$
Se $y= 0, x= 1$ .

Segno della funzione:

$y> 0$ se $x-1 > 0 , x> 1$

Limiti:

$limx\to -\infty (x-1)e^{2}-x= limx\to -\infty (x)e-x= -\infty$ (non può esserci asintoto obliquo, poiché la

funzione non è un infinito di ordine $1).$

$limx\to +\infty (x-1)e^{2}-x= limx\to +\infty (x)e-x= limx\to +\infty$
$ex= 0+$ (asintoto $y= 0$ per $x\to +\infty$ )

Derivata prima:

f'(x) $= (2 -$
a) $e^{2}-x$
$a= (2 -x)e^{2}-x\ge 0$ se $2 -x\ge 0, x\le 2$ .
La funzione è crescente se $x< 2$ , decrescente se $x> 2$

$x= 2$ è punto di massimo relativo (e assoluto), ed è $f(2) = 1$

Derivata seconda:

f''(x) =
$a∙e^{2}-x$
a(
$a-3) = e^{2}-x∙(x-3) \ge 0$ se $x-3 \ge 0, x\ge 3$

Quindi il grafico volge la concavità verso l’alto se $> 3$ e verso il basso se $x< 3.$

$x= 3$ è un punto di flesso (ordinata $f(3) =$
e).

Il grafico della funzione è il seguente:

Si calcoli l’area S(k) della regione di piano del primo quadrante delimitata da $\Gamma 1$ , dall’asse
$x$ e dalla retta $x= k,$ con $k> 1.$ Cosa si può dire di S(k) quando $k\to +\infty$ ?

L’area richiesta si ottiene calcolando il seguente integrale:

S(k) $= \int (x-1)e^{2}-x dx$
$$ k 1 $$

Cerchiamo, integrando per parti, una primitiva di f(x) $= (x-1)e^{2}-x$ .

$\int (x-1)e^{2}-x dx= \int (x-1)(-e^{2}-x)' dx= -e^{2}-x(x-1) -\int 1 ∙(-e^{2}-x)dx=$

$= -e^{2}-x(x-1) -e^{2}-x+ C= -x∙e^{2}-x+ C$

Quindi:

S(k) $= \int (x-1)e^{2}-x dx$
$$ k 1 $$
$= [-x∙e^{2}-x]1$
$k= -k∙e^{2}-k-(-e) = e-k∙e^{2}-k$

Risulta infine:

$$ lim $$
$k\to +\infty$ S(k) = lim
$k\to +\infty (e-k∙e^{2}-k) =$ e

Con la collaborazione di Angela Santamaria, Simona Scoleri e Stefano Scoleri

PNI 2010 - SESSIONE SUPPLETIVA

QUESITO $1$

In cima $ad$ una roccia a picco sulla riva di un fiume è stata costruita una torretta
d’osservazione alta $11$ metri. Le ampiezze degli angoli di depressione per un punto situato
sulla riva opposta del fiume, misurate rispettivamente dalla base e dalla sommità della
torretta, sono pari a $18^\circ$ e $24^\circ$ . Si determini la larghezza del fiume in quel punto.

Indichiamo con $h$ l’altezza della roccia e con $l$ la larghezza del fiume. Risulta:

$h= l∙tg18^\circ , h+ 11 = l∙tg24^\circ$ , da cui:

$tg18^\circ = h+ 11$
$tg24^\circ \implies h(tg24^\circ -tg18^\circ ) = 11 tg18^\circ \implies h=$
$11 tg18^\circ$
$tg24^\circ -tg18^\circ \cong 29.71 m$

Quindi:
$l=$
$tg18^\circ \cong 29.71 m$
$tg18^\circ \cong 91.44 m$

Il fiume, nel punto richiesto, è quindi largo circa $91.44$ metri.

QUESITO $2$

Considerata la funzione: f(x) =
$33x-ax$
$6x-5x,$ dove a è una costante reale positiva, si determini

tale costante, sapendo che $limx\to 0$ f(x) $= 2$ .

Risulta:
$$ lim $$
$x\to 0$ f(x) = lim
$x\to 0$
$33x-ax$
$6x-5x=$ lim
$x\to 0$
$e3xln3 -exlna$
$exln6 -exln5 =$ lim
$x\to 0$
$(e3xln3 -1) -(exlna-1)$
$(exln6 -1) -(exln5 -1) =$

= lim
$x\to 0$
$3xln3 -xlna$
$xln6 -xln5 = 3ln3 -lna$
$ln6 -ln5 =$
$ln(27$
a)
$ln(6$
$= 2$ se $ln(27$
a) $= 2 ∙ln(6$
$5)$ da cui:

$ln(27$
a) $= ln(36$
$25) \implies 27$
$a= 36$
$25 \implies a= 27 ∙25$
$$ 36 = 75 4 $$

(Nota: ricordiamo che , se f(x) $\to 0$ , risulta: ef(x) $-1$ ~f(x) ).

QUESITO $3$

Su un piano orizzontale $\alpha$ si pongono un cono circolare retto, il cui raggio di base è $r$ e
l’altezza $2r,$ e una sfera di raggio $r.$ A quale distanza $x$ dal piano $\alpha$ bisogna segare questi
due solidi con un piano orizzontale $\beta$ , perché la somma delle aree delle sezioni così
ottenute sia massima?

La distanza $x$ dal piano $\alpha$ soddisfa la limitazione: $0 \le x\le 2r$

La sezione con il cono ha area: S(cono) $= \pi ∙GL2$
Determiniamo $GL.$
Dalla similitudine fra i triangoli GLC e AEC segue che:

$GL: CL= AE: CE \implies GL: (2r-x) = r: 2r \implies GL= 2r-x$

Quindi: S(cono) $= \pi ∙GL2 = \pi$ ∙(
$2r-x$
$$ 2 ) 2 $$

La sezione con la sfera ha area: S(sfera) $= \pi ∙SH2$
Per il secondo teorema di Euclide applicato al triangolo rettangolo FSM risulta:

$SH2 = FH∙HM= x(2r-x)$

Quindi: S(sfera) $= \pi ∙SH2 = \pi ∙x∙(2r-x)$

La somma delle aree delle sezioni è quindi:

$S=$ S(cono) + S(sfera) $= \pi$ ∙(
$2r-x$
$$ 2 ) 2 $$
$+ \pi ∙x∙(2r-x)$ che è massima se lo è:

$y=$ (
$2r-x$
$$ 2 ) 2 $$
$+ x∙(2r-x) = (2r-x)$ (
$2r-x$
$+ x) = (2r-x)$ (
$2r+3x$
) = max se lo è:

$z= (2r-x)(2r+ 3x) = -3x^{2} -4rx+ 4r^{2};$ tale funzione ha per grafico una parabola con
la concavità rivolta verso il basso, quindi ha il massimo nel vertice, cioè per:

$x= -$
$2a=$
$4r$
$$ 6 = 2 $$
$3 r$ , valore che soddisfa la limitazione della $x.$

In conclusione:

La somma delle aree delle sezioni è massima quando la distanza $x$ dal piano di base $\alpha$ è
uguale $ai 2/3$ del raggio di base del cono.

QUESITO $4$

Si dimostri che per gli zeri $x^{1}$ e $x^{2}$ di una funzione f(x) $= ax2 + bx+ c$ vale la relazione
$f'(x^{1}) + f'(x^{2}) = 0$ e si dia una interpretazione geometrica della affermazione dimostrata.

Risulta: f'(x) $= 2ax+ b,$ quindi (ricordando che $x^{1} + x^{2} = -b/a$ )

$f'(x^{1}) + f'(x^{2}) = (2ax1 + b) + (2ax2 + b) = 2a(x^{1} + x^{2}) + 2b= 2a∙(-b$
a) $+ 2b= 0$

Interpretazione geometrica:

le tangenti nei punti di intersezione di una parabola con l’asse delle $x$ hanno coefficienti
angolari opposti.

Infatti $f'(x^{1})$ è il coefficiente angolare della tangente nel punto di ascissa $x^{1}$ ed $f'(x^{2})$ è il
coefficiente angolare della tangente nel punto di ascissa $x^{2}$ e la relazione
$f'(x^{1}) + f'(x^{2}) = 0$ può essere vista nella forma $f'(x^{1}) = -f'(x^{2}).$

QUESITO $5$

Si calcoli il valore medio della funzione $f (x) =$
$ex(x-1)$
$$ x^{2 $$
, nell’intervallo $1 \le x \le 2.$

Il valor medio richiesto è dato da:

$\int$ f(x)dx
$$ b a $$
$b-a$
$\int ex(x-1)$
$$ x^{2} dx 2 1 $$
$2 -1$
$= \int ex(x-1)$
$$ x^{2} dx 2 1 $$

Cerchiamo una primitiva di
$ex(x-1)$
$$ x^{2 $$
integrando per parti:

$\int ex(x-1)$
$$ x^{2 $$
$dx= \int (-1$
$x)$
'
$(ex(x-1))dx= -1$
$xex(x-1) -\int -1$
$x[ex(x-1) + ex]dx=$

$= -ex+ ex$
$x+ \int 1$
$x(xex-ex+ ex)dx= -ex+ ex$
$x+ \int exdx= ex$
$x+ k$

Quindi:

$\int$ f(x)dx
$$ b a $$
$b-a$
$= \int ex(x-1)$
$$ x^{2} dx 2 1 $$
= [ex
x]
$$ 1 2 $$
$= e^{2}$
$2 -e\cong 0.98$

QUESITO $6$

Si determini il punto della parabola $4y= x^{2}$ più vicino al punto di coordinate $(6, -3).$

Indichiamo con $P= (x; y) = (x;$
$4 x^{2})$ il generico punto della parabola e calcoliamo la
distanza da punto dato $A= (6; -3).$

$PA= \sqrt{x-6}^{2} + (1$
$4 x^{2} + 3)$

Tale distanza è minima se lo è il suo quadrato $z=$
$$ x^{4} 16 + $$
$5x^{2}$
$2 -12x+ 45$ .
Risulta:

$z' =$
$4 x^{3} + 5x-12 \ge 0$ se $x^{3} + 20x-48 \ge 0$ da cui, abbassando di grado con la
regola di Ruffini, si ha:

$(x -2)(x^{2} + 2 x + 24) \ge 0$ ed essendo $(x^{2} + 2 x + 24) \ge 0 \forall x$ poiché il delta è negativo, si
ha che:
$z' \ge 0$ quando $x\ge 2$ : pertanto $z$ cresce per $x> 2$ e decresce per $x< 2$ :
per $x= 2$ la distanza richiesta risulta minima.
Il punto della parabola più vicino $ad$ A è quindi il punto di coordinate $(2; 1).$

QUESITO $7$

Si consideri l’equazione $x^{3} -3x^{2} + 6x-6 = 0.$
Si dimostri che essa ammette una soluzione reale $x^{0}$ tale che $1 < x^{0} < 2$ .
Avvalendosi di un qualsiasi procedimento iterativo si determini $x^{0}$ a meno di $1/100.$

Consideriamo la funzione di equazione g(x) $= x^{3} -3x^{2} + 6x-6$ e calcoliamo i
valori che assume agli estremi dell’intervallo dato:
$g(1) = 1 -3 + 6 -6 = -2 < 0, g(2) = 8 -12 + 12 -6 = 2 > 0$
Essendo la funzione continua nell’intervallo chiuso e limitato $[1; 2],$ per il teorema
degli zeri ammette almeno uno zero nell’aperto $(1; 2)$ , quindi l’equazione data
ammette almeno una soluzione in tale intervallo. Dimostriamo che tale soluzione è
unica.
g'(x) $= 3x^{2} -6x+ 6 \ge 0$ se $x^{2} -2x+ 2 \ge 0 \forall x$

La funzione è quindi sempre crescente, pertanto il suo grafico interseca una sola
volta l’asse delle $x:$ l’equazione ammette quindi una sola soluzione nell’intervallo
$$ (1; 2). $$

Cerchiamo un valore approssimato a meno di $1/100$ utilizzando il metodo di Newton.
Risulta:
g''(x) $= 6x-6 > 0$ se $x>1:$ quindi per $1<x<2$ risulta g''(x) $∙g(1) < 0,$ pertanto il punto
iniziale dell’iterazione è $x^{0} = b= 2.$

$xn+1 = xn-g(xn)$
g'(xn)

$x^{1} = x^{0} -g(x^{0})$
$g'(x^{0}) = 2 -g(2)$
$g'(2) = 2 -2$
$$ 6 = 5 $$
$3 \cong 1.66667$
$x^{2} = x^{1} -g(x^{1})$
$g'(x^{1}) = 5$
$3 -$
$g(5$
$g'(5$
$$ 3) = 187 $$
$117 \cong 1.5983$
$x^{3} = x^{2} -g(x^{2})$
$g'(x^{2}) = 187$
$117 -$
$g(187$
$$ 117) $$
$g' (187$
$$ 117) $$
$\cong 1.5961$

Quindi la radice approssimata a meno di $1/100$ è $x^{0} = 1.60$



QUESITO $8$

Nel piano cartesiano Oxy è dato il cerchio $C$ con centro nell’origine e raggio $r= 3;$
siano $P(0, 3)$ e $Q(2, \sqrt{5})$ punti di $C.$ Si calcoli il volume del solido ottenuto dalla
rotazione attorno all’asse $x$ del quadrilatero mistilineo PORQ (con $R$ proiezione di $Q$
sull’asse $x).$

Il solido ottenuto è un segmento sferico a due basi,
il cui volume è dato da:
$V= 1$
$2 \pi h(1$
$3 h^{2} + r^{1}$
$2 + r^{2}$
Nel nostro caso:
$h= OR= 2, r^{1} = QR= \sqrt{5} , r^{2} = OP= 3$
Quindi:
$V= 1$
$2 \pi ∙2 (4$
$3 + 5 + 9) = 46$
$3 \pi$

Calcoliamo il volume del solido senza ricorrere alla formula del volume del segmento
sferico. Tale volume equivale a quello del solido generato dalla rotazione attorno
all’asse $x$ dell’arco $PQ.$ Se $y=f(x)$ è l’equazione dell’arco $PQ,$ il volume richiesto è
dato da:
$V= \pi \int f^{2}(x)dx$
$$ b a $$

L’equazione del cerchio è: $x^{2} + y^{2} = 9,$ quindi $f^{2}(x) = y^{2} = 9 -x^{2}$ , quindi:
$V= \pi \int f^{2}(x)dx$
$$ b a $$
$= \pi \int (9 -x^{2})dx= \pi [9x-1$
$3 x^{3}]$
$$ 0 2 $$
$= \pi (18 -8$
$$ 3) 2 0 = 46 $$
$3 \pi$

QUESITO $9$

Siano dati un ottaedro regolare di spigolo $l$ e la sfera in esso inscritta; si scelga a caso un
punto all’interno dell’ottaedro. Si determini la probabilità che tale punto risulti interno alla
sfera.

Ricordiamo che il volume dell’ottaedro regolare di spigolo $l$ è:

V(ottaedro regolare) $= l^{3} ∙\sqrt{2}$

Questa formula si ottiene raddoppiando il volume della piramide regolare retta a base
quadrata con spigoli tutti uguali $ad l;$ infatti:

V(tetraedro) $= 1$
$3 ∙l^{2}$ ∙VO

Ma $VO= \sqrt{VH2} -OH2 = \sqrt (l\sqrt{3}$
$$ 2 ) 2 $$
-(
$$ l 2) 2 = \sqrt{1 $$
$2 l^{2} = l\sqrt{2}$
$2$ quindi:

V(tetraedro) =
$3 ∙l^{2} ∙l\sqrt{2}$
$$ 2 = 1 $$
$6 l^{3}\sqrt{2}$ pertanto:

V(ottaedro regolare) $= 2 ∙1$
$6 l^{3}\sqrt{2} = l^{3} ∙\sqrt{2}$

Calcoliamo ora il raggio della sfera inscritta nell’ottaedro. Il centro della sfera coincide con
il centro O dell’ottaedro.

Il raggio $R$ della sfera inscritta è uguale alla distanza di O dalla faccia VBC, che equivale
all’altezza del triangolo rettangolo VOH relativa all’ipotenusa $VH,$ quindi:

$R=$ OV∙OH
$$ VH = $$
$l\sqrt{2}$
$2$ ∙l
$l\sqrt{3}$
$= l^{2} ∙\sqrt{2}$
$4 ∙2$
$l\sqrt{3}$
$= l∙\sqrt{6}$

Il raggio della sfera inscritta in un ottaedro di spigolo $l$ è dato da $\sqrt{6}$
$6$ ∙l

Il volume della sfera inscritta nell’ottaedro è quindi:

V(sfera inscritta) $= 4$
$3 \pi R^{3} = 4$
$3 \pi (l∙\sqrt{6}$
$$ 6 ) 3 = 4 $$
$3 \pi l^{3} ∙\sqrt{6}$
$36 = \pi l^{3} ∙\sqrt{6}$
$$ 27 $$

La probabilità richiesta è data da:

$p=$ volume favorevole
volume possibile
V(sfera)
V(ottaedro) =
$\pi l^{3} ∙\sqrt{6}$
$$ 27 $$
$l^{3} ∙\sqrt{2}$
$= \pi \sqrt{3}$
$\cong 0.605 \cong 60.5%$

QUESITO $10$

Un’urna contiene $20$ palline, che possono essere rosse o azzurre. Quante sono
quelle azzurre, se, estraendo $2$ palline senza riporre la prima estratta, la probabilità di
estrarre almeno una pallina azzurra è $27/38$ ?

Ricordiamo che l’estrazione senza reimbussolamento equivale
all’estrazione simultanea. Estraiamo due palline e calcoliamo la
probabilità che siano entrambe ROSSE:

$p(2 R) =$
coppie favorevoli
coppie $possibili=$
$Cx,2$
$C20,2 =$
$(x$
$$ 2) (20 2 ) = $$
$x(x-1)$
$20∙19$
$x(x-1)$
$$ 380 $$

La probabilità che almeno una pallina sia azzurra è data da:

p(almeno una azzura) $= 1 -p(2$ rosse) $= 1 -$
$x(x-1)$
$$ 380 = 27 $$
$38$ da cui:

$380 -x(x-1) = 270 , x^{2} -x-110 = 0$ , da cui $x= 11 (x= -10$ non accettabile)

Le palline azzurre sono quindi $20 -x= 20 -11 = 9$

Altro metodo:

$p(A-R) + p(R-A) + p(A-A) = 20 -x$
$$ 20 $$
∙x
$19 + x$
$20 ∙20 -x$
$$ 19 $$
$+ 20 -x$
$$ 20 $$
$∙19 -x$
$$ 19 = 27 38 $$

$x(20 -x)$
$$ 190 $$
$+ (20 -x)(19 -x)$
$$ 380 = 27 $$
$38 \implies 2x(20 -x) + (20 -x)(19 -x) = 270$

da cui $x= 9$ e $x= -11$ come prima.

N.B.

$p(1$ azzurra) $+ p(2azzurre) = 11 ∙9$
$C20,2$
$+ C9,2$
$C20,2$
$$ = 99 190 + 36 190 = 135 190 = 27 38 $$

Con la collaborazione di Angela Santamaria, Simona Scoleri e Stefano Scoleri
