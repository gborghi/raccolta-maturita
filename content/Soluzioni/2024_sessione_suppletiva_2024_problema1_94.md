---
tipo: soluzione
prova_stem: 2024_sessione_suppletiva_2024_problema1_94
pdf: Prova_Maturita_2024.pdf
source: text
title: 2024 Suppletiva — Problema 1 — Svolgimento
---


Sessione suppletiva 2024 - Problema $1$

LICEO SCIENTIFICO SUPPLETIVA 2024 - PROBLEMA $1$

Si consideri il grafico $\gamma$ in figura, rappresentativo di una funzione f(x) =
A(x)
B(x) , dove A(x) e B(x) sono dei
polinomi, definita nel dominio $D= (-\infty ; 0) \cup (0; 2) \cup (2; +\infty$ ).

a)

Analizzando il grafico, si deducano lo zero, l’insieme immagine e gli estremi relativi di $f.$ Determinare i
valori dei limiti agli estremi del dominio e i valori di lim
$x\to \pm \infty$
f(x)
$x$ e lim
$x\to \pm \infty$ (f(x) $-x).$ Scrivere le equazioni
degli asintoti di $f.$

Lo zero della funzione è $x= 4.$

Insieme immagine: Im(f) $= {y\in R: -\infty < y\le -16, 0 \le y< +\infty$ }

Estremi relativi: punto di minimo relativo $x= 4,$ con ordinata $y= 0;$ punto di massimo relativo $x= -4$
con ordinata $y= -16.$

Limiti agli estremi del dominio.

Il dominio della funzione è: $D= {x\in R: -\infty < x< 0 , 0 < x< 2 , 2 < x< +\infty$ }

$$ lim $$
$x\to -\infty$ f(x) $= -\infty$ ; lim
$x\to 0-f(x) = -\infty$ ; lim
$x\to 0+$ f(x) $= +\infty$ ; lim
$x\to 2$ f(x) $= 2$ ; lim
$x\to +\infty$ f(x) $= +\infty$

Osserviamo che la funzione ha l’asintoto obliquo $y= x-8 = mx+ q,$ quindi:

Sessione suppletiva 2024 - Problema $1$

$$ lim $$
$x\to \pm \infty$
f(x)
$= m= 1$ ; lim
$x\to \pm \infty$ (f(x) $-x) = q= -8$ .

Asintoti:
Asintoto verticale: $x= 0;$ asintoto obliquo: $y= x-8$ (per $x\to \pm \infty$ ); non ci sono asintoti orizzontali.

Osserviamo che la funzione ha in $x= 2$ un punto di discontinuità eliminabile (terza specie).

$b)$

Supponendo che la funzione $f$ abbia equazione

$y= a(x-b)2(x-c)$
$x(x-d)$

determinare i valori dei parametri a, $b, c, d.$

Deve essere $d= 2,$ perché la funzione non è definita per $x= 0$ e $x= 2.$

Siccome il grafico della funzione è tangente all’asse delle $x$ in $x= 4,$ tale valore deve essere una radice
“multipla”, quindi annulla $(x-b)2,$ pertanto $b= 4.$

Deve essere lim
$x\to \pm \infty$
f(x)
$= m= 1$ ed è lim
$x\to \pm \infty$
f(x)
= lim
$x\to \pm \infty$
$$ ax3 $$
$x^{3} = a= 1$

Quindi la funzione ha equazione del tipo:
$y=$
$(x-4)^{2}(x-c)$
$x(x-2)$

Siccome $x= 2$ è un punto discontinuità eliminabile e dal grafico si evince che lim
$x\to 2$ f(x) $= 2,$ dovrà
essere:
$$ lim $$
$x\to 2$
$(x-4)^{2}(x-c)$
$x(x-2)$
$= 2$ solo se $c= 2$

I valori richiesti sono quindi: $a= 1, b= 4, c= 2, d= 2$

La funzione ha pertanto equazione:

$y=$ f(x) =
$(x-4)^{2}(x-2)$
$x(x-2)$
, come dire f(x) =
$(x-4)^{2}$
con $x\ne 2,$ il cui grafico è (come indicato nei dati):

Sessione suppletiva 2024 - Problema $1$

Il grafico $\gamma$ è quello dell’iperbole di equazione $y=$
$(x-4)^{2}$
privata del punto di coordinate $(2; 2).$

$c)$

Dal grafico $\gamma$ , dedurre i grafici delle funzioni $f(|x|)$ e ln(f(x)) specificando, per ciascuna, dominio,
asintoti, estremi e insieme immagine.

Il grafico della funzione $f(|x|)$ si ottiene da quello di f(x) confermando la parte con $x> 0$ e ribaltando tale parte
rispetto all’asse delle ordinate. Abbiamo quindi il seguente grafico (primo e seguente quadrante):

Dominio di $f(|x|): -\infty < x< +\infty$ con $x\ne \pm 2$ e $x\ne 0$

Asintoti: $y= x-8$ per $x\to +\infty$ e $y= -x-8$ per $x\to -\infty$ e $x= 0$ destro e sinistro.

Estremi: estremo inferiore $y= 0$ (che è minimo assoluto), estremo superiore $y= +\infty$

$(x= \pm 4$ punti di minimo relativo ed assoluto,
con ordinata $y= 0)$

Insieme immagine: $Im(f(|x|) = {y\in R: 0 \le y< +\infty$ }

Grafico di $y=$ ln(f(x))

Dominio: f(x) $> 0,$ quindi: $0 < x< 2, 2 < x< 4, 4 < x< +\infty$

Intersezioni con gli assi cartesiani

Non ci sono intersezioni con l’asse delle $y,$ poiché per $x= 0$ la funzione non esiste
Risulta $y= 0$ se f(x) $= 1:$ in un punto compreso fra $2$ e $4$ ed in un punto fra $6$ e $8.$

Limiti agli estremi del dominio

$$ lim $$
$x\to 0+ ln$ (f(x)) $= +\infty$ ,
$$ lim $$
$x\to 2 ln$ (f(x)) $= ln (2)$ ,
$$ lim $$
$x\to 4$ ln(f(x)) $= -\infty$ ,
$$ lim $$
$x\to +\infty$ ln(f(x)) $= +\infty$

Abbiamo quindi gli asintoti verticali di equazioni: $x= 0 , x= 4$ . Non ci sono asintoti orizzontali.

Controlliamo se per $x\to +\infty$ può esserci asintoto obliquo.

Sessione suppletiva 2024 - Problema $1$

$$ lim $$
$x\to +\infty$
ln(f(x))
= lim
$x\to +\infty$
$ln (x-4)^{2}$
$$ x x $$
= lim
$x\to +\infty$
$ln x$
$x= 0+$

$(ln x$ è infinito di ordine inferiore rispetto a $x$ per $x\to +\infty$ : non ci sono asintoti obliqui).

Monotonia

La funzione $ln$ (f(x)) decresce dove decresce f(x): $0 < x< 2, 2 < x< 4$ e cresce dove f(x) cresce:
$x> 4.$ Non ci sono massimi e minimi relativi.

Il grafico di $ln$ (f(x)) è quindi del tipo:

Dominio: $0 < x< 2, 2 < x< 4, 4 < x< +\infty$

Asintoti: $x= 0$ per $x\to 0+, x= 4$ per $x\to 4\pm$

Estremi: estremo inferiore $y= -\infty$ , estremo superiore $x= +\infty$ . Non ci sono minimi locali né massimi locali.

Insieme immagine: Im(ln (f(x)) $= {y\in R: -\infty < y< +\infty$ }

$d)$

Si consideri la funzione F(x) $= \int$ f(t)
$$ x 3 $$
$dt,$ definita nell’intervallo $[3;8].$ Tracciare un suo grafico
rappresentativo $\Gamma$ , specificando l’ascissa del punto di flesso e il coefficiente angolare della retta
tangente in tale punto.

Ricordiamo che la funzione $y=$ f(x) è continua nell’intervallo chiuso e limitato $[3; 8].$
Per il “Teorema fondamentale del calcolo integrale” la funzione $y=$ F(x) è continua e derivabile in $[3; 8]$
ed in ogni punto di tale intervallo si ha: F'(x) = f(x).

Dominio di $F: [3; 8]$

Valori agli estremi del dominio: $F(3) = \int$ f(t)
$$ 3 3 $$
$dt= 0; F(8) = \int$ f(t)
$$ 8 3 $$
$dt.$ Per trovare $F(8)$ occorre
quindi cercare una primitiva di f(x).

Sessione suppletiva 2024 - Problema $1$

$\int$ f(x) $dx= \int (x-4)^{2}$
$dx= \int$
$x^{2} -8x+ 16$
$dx= \int (x-8 + 16$
$x) dx= 1$
$2 x^{2} -8x+ 16 ln|x| + k$

Quindi:

$F(8) = \int$ f(t)
$$ 8 3 $$
$dt=$ [
$2 x^{2} -8x+ 16 ln|x|]$
$$ 3 8 $$
$= 32 -64 + 16 ln 8 -9$
$2 + 24 -16 ln 3 =$
$= -25$
$2 + 16 ln 8$
$3 \cong 3.2$

Monotonia di $F$ (basta osservare il segno di $f$ in $[3; 8],$ essendo $F' = f):$

F'(x) = f(x) $\ge 0$ se $3 \le x\le 8: F$ è sempre crescente.

Concavità di $F$ (basta osservare la monotonia di $f$ in $[3; 8]),$ essendo F''(x) = f'(x) ):

F''(x) = f'(x) $< 0$ se $3 \le x< 4,$ F''(x) $= 0$ se $x= 4,$ F''(x) $> 0$ se $4 < x\le 8.$

Il grafico $\Gamma$ di $F$ volge quindi la concavità verso il basso se $3 < x< 4$ , verso l’alto se $4 < x< 8,$
ed ha un flesso in $x= 4.$

Coefficiente angolare $m$ della tangente inflessionale:

$m= F'(4) = f(4) = 0:$ il flesso ha quindi tangente orizzontale.

Grafico di $F:$

Giuseppe Scoleri, con la collaborazione di Angela Santamaria
