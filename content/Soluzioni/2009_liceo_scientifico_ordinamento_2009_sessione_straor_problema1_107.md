---
tipo: soluzione
prova_stem: 2009_liceo_scientifico_ordinamento_2009_sessione_straor_problema1_107
pdf: Prova_Maturita_2009.pdf
source: text
title: >-
  Soluzioni —
  2009_liceo_scientifico_ordinamento_2009_sessione_straor_problema1_107
---
# Soluzioni — 2009_liceo_scientifico_ordinamento_2009_sessione_straor_problema1_107

## Problema 1

Liceo Scientifico Ordinamento 2009
Sessione $Straordinaria-$ Problema $1$

ORDINAMENTO 2009 - SESSIONE STRAORDINARIA - PROBLEMA $1$

Si consideri la funzione $f$ definita da: f(x) =
$x+1$
$e3x$ .

Si studi $f$ e se ne tracci il grafico $\gamma$ , su un piano riferito $ad$ un sistema di assi cartesiani
ortogonali Oxy.

Dominio:

La funzione è definita su tutto $R.$

Intersezioni con gli assi:

Se $x=0: y=1$ ; se $y=0: x=-1:$ la funzione non può essere pari né dispari.

Positività:

La funzione è positiva se $x>-1.$

Limiti:

$$ lim $$
$x\to -\infty$
$x+ 1$
$e3x= -\infty$ ; lim
$x\to +\infty$
$x+ 1$
$e3x= 0+; (e3x$ domina rispetto $ad x)$

Asintoti:

La funzione è continua su tutto $R:$ non ci sono asintoti verticali.
Dal precedente calcolo dei limiti si deduce che c’è l’asintoto orizzontale $y=0$ per $x\to +\infty$
Vediamo se per caso c’è asintoto obliquo al meno infinito:

$$ lim $$
$x\to -\infty$
f(x)
= lim
$x\to -\infty$
$x+1$
$x e3x=$ lim
$x\to -\infty$
$xe3x=$ lim
$x\to -\infty$
$e3x= +\infty$ : non c’è asintoto obliquo.

Derivata prima:

f'(x) =
$-3x-2$
$e3x$
$\ge 0$ se $x\le -$
$3$ : la funzione è crescente se $x< -$
$3$ e decrescente se
$x> -$
$3 ; x= -$
$3$ è punto di massimo relativo (e assoluto), con ordinata
$f(-$
$$ 2 3) = 1 3 $$
$e-2 =$
$$ e^{2} 3 . $$

Liceo Scientifico Ordinamento 2009
Sessione $Straordinaria-$ Problema $1$

Derivata seconda:

f''(x) =
$9x+3$
$e3x\ge 0$ se $x\ge -$
$3$ . Concavità verso l’alto se $x> -$
$3$ , verso il basso se
$x< -$
$3 ; x= -$
$3$ punto di flesso, con ordinata $f(-$
$$ 1 3) = 2 3 $$
$e-1 =$
$2e$

Grafico della funzione:

Si scriva l’equazione della tangente a $\gamma$ nel punto di flesso e si calcoli l’area del triangolo
che essa forma con gli assi cartesiani.

$F= (-1$
$3 ; 2e$
$3 ) ; f' (-1$
$3) = -e ; y-2e$
$3 = -e(x+ 1$

L’equazione della tangente nel punto di flesso è:

Liceo Scientifico Ordinamento 2009
Sessione $Straordinaria-$ Problema $1$

$y= -ex+ 1$
$3$ e

Cerchiamo le intersezioni della tangente inflessionale con gli assi cartesiani:

Se $x= 0, y=$
$3$ e; Se $y= 0, x=$
$$ 1 3 $$

Area(OAB) $= 1$
$2 ∙1$
$3 ∙1$
$3 e= 1$
$18 e\cong 0.15 u^{2}$

Si provi che la funzione F(x) = -
$9 e-3x(3x+ 4)$ è una primitiva della funzione $f (x).$

Deve essere: F'(x) = f(x).

F'(x) $= 1$
$3 e-3x(3x+ 4) -1$
$9 e-3x(3) = e-3x(x+ 4$
$3 -1$
$3) = e-3x(x+ 1) = x+ 1$
$e3x=$ f(x).

Si calcoli l’area A(k) della superficie piana, delimitata dalla curva $\gamma$ , dall’asse $x$ e dalle
rette $x= -1$ e $x = k$ con $k > 0.$ Cosa si può dire di A(k) quando $k\to +\infty$ ?

L’area richiesta si ottiene calcolando il seguente integrale:

Liceo Scientifico Ordinamento 2009
Sessione $Straordinaria-$ Problema $1$

$Area= \int x+ 1$
$e3x dx$
$-1$
$= \int (x+ 1)e-3x dx$
$-1$

Cerchiamo una primitiva di $(x+ 1)e-3x$ integrando per parti:

$f= x+ 1,$
$f' = 1 , g' = e-3x , g= -1$
$3 e-3x$

$\int (x+ 1)e-3x dx= (x+ 1) ∙(-1$
$3 e-3x) -\int 1 ∙(-1$
$3 e-3x) dx= -1$
$3 e-3x(x+ 1) -1$
$9 e-3x$

$= -3x+ 4$
$9e3x$

Quindi:

$\int (x+ 1)e-3x dx$
$-1$
$= [-3x+ 4$
$9e3x]$
$-1$
$= -3k+ 4$
$9e3k+$
$9e-3 =$ A(k)

$$ lim $$
$k\to +\infty$ A(k) = lim
$k\to +\infty (-3k+ 4$
$9e3k+$
$9e-3) =$
$9e-3 = e^{3}$
$9 \cong 2.23 u^{2}$

Con la collaborazione di Angela Santamaria
