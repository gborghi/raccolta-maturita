---
tipo: soluzione
prova_stem: 2005_scuole_italiane_allestero_americhe_2005_sessione_s_problema1_29
pdf: Prova_Maturita_2005.pdf
source: text
title: >-
  Soluzioni —
  2005_scuole_italiane_allestero_americhe_2005_sessione_s_problema1_29
---
# Soluzioni — 2005_scuole_italiane_allestero_americhe_2005_sessione_s_problema1_29

## Problema 1

Scuole italiane all’estero (Americhe) 2005
Sessione $Suppletiva-$ Problema $1$

Scuole italiane all’estero (Americhe) 2005 - Suppletiva

PROBLEMA $1$

In un piano, riferito $ad$ un sistema di assi cartesiani ortogonali Oxy, sono assegnate le
curve di equazione
$(1) y=$
$3 x^{3} + kx-3,$
dove $k$ è un parametro reale.

a)

Dimostrare che tutte le curve $(1)$ passano per uno stesso punto A che per ciascuna di
esse è punto di flesso e centro di simmetria.

Si tratta di un fascio di cubiche che hanno una generatrice di equazione $x=0.$ Per $x=0$
otteniamo $y=-3.$ Il punto $A=(0; -3)$ appartiene a tutte le curve.
Dimostriamo che A è il punto di flesso per tutte le curve.
Ricordiamo che tutte le cubiche hanno uno ed un solo flesso, che è centro di simmetria.
Il flesso, i questo caso, si ottiene annullando la derivata seconda:
$y' = x^{2} + k,$ y" $= 2x= 0$ se $x= 0:$ A è quindi flesso per tutte le curve.
Verifichiamo direttamente che A è centro di simmetria per ogni $k.$
Le equazioni della simmetria di centro (a; $b)$ sono:

{x' $= 2a-x$
$y' = 2b-y$ ; se (a; $b)=(0; -3)$ abbiamo: {
$x' = -x$
$y' = -6 -y$ da cui: {
$x= -x'$
$y= -6 -y'$

La curva di equazione $y=$
$3 x^{3} + kx-3$ diventa quindi: $-6 -y' = -$
$3 (x')^{3} -kx-3,$

$y' =$
$3 (x')^{3} + kx-3,$ che coincide con la curva di partenza, quindi A è centro di
simmetria per tutte le curve.

$b)$

Dimostrare inoltre che tutte le curve $(1)$ hanno un massimo e un minimo relativi oppure
non hanno né l’uno né l’altro.

Studiamo la derivata prima della $(1): y' = x^{2} + k.$ Essendo la $(1)$ derivabile in ogni punto,
gli eventuali massimi e minimi relativi sono a tangente orizzontale, quindi annullano la
derivata prima, perciò si le curve $(1)$ hanno un massimo e un minimo relativi dove:
$x^{2} + k= 0,$ da cui (solo se $k\le 0), x= \pm \sqrt -k.$ In particolare se $k<0$ abbiamo un

Scuole italiane all’estero (Americhe) 2005
Sessione $Suppletiva-$ Problema $1$

massimo ed un minimo relativo (la derivata prima è positiva per $x< -\sqrt -k$ vel $x> \sqrt -k),$
se $k=0,$ essendo $x^{2} + k> 0$ per ogni $x$ diverso da $0,$ la funzione è sempre crescente ed
ha in $x=0$ un flesso a tangente orizzontale; se $k>0$ non abbiamo né un massimo relativo
né un minimo relativo, essendo la derivata prima sempre positiva, quindi la funzione è
sempre crescente (per $x=0$ abbiamo un flesso a tangente obliqua).

Diamo degli esempi grafici per $k>0, k=0, k<0:$

$c)$

Trovare a quale valore di $k$ corrisponde una curva $(1)$ tangente all’asse $x.$

La curva è tangente all’asse $x$ se il massimo appartiene a tale asse (si osservino i grafici
precedenti), quindi se
$3 x^{3} + kx-3 = 0$ quando $x= -\sqrt -k,$ perciò:
$3 (-\sqrt -k)$
$3 -k\sqrt -k-3 = 0,$
$-1$
$3 \sqrt -k^{3} -k \sqrt -k= 3,$
$-1$
$3 |k|\sqrt -k -k \sqrt -k-3 = 0$
Essendo $k<0:$

$3 k\sqrt -k -k \sqrt -k-3 = 0,$
$-2$
$3 k\sqrt -k= 3,$
$-4$
$9 k^{3} = 9,$
$k^{3} = -81$

$k= -\sqrt{81}$
$$ 4 3 $$

La curva ha in tal caso equazione: $y=$
$3 x^{3} -\sqrt$
$$ 81 4 3 $$
$x-3,$ ed il suo grafico è:

Scuole italiane all’estero (Americhe) 2005
Sessione $Suppletiva-$ Problema $1$

$d)$

Indicare con $\gamma$ quella, tra le curve $(1),$ la cui tangente in A individua con gli assi coordinati
un triangolo di area $9/4$ e, nel medesimo tempo, presenta un massimo e un minimo
relativi.

In base alle informazioni deve essere $k<0,$ quindi la situazione grafica è la seguente:

Ricordiamo che $A= (0; -3), y=$
$3 x^{3} + kx-3$ e $y' = x^{2} + k,$ da cui $y'(0) = k$
La tangente in A ha quindi equazione: $y+ 3 = k(x-0), y= kx-3.$ Il punto $B$ ha
coordinate (
$k; 0).$ Il triangolo richiesto, OAB, ha quindi area:

Area(OAB) $= 1$
$2 ∙3 ∙|3$
$k| =$
$-2k= 9$
da cui: $k= -2$

La curva $\gamma$ ha quindi equazione: $y=$
$3 x^{3} -2x-3$ .

Scuole italiane all’estero (Americhe) 2005
Sessione $Suppletiva-$ Problema $1$

e)

Fra i rettangoli contenuti nella regione finita di piano delimitata dalla curva $\gamma$ e dagli assi
coordinati e aventi un lato sull’asse $x$ e gli estremi del lato opposto sulla curva $\gamma$ ,
determinare i vertici di quello per il quale questo secondo lato dista $95/24$ dall’asse $x.$

L’ordinata di $P$ e $Q$ è -
$$ 95 $$
$24$ quindi le ascisse si ottengono risolvendo l’equazione:

$3 x^{3} -2x-3 = -$
$$ 95 $$
$24$ da cui: $8x^{3} -48x+ 23 = 0.$

Osservandi la figura proviamo con la radice $x=$
$2: 1 -24 + 23 = 0.$
Quindi l’ascissa di $P$ e di $S$ è $x=$
$$ 1 2. $$
Applicando la regola di Ruffini otteniamo: $8x^{3} -48x+ 23 = (x-$
$2) (8x^{2} + 4x-46) = 0$
e risulta:

$8x^{2} + 4x-46 = 0, x=$
$-1-\sqrt{93}$
$\cong -2.7, x=$
$-1+\sqrt{93}$
$\cong 2.2.$

Quindi l’ascissa di $Q$ e di $R$ è: $x=$
$-1+\sqrt{93}$

I vertici del rettangolo sono quindi:

$P=$ (
$2 ; -$
$$ 95 $$
$24) , Q=$ (
$-1+\sqrt{93}$
; -
$$ 95 $$
$24) , R=$ (
$-1+\sqrt{93}$
; $0) , S=$ (
$$ 1 2 ; 0). $$

Con la collaborazione di Angela Santamaria
