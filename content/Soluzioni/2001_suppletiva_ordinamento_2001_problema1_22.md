---
tipo: soluzione
prova_stem: 2001_suppletiva_ordinamento_2001_problema1_22
pdf: Prova_Maturita_2001.pdf
source: text
title: Soluzioni — 2001_suppletiva_ordinamento_2001_problema1_22
---
# Soluzioni — 2001_suppletiva_ordinamento_2001_problema1_22

## Problema 1

Suppletiva Ordinamento 2001 -
Problema $1$

ORDINAMENTO 2001 - SESSIONE SUPPLETIVA - PROBLEMA $1$

Si consideri la funzione reale $fm$ di variabile reale $x$ tale che:

$fm=$
$$ x^{2 $$
$|x-2m| + m$

dove $m$ è un parametro reale non nullo.

a)

Trovare gli insiemi di definizione, di continuità e di derivabilità della funzione.

Insieme di definizione:

$|x-2m| + m\ne 0 , |x-2m| \ne -m:sempre$ verificato se $m> 0$ ;

se $m< 0: x-2m\ne \pm m$ , da cui:

$x-2m\ne m , x\ne 3m (m$ negativo)

$x-2m\ne -m , x\ne m (m$ negativo)

Quindi l’insieme di definizione è il seguente:

Se $m>0:$ tutto $R$
Se $m<0: x\ne 3m$ e $x\ne m$

Insieme di continuità:

Trattandosi una funzione razionale fratta l’insieme di continuità coincide con l’insieme di
definizione. Quindi:

Se $m>0:$ la funzione è continua su tutto $R$
Se $m<0:$ la funzione è continua per ogni $x$ diverso da $m$ e da $3m$ (in cui ci sono delle
discontinuità di seconda specie, essendo i limiti infiniti)

Insieme di derivabilità:

Riscriviamo la funzione nella seguente forma:

Suppletiva Ordinamento 2001 -
Problema $1$

f(x) =

$$ x^{2 $$
$x-2m+ m=$
$$ x^{2 $$
$x-m$ se $x\ge 2m$ e $x\ne m$
$$ x^{2 $$
$-x+ 2m+ m=$
$$ x^{2 $$
$-x+ 3m$ se $x< 2m$ e $x\ne 3m$

Se $x\ne 2m$ (con $x\ne m$ e $x\ne 3m$ quando $m< 0)$ la funzione è sempre derivabile.
Se $x= 2m$ calcoliamo la derivata destra e sinistra:

Se $x>2m,$ f(x) =
$$ x^{2 $$
$x-m$ , quindi f'(x) =
$2x(x-m)-x^{2}$
$(x-m)2$
$x^{2}-2mx$
$(x-m)2 \to 0$ se $x\to (2m)+$

Se $x<2m,$ f(x) =
$$ x^{2 $$
$-x+3m$ , quindi f'(x) =
$2x(-x+3m)+x^{2}$
$(-x+3m)2$
$-x^{2}+6mx$
$(-x+3m)2 \to 8$ se $x\to (2m)-$

Quindi la funzione non è derivabile se $x=2m.$ Riepilogando:

Se $m>0:$ la funzione è derivabile per ogni $x\ne 2m$ .
Se $m<0:$ la funzione è derivabile per ogni $x\ne m, 2m, 3m$ .

$b)$

Indicata con $C^{1}$ la curva rappresentativa della funzione $f^{1}(x)$ corrispondente $ad$
$m= 1$ , studiarla e disegnarla in un piano riferito a un sistema di assi cartesiani
ortogonali, dopo aver determinato, in particolare, le equazioni dei suoi asintoti e il
comportamento nel punto A di ascissa $2.$

Per $m=1$ la funzione ha equazione:

$y=$ f(x) =
$$ x^{2 $$
$|x-2| + 1 =$

$$ x^{2 $$
$x-1$ se $x\ge 2$
$$ x^{2 $$
$-x+ 3$ se $x< 2$

La funzione è definita su tutto $R.$

Simmetrie notevoli: la funzione non è pari né dispari

Intersezioni con gli assi:
Se $x=0, y=$
$$ x^{2 $$
$-x+3 = 0$
Se $y=0, x=0$

Segno della funzione:
il numeratore non è mai negativo (si annulla solo per $x=0),$ il denominatore è sempre
positivo, quindi la funzione non è mai negativa e si annulla solo per $x=0.$

Suppletiva Ordinamento 2001 -
Problema $1$

Limiti:

$limx\to -\infty$ f(x) = lim
$x\to -\infty$
$$ x^{2 $$
$-x+3 = +\infty ; limx\to +\infty$ f(x) = lim
$x\to -\infty$
$$ x^{2 $$
$x-1 = +\infty$

Asintoti obliqui:

Per $x\to -\infty$ risulta f(x) =
$$ x^{2 $$
$-x+3$ ~ $-x$ ; quindi $m=-1.$

$q=$ lim
$x\to -\infty$ [f(x) $-mx] =$ lim
$x\to -\infty$ [
$$ x^{2 $$
$-x+ 3 +$ x] = lim
$x\to -\infty$ (
$3x$
$-x+ 3) = -3$

Quindi per $x\to -\infty$ abbiamo l’asintoto: $y= -x-3$

Per $x\to +\infty$ risulta f(x) =
$$ x^{2 $$
$x-1$ ~x ; quindi $m=1$

$q=$ lim
$x\to +\infty$ [f(x) $-mx] =$ lim
$x\to +\infty$ [ $x^{2}$
$x-1 -x] =$ lim
$x\to +\infty$ (
$x-1) = 1$

Quindi per $x\to +\infty$ abbiamo l’asintoto: $y= x+ 1$

Derivata prima:

f'(x) =
$x^{2} -2x$
$(x -1)^{2}$ se $x> 2$
$-x^{2} + 6x$
$(x-3)^{2}$ se $x< 2$

Se $x\to 2-$ f'(x) $\to 0$
Se $x\to 2+$ f'(x) $\to 8$

Quindi in $A=(2;4)$ abbiamo un punto angoloso (la semi tangente destra ha $m=8,$ la sinistra
$m=0).$

Studiamo il segno della derivata prima.
Se $x> 2$ f'(x) $\ge 0$ se
$x^{2}-2x$
$(x-1)^{2} \ge 0 \implies x^{2} -x\ge 0: x\le 0$ vel $x\ge 1$
Quindi se $x> 2$ la funzione è sempre crescente.

Se $x< 2$ f'(x) $\ge 0$ se
$-x^{2}+6x$
$(x-3)^{2} \ge 0 \implies -x^{2} + 6x\ge 0 , x^{2} -6x\le 0, 0 \le x\le 6$

Quindi se $x< 2$ la funzione è crescente per $0 < x< 2$ e decrescente per $x< 0$ ; quindi
$x=0$ è punto di minimo relativo (e assoluto).

Suppletiva Ordinamento 2001 -
Problema $1$

Derivata seconda:

f''(x) =

$(x -1)^{3}$ se $x> 2$
-
$$ 18 $$
$(x -3)^{3}$ se $x< 2$

Studiamo il segno della derivata seconda.

Se $x> 2$ f''(x) $\ge 0$ se
$(x-1)^{3} \ge 0$ : sempre se $x> 2$ : la concavità è sempre rivolta
verso l’alto se $x>2.$

Se $x< 2$ f''(x) $\ge 0$ se
$-18$
$(x-3)^{3} \ge 0 , x-3 < 0$ (denominatore sempre $<0),$ quindi
$x< 3$ : sempre se $x< 2$ : la concavità è sempre rivolta verso l’alto se $x<2.$

Il grafico della funzione è il seguente:

$c)$

Calcolare l’area della regione finita di piano delimitata dalla curva $C^{1}$ e dalla retta
parallela all’asse delle ascisse condotta per il punto A.

La retta parallela all’asse $x$ condotta da A ha equazione $y=4.$

La regione richiesta è indicata nella figura seguente:

Suppletiva Ordinamento 2001 -
Problema $1$

Cerchiamo l’ascissa del punto $B:$

$$ x^{2 $$
$-x+3 = 4 , x^{2} + 4x-12 = 0 , x= 2$ e $x= -6$ : quindi $B$ ha ascissa $- 6.$

L’area richiesta si ottiene calcolando il seguente integrale:

$\int (4 -$
$$ x^{2 $$
$-x+ 3)$
$-6$
$dx= \int (x^{2} + 4x-12$
$x-3$
$-6$
$$ dx $$

Effettuando la divisione tra il numeratore ed il denominatore otteniamo:

$x^{2} + 4x-12 = (x-3)(x+ 7) + 9$ , quindi:

$\int (x^{2} + 4x-12$
$x-3$
$-6$
$dx= \int ((x-3)(x+ 7) + 9$
$x-3$
$-6$
$dx= \int (x+ 7 +$
$x-3)$
$-6$
$dx=$

$= [x^{2}$
$2 + 7x+ 9ln|x-3|]$
$-6$
$= 2 + 14 -(18 -42 + 9ln9) = (40 -9ln9) u^{2} \cong 20.22 u^{2}$

L’area richiesta vale $(40 -9ln9) u^{2} \cong 20.22 u^{2}$ .

Con la collaborazione di Angela Santamaria
