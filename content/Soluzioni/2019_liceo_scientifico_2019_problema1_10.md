---
tipo: soluzione
prova_stem: 2019_liceo_scientifico_2019_problema1_10
pdf: Prova_Maturita_2019.pdf
source: text
title: 2019 Liceo Scientifico — Problema 1 — Svolgimento
---


Liceo Scientifico 2019 - Problema $1$

LICEO SCIENTIFICO 2019 - PROBLEMA $1$

a)

Dimostriamo che, per ogni a e $b$ reali, con a non nullo, la funzione $g$ ammette massimo e minimo assoluti.
La funzione $g$ è definita e continua su tutto $R$ e risulta:

$$ lim $$
$x\to \pm \infty$ g(x) = lim
$x\to \pm \infty ax e-x^{2} = 0$ (Ricordiamo che lim
$x\to -\infty xa ebx= 0$ per ogni a e $b$ positivi).

Studiamo la derivata prima di g(x):

g'(x) = a $e2x-x^{2} + (ax+ b) e2x-x^{2} (2 -2x) = e2x-x^{2} (-2ax2 + 2x(a-b) + 2a)$

g'(x) $\ge 0: -2ax2 + 2x(a-b) + 2b+ a\ge 0 , 2ax2 -2x(a-b) -2b-a\le 0$

$\Delta$
$4 = (a+ b)2 + 2a^{2} > 0 \forall a\ne 0$ e $\forall b\in R$
$x1,2 =$
$a-b\mp \sqrt \Delta$
$2a$
, $x^{1} =$
$a-b-\sqrt \Delta$
$2a$
$x^{2} =$
$a-b+ \sqrt \Delta$
$2a$

Se $a> 0$ risulta $x^{1} < x^{2}$ e risulta g'(x) $\ge 0$ se $x^{1} \le x\le x^{2}$ :

$g$ decresce per $x< x^{1},$ cresce per $x^{1} < x< x^{2}$ , decresce per $x> x^{2}:$ quindi, ricordando quanto detto
sui limiti per $x\to \pm \infty$ , possiamo concludere che $x^{1}$ e punto di minimo assoluto e $x= x^{2}$ è punto di
massimo assoluto.

Se $a< 0$ risulta $x^{1} > x^{2}$ e risulta g'(x) $\ge 0$ se $x\le x^{2}$ vel $x\ge x^{1}$ :

$g$ cresce per $x< x^{2},$ decresce per $x^{2} < x< x^{1}$ , cresce per $x> x^{1}:$ quindi, ricordando quanto detto
sui limiti per $x\to \pm \infty$ , possiamo concludere che $x^{1}$ e punto di minimo assoluto e $x^{2}$ è punto di
massimo assoluto.

Determiniamo ora a e $b$ in modo che i grafici di $f$ e $g$ si intersechino in $A(2; 1).$
Imponiamo che i grafici di $f$ e $g$ passino per A:

${1 = 4a-2 + b$
$(2a+ b)e^{0} = 1$ ; ... { $a= 1$
$b= -1$

Liceo Scientifico 2019 - Problema $1$

$b)$

Studiamo le due funzioni con $a= 1$ e $b= - 1$ :

$y=$ f(x) $= x^{2}-x-1$

Si tratta di una parabola con asse parallelo all’asse $y,$ con vertice $V=$ (
$2 ; -$
$4),$ che taglia l’asse $y$ in
$(0; - 1)$ e l’asse $x$ in $x=$
$1\pm \sqrt{5}$
$2$ . Il suo grafico è quindi il seguente:

Studiamo ora la seconda funzione:

$y=$ g(x) $= (x-1) e2x-x^{2}$

Dominio: tutto $R.$
Intersezioni con gli assi: se $x=0, y = -1;$ se $y=0, x=1.$
Segno: $y> 0$ se $x> 1$
Limiti:

$$ lim $$
$x\to \pm \infty$ g(x) = lim
$x\to \pm \infty x e-x^{2} = 0\pm (y=0$ asintoto per $x\to \pm \infty$ ).

Studio derivata prima:
ponendo $a=1$ e $b=-1$ nella derivata calcolata con a e $b$ generici si ha:

g'(x) $= e2x-x^{2} (-2x^{2} + 4x-1);$ g'(x) $\ge 0$ se $-2x^{2} + 4x-1 \ge 0 : 2 -\sqrt{2}$
$\le x\le 2 -\sqrt{2}$

Essendo $a>0,$ come studiato precedentemente abbiamo: $x=$
$2-\sqrt{2}$
$2$ punto di minimo assoluto, di ordinata
$- \sqrt$
$2$ ed $x=$
$$ 2+\sqrt{2 $$
$2$ punto di massimo assoluto, di ordinata $\sqrt$
$$ e 2 . $$
Studio derivata seconda:

g''(x) $= ⋯= 2(x-1) e2x-x^{2}(2x^{2} -4x-1) \ge 0$ se $(x-1)(2x^{2} -4x-1) \ge 0$ e studiando il segno
dei due fattori si ottiene:

Liceo Scientifico 2019 - Problema $1$

g''(x) $\ge 0$ per $1 -\sqrt{3}$
$2 \le x\le 1$ vel $x\ge 1 + \sqrt{3}$
$2$ , quindi il grafico di $g$ ha la concavità verso l’alto per
$1 -\sqrt{3}$
$2 < x< 1$ e $x> 1 + \sqrt{3}$
$2$ e verso il basso per $x< 1 -\sqrt{3}$
$2$ e $1 < x< 1 + \sqrt{3}$
$2$ , pertanto abbiamo
tre flessi per $x= 1 -\sqrt{3}$
$2 , x= 1, x= 1 + \sqrt{3}$
$2$ ( le ordinate sono rispettivamente $\cong -0.7, 1$ e $\cong 0.7).$
Il grafico della $g$ è il seguente:

Dimostriamo ora che la $g$ è simmetrica rispetto $ad F^{2} = (1; 0).$
Le equazioni della simmetria rispetto al punto di coordinate (a; $b)$ sono:

{x' $= 2a-x$
$y' = 2b-y$ ; quindi nel nostro caso: {x' $= 2 -x$
$y' = -y$ ,
da cui: ${x= 2 -x'$
$y= -y'$
$y=$ g(x) $= (x-1) e2x-x^{2},$ trascurando gli apici, diventa: $-y= (2 -x-1)e^{2}(2-x)-(2-x)2$ , da cui,
eseguendo i calcoli:
$y= (x-1) e2x-x^{2} =$ g(x) : e ciò dimostra che la $g$ è simmetrica rispetto $ad F^{2} = (1; 0).$

Dimostriamo che i grafici di $f$ e $g$ sono tangenti in $B=(0; - 1).$ Abbiamo già visto che entrambi i grafici
passano per $B,$ resta da verificare che $f'(0) = g'(0).$

f'(x) $= 2x-1,$
quindi: $f'(0) = -1$ ; g'(x) $= e2x-x^{2} (-2x^{2} + 4x-1)$ ,
quindi $g'(0) = -1.$

La tangente comune $ai$ due grafici ha equazione: $y= -x-1$ .

Per calcolare l’area della regione $S$ delimitata dai grafici di $f$ e $g$ rappresentiamoli nello stesso piano
cartesiano:

Liceo Scientifico 2019 - Problema $1$

L’area richiesta si ottiene calcolando il seguente integrale:
Area(S) $= \int$ [g(x) $-f(x)]$
$$ 2 0 $$
$dx= \int [(x-1) e2x-x^{2} -(x^{2} -x-1)]$
$$ 2 0 dx $$
Cerchiamo una primitiva di g(x):
$\int (x-1) e2x-x^{2} dx= -1$
$2 \int (2 -2x) e2x-x^{2} dx= -1$
$2 e2x-x^{2}$
Cerchiamo una primitiva di f(x):
$\int (x^{2} -x-1) dx= 1$
$3 x^{3} -1$
$2 x-x$
Quindi:
Area(S) $= \int [(x-1) e2x-x^{2} -(x^{2} -x-1)]$
$$ 2 0 $$
$dx= [-1$
$2 e2x-x^{2} -(1$
$3 x^{3} -1$
$2 x-x)]$
$$ 0 2 $$
$= ⋯= 4$
$3 u^{2}$
Area(S) $= 4$
$3 u^{2} \cong 1.33 u^{2}$

$c)$

Rappresentiamo i tre punti nel piano cartesiano insieme alla regione $S:$

Liceo Scientifico 2019 - Problema $1$

Chiaramente $P^{3}$ è esterno $ad S$ (quindi non contribuisce alla circuitazione); $P^{1}$ è visibilmente interno $ad S.$
Verifichiamo la posizione di $P^{2},$ confrontando la sua ordinata con l’ordinata del punto di g(x) che ha la
stessa ascissa $1: y(P^{2}) = 1, g(xP2) = g($
$$ 3 2) = ( 3 $$
$2 -1) e^{3}-9$
$$ 4 = 1 $$
$2$ e
$4 \cong 1.06:$
quindi $P^{2}$ è interno $ad S.$
Supponiamo che il contorno di $S$ sia orientato in senso orario (nel caso contrario cambia il segno dei
risultati). Detta $C$ la circuitazione del campo totale, risulta:
C(B⃗⃗ ) $= \mu 0 i^{1}+ \mu 0 i^{2}$ (dove le intensità di corrente sono intese in senso algebrico). Si ha:
C(B⃗ ) $= \mu 0(2 A+ i^{2}) = f(i^{2})$ . Notiamo che $C$ non dipende da $i^{3}.$ Studiamo la variazione di $C$ al variare di
$i^{2}$ .
Se $i^{2}$ ha lo stesso segno di $i^{1}$ (positivo con il verso scelto per il contorno di $S),$ risulta:
C(B⃗ ) $= \mu 0(2 A+ i^{2}) > 0.$
Se $i^{2}$ ha stesso segno opposto a quello di $i^{1},$ quindi segno negativo, si hanno i seguenti casi:

•
Se $i^{2} = -2$ A: C(B⃗ ) $= \mu 0(2 A-2$ A) $= 0.$
•
Se $i^{2} < -2$ A: C(B⃗ ) $= \mu 0(2 A+ i^{2}) < 0.$
•
Se $-2 A< i^{2} < 0$ A: C(B⃗ ) $= \mu 0(2 A+ i^{2}) > 0.$
•
Se $i^{2} = 0$ A: C(B⃗ ) $= \mu 0(2$ A) $> 0.$

Se orientiamo il contorno di $S$ in senso antiorario si avranno risultati opposti.

$d)$

$R= 0.20 \Omega$ ,
$B = 1.5 ∙10-2 T,$
i(massima) $= 5.0 mA= 5,0 ∙10-3$ A

Se $\omega$ è costante, l’angolo $\alpha$ fra la normale $ad S$ e B⃗ è dato da: $\alpha = \omega t$ . Il flusso di B⃗ attraverso $S$ è:

$\Phi$ (B⃗ ) = BScos $\alpha = BScos(\omega t)$

La f.e.m. indotta $f$ (per la legge di $Faraday- Neumann-Lenz)$ è data da:

$f= -$
$d\Phi$ (B⃗ )
$$ dt $$
= -
$dt(BScos(\omega t)) = BS\omega sin(\omega t).$ Inoltre:

$i= f$
$R= BS\omega$
$R sin(\omega t)$ ; i(massima) $= BS\omega$
$= 5,0 ∙10-3$ A ,

$\omega =$
$(5,0∙10-3$ A ) $R$
$$ BS = $$
$(5,0∙10-3$ A ) $(0.20 \Omega$ )
$(1.5 ∙10-2 T)(4$
$3 m^{2}) = 0.05$
$$ rad $$
$s= \omega$ .

Con la collaborazione di Angela Santamaria e Stefano Scoleri
