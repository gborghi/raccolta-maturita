---
tipo: soluzione
prova_stem: 2007_estero_calendario_australe_2007_sessione_ordinaria_questionario_35
pdf: Prova_Maturita_2007.pdf
source: text
title: 2007 Estero Australe Ordinaria — Questionario — Svolgimento
---

## Quesito 1

Estero (Calendario australe) 2007

Scuole italiane all’estero (Calendario australe) 2007 - Quesiti

QUESITO $1$

Si traccino i grafici delle seguenti funzioni di $R$ in $R:$

$f: x\to 3x+1 ; g: x\to 3x+ 1 ; h: x\to 3|x| ; k: x\to 3-x$

Tutti i grafici richiesti sono deducibili dal grafico della funzione $y=$ a(x) $= 3x.$

f(x) $= 3x+1 = a(x+ 1):$ si trasla verso sinistra di $1$ il grafico di a(x)
g(x) $= 3x+ 1 =$ a(x) $+ 1:$ si trasla verso l’alto di $1$ il grafico di a(x)

h(x) $= 3|x| = a(|x|):$ si conferma il grafico di a(x) che si trova a destra dell’asse $y$ e lo si
ribalta rispetto all’asse $y.$

## Quesito 2

Estero (Calendario australe) 2007

k(x) $= 3-x= a(-x):$ si ribalta il grafico di a(x) rispetto all’asse $y.$

QUESITO $2$

Quante cifre ha il numero $559$ nella rappresentazione decimale? Motiva esaurientemente
la risposta.

Osserviamo che $log(559) = 59$ log $(5) \cong 41.24,$ quindi la parte intera di $log(559)$ è $41;$
pertanto $559$ ha $42$ cifre (ricordiamo che la parte intera del logaritmo decimale di un
numero è uguale al numero delle cifre diminuito di $1).$

QUESITO $3$

Si consideri una sfera di volume $V$ e superficie $S.$ Si dimostri che il tasso di variazione di
$V$ rispetto al raggio è uguale a $S.$

Il volume di una sfera di raggio $R$ è $V=$
$3 \pi R^{3};$ la variazione di $V$ rispetto $ad R$ è la
derivata di $V$ rispetto $ad R,$ quindi:

$$ dV $$
$dR= 4\pi R^{2} =$ Superficie sfera

QUESITO $4$

Si illustrino il significato e l’ambito di utilizzo del simbolo $(n$
$m)$ e si risolva l’equazione:

$2 (x$
$2) = 3 (x-1$
) con $x\in N$

## Quesito 3

Estero (Calendario australe) 2007

k(x) $= 3-x= a(-x):$ si ribalta il grafico di a(x) rispetto all’asse $y.$

QUESITO $2$

Quante cifre ha il numero $559$ nella rappresentazione decimale? Motiva esaurientemente
la risposta.

Osserviamo che $log(559) = 59$ log $(5) \cong 41.24,$ quindi la parte intera di $log(559)$ è $41;$
pertanto $559$ ha $42$ cifre (ricordiamo che la parte intera del logaritmo decimale di un
numero è uguale al numero delle cifre diminuito di $1).$

QUESITO $3$

Si consideri una sfera di volume $V$ e superficie $S.$ Si dimostri che il tasso di variazione di
$V$ rispetto al raggio è uguale a $S.$

Il volume di una sfera di raggio $R$ è $V=$
$3 \pi R^{3};$ la variazione di $V$ rispetto $ad R$ è la
derivata di $V$ rispetto $ad R,$ quindi:

$$ dV $$
$dR= 4\pi R^{2} =$ Superficie sfera

QUESITO $4$

Si illustrino il significato e l’ambito di utilizzo del simbolo $(n$
$m)$ e si risolva l’equazione:

$2 (x$
$2) = 3 (x-1$
) con $x\in N$

## Quesito 4

Estero (Calendario australe) 2007

k(x) $= 3-x= a(-x):$ si ribalta il grafico di a(x) rispetto all’asse $y.$

QUESITO $2$

Quante cifre ha il numero $559$ nella rappresentazione decimale? Motiva esaurientemente
la risposta.

Osserviamo che $log(559) = 59$ log $(5) \cong 41.24,$ quindi la parte intera di $log(559)$ è $41;$
pertanto $559$ ha $42$ cifre (ricordiamo che la parte intera del logaritmo decimale di un
numero è uguale al numero delle cifre diminuito di $1).$

QUESITO $3$

Si consideri una sfera di volume $V$ e superficie $S.$ Si dimostri che il tasso di variazione di
$V$ rispetto al raggio è uguale a $S.$

Il volume di una sfera di raggio $R$ è $V=$
$3 \pi R^{3};$ la variazione di $V$ rispetto $ad R$ è la
derivata di $V$ rispetto $ad R,$ quindi:

$$ dV $$
$dR= 4\pi R^{2} =$ Superficie sfera

QUESITO $4$

Si illustrino il significato e l’ambito di utilizzo del simbolo $(n$
$m)$ e si risolva l’equazione:

$2 (x$
$2) = 3 (x-1$
) con $x\in N$

## Quesito 5

Estero (Calendario australe) 2007

$(n$
$m)$ è il simbolo del cosiddetto “coefficiente binomiale” ed il termine deriva dal fatto che
con esso si possono indicare i coefficienti dello sviluppo della potenza del binomio
$(a+$ b)n $= \sum$
$(n$
$m)ambn-m$
$m=0$

Con questo simbolo si indicano anche le “combinazioni semplici di $n$ oggetti $ad m ad$ m”,
date da:

$(n$
$m) = Cn,m=$ Dn,m
$m! = n(n-1) ... (n-m+ 1)$
$m!$
$n!$
$m! (n-m)!$

$(n$ ed $m$ sono numeri naturali, con $n> 0$ ed $n\ge m.$

Risolviamo ora l’equazione data:

$2 (x$
$2) = 3 (x-1$
) con $x\in N$

Deve essere $x\ge 2$ e $x-1 \ge 2:$ quindi $x\ge 3$ e $x$ intero.

$2 ∙x(x-1)$
$2!$
$= 3 ∙(x-1)(x-2)$
$2!$
, $2x(x-1) = 3(x-1)(x-2), 2x= 3x-6, x= 6$

QUESITO $5$

La capacità di un serbatoio è la stessa di quella del cilindro circolare retto di volume
massimo inscrivibile in una sfera di $2$ metri di diametro. Quale è la capacità in litri del
serbatoio?

Indichiamo con $R$ il raggio della sfera, con $r$ il raggio del cilindro e con $h$ l’altezza del
cilindro.

Il volume del cilindro è dato da: V(cilindro) $= \pi r2h$ . Ma risulta:

$r^{2} = R^{2} -$
$$ h^{2 $$
$4$ quindi: V(cilindro) $= \pi (R^{2} -$
$$ h^{2 $$
$4 ) h=$ f(h) , con $0 \le h\le 2R$

Tale volume è massimo se lo è $y= (R^{2} -$
$$ h^{2 $$
$4 ) h;$ calcoliamo la derivata prima:

## Quesito 6

Estero (Calendario australe) 2007

$y' = R^{2} -h^{2}$
$4 + h(-1$
$2 h) = -3$
$4 h^{2} + R^{2} \ge 0$ se $h^{2} \le 4$
$3 R^{2} , -R\sqrt{4}$
$3 \le h\le R\sqrt{4}$

Quindi, con le limitazioni su $h,$ la funzione è crescente se $0 < h< R\sqrt$
$3$ e decrescente se
$R\sqrt$
$3 < h< 2R:$ la funzione ha quindi un massimo (assoluto) per $h= R\sqrt$
$$ 4 3 = 2 $$
$3 R\sqrt{3}$ .
Per tale valore di $h$ si ha: $r^{2} = R^{2} -$
$$ h^{2 $$
$4 = R^{2} -$
$4$ ∙
$3 R^{2} =$
$2R^{2}$
$3 , r=$
$R\sqrt{6}$

Il cilindro di volume massimo inscritto in una sfera di raggio $R$ è quello di altezza
$3 R\sqrt{3}$
e raggio di base
$R\sqrt{6}$

Con $R=1 m$ abbiamo: $h=$
$3 R\sqrt{3} =$
$3 \sqrt{3} m$ ed $r=$
$R\sqrt{6}$
$$ 3 = \sqrt{6 $$
$3 m$
Quindi il volume del cilindro è:

V(cilindro) $= \pi r2h= \pi (\sqrt{6}$
$$ 3 ) 2 (2 $$
$3 \sqrt{3}) = \pi (4$
$9) \sqrt{3} m^{3} \cong 2.418 m^{3} \cong$ 2418 $dm3 \cong$ 2418 $l$

QUESITO $6$

Dato un tetraedro regolare, si costruisca il tetraedro regolare avente per vertici i centri
delle facce del primo. Si dimostri che ogni faccia di un tetraedro è parallela $ad$ una faccia
dell’altro.

Sia NOR una faccia del tetraedro interno. $N$ è il baricentro del triangolo ACD, quindi (per
una nota proprietà) $DN=2NJ$ (essendo $DJ$ mediana); analogamente risulta: $DO=2OK.$
Quindi, per il teorema di Talete, NO è parallelo a $JK,$ che è parallelo $ad AB:$ quindi NO è
parallelo $ad AB.$
In modo analogo si dimostra che $RO$ è parallelo $ad AC$ e che $NR$ è parallelo a $BC:$ il
piano NOR è quindi parallelo al piano ABC.

Lo stesso ragionamento può essere fatto per le
altre facce del tetraedro interno.

Osserviamo che dalla similitudine dei triangoli
DNO e DJK segue che $DN=$
$3 DJ,$ quindi:
$NO=$
$3 JK=$
$$ 2 3 ( 1 $$
$2 AB) =$
$3 AB:$ lo spigolo del
tetraedro interno è un terzo dello spigolo del
tetraedro esterno.

## Quesito 7

Estero (Calendario australe) 2007

QUESITO $7$

Si dia una definizione di “asintoto” - orizzontale, obliquo, verticale - di una curva e si
fornisca un esempio di funzione f(x) il cui grafico presenti un asintoto orizzontale e due
asintoti verticali.

Si dice che la curva $\gamma$ (eventualmente grafico di una funzione di equazione $y=f(x))$
ammette la retta $r$ come asintoto se la distanza del generico punto $P$ della curva dalla
retta $r$ tende a zero quando $P$ si allontana indefinitamente su $g.$

Per una trattazione completa sugli asintoti si veda la seguente pagina:

$http://www.matefilia.it/argomen/asintoti/asintoti.htm$

Diamo ora un esempio di funzione che presenta un asintoto orizzontale e due asintoti
verticali:

f(x) =
$$ x^{2 $$
$x^{2} -1$

Questa funzione ha l’asintoto orizzontale $y=1$ e gli asintoti verticali $x=-1$ e $y=1:$

## Quesito 8

Estero (Calendario australe) 2007

QUESITO $8$

La risoluzione di un problema assegnato conduce all’equazione $2senx + k$ cos $x = 1$
ove $k>0$ e $0 \le x\le$
$\pi$
$3$ . Si discutano le possibili soluzioni del problema.

Posto $X=cosx$ e $Y=senx,$ la nostra equazione corrisponde al seguente sistema:

$2Y+ kX= 1$
$X^{2} + Y^{2} = 1$
$k> 0$
$2 \le X\le 1, 0 \le Y\le \sqrt{3}$

$2Y+ kX= 1$ rappresenta un fascio proprio di rette con generatrici $2Y-1 = 0$ (per $k=0)$
e $X= 0$ (per $k= \infty$ ); quindi ha centro in $S= (0;$
$2)$ L’arco di circonferenza che può essere
intersecato ha estremi $A= (1,0)$ e $B=$ (
$$ 1 2 , \sqrt{3 $$
$2$ ). Cerchiamo i valori di $k$ corrispondenti alle
rette per A e $B:$

retta per A: $k=1;$ retta per $B: \sqrt{3} +$
$2 k= 1, k= 2 -2\sqrt{3} < 0$

Tenendo presente che $k>0$ abbiamo;

$1$ soluzione per $0 < k\le 1$

Con la collaborazione di Angela Santamaria
