---
tipo: soluzione
prova_stem: 2005_estero_calendario_australe_2005_sessione_suppletiv_problema1_60
pdf: Prova_Maturita_2005.pdf
source: text
title: >-
  Soluzioni —
  2005_estero_calendario_australe_2005_sessione_suppletiv_problema1_60
---
# Soluzioni — 2005_estero_calendario_australe_2005_sessione_suppletiv_problema1_60

## Problema 1

Estero (Calendario australe) 2005
Sessione $Suppletiva-$ Problema $1$

Scuole italiane all’estero (Calendario australe suppletiva ) 2005

PROBLEMA $1$

Si consideri l’equazione $y= x^{3} -ax+ b$ .

Si determinino a e $b$ in modo che la sua curva rappresentativa $\Gamma$ sia tangente, nel punto
A di ascissa $- 1,$ alla retta $r$ d’equazione $y= 4.$ Si disegni $\Gamma$ .

Il punto A ha coordinate $A=(-1; 4).$ Imponiamo il passaggio per A: $4 = -1 + a+ b.$
Deve essere $y'(-1) = 0; y' = 3x^{2} -a,$ quindi: $3 -a= 0.$ Pertanto:

${a+ b= 5$
$3 -a= 0 ; {b= 2$
$a= 3: y= x^{3} -3x+ 2$ .

La retta $r$ incontra $\Gamma$ in un altro punto $B.$ Si calcoli l’area della regione di piano delimitata
dal segmento $AB$ e da $\Gamma$ .

Cerchiamo $B:$

${y= x^{3} -3x+ 2$
$y= 4$
; $x^{3} -3x+ 2 = 4 , x^{3} -3x-2 = 0.$

Essendo $x=-1$ una radice dell’equazione, il polinomio $x^{3} -3x^{2} -2$ è divisibile per $x+1;$
utilizzando la regola di Ruffini otteniamo:
$x^{3} -3x-2 = (x+ 1)(x^{2} -x-2) = (x+ 1)^{2}(x-2) = 0$ se $x= -1$ e $x= 2.$ Quindi:
$B= (2; 4).$

Studiamo la funzione $y=$ f(x) $= x^{3}-3x+ 2= (x+ 1)^{2}(x-2)$
I tratta di una cubica (quindi definita, continua e derivabile su tutto $R)$ tangente all’asse $x$
in $x=-1,$ e passante per $(2;0).$
I limiti a +/- infinito sono +/- infinito, non ci sono asintoti.
Derivata prima: $y' = 3x^{2} -3 \ge 0$ per $x\le -1$ vel $x\ge 1:$ la funzione cresce per $x<-1$ e
$x>1$ e decresce per $-1<x<1.$ Massimo relativo $M=(-1; 4),$ minimo relativo $(1; 0).$
Sappiamo che ogni cubica ammette uno ed un solo flesso (centro di simmetria), che si
ottiene annullando la derivata seconda: y'' $= 6x= 0$ se $x= 0:$ flesso $F=(0; 2).$

Estero (Calendario australe) 2005
Sessione $Suppletiva-$ Problema $1$

Grafico della funzione con evidenziatala regione richiesta:

L’area della regione richiesta si ottiene calcolando il seguente integrale:

$Area= \int [4 -(x^{3} -3x+ 2)]$
$-1$
$= \int [2 -x^{3} + 3x]$
$-1$
$dx= [2x-1$
$4 x^{4} + 3$
$2 x^{2}]$
$-1$

$= 4 -4 + 6 -(-2 -1$
$$ 4 + 3 2) = 27 $$
$4 u^{2} = 6.75 u^{2} =$ Area

Si determini l’equazione della retta $s$ per l’origine degli assi che delimita con $\Gamma$ e con
l’asse $y$ una regione finita di piano, nel secondo quadrante, di area
$$ 5 4 . $$

La generica retta per l’origine ha equazione del tipo $y= mx$ .
La regione richiesta è indicata nella seguente figura:

Estero (Calendario australe) 2005
Sessione $Suppletiva-$ Problema $1$

Cerchiamo l’ascissa del punto di intersezione $C$ fra la curva e la retta:

$y= mx$
$y= x^{3} -3x+ 2 ; x^{3} -3x+ 2 = mx , x^{3} -(3 + m)x+ 2 = 0.$

Quindi, detta $t$ l’ascissa di $C,$ deve essere: $t^{3} -(3 + m)t+ 2 = 0, m=$
$t^{3}-3t+2$

Deve inoltre essere:

$Area= 5$
$4 = \int [x^{3} -3x+ 2 -mx]$
$$ 0 t $$
$dx= [1$
$4 x^{4} -3$
$2 x^{2} + 2x-1$
$2 mx2]$
$$ t 0 = $$

$= -1$
$4 t^{4} + 3$
$2 t^{2} -2t+ 1$
$2 mt2 = 5$

Dobbiamo quindi risolvere il seguente sistema:

$t^{3} -(3 + m)t+ 2 = 0$
$t^{4} -6t^{2} + 8t-2mt2 + 5 = 0$ ; {
$t^{3} -3t^{2} -mt+ 2 = 0$
$m=$
$t^{4}-6t^{2}+8t+5$
$2t^{2}$
; ${m=$
$t^{3}-3t+2$
; quindi:

$t^{3} -3t+ 2$
$= t^{4} -6t^{2} + 8t+ 5$
$2t^{2}$
, $2t^{4} -6t^{2} + 4t= t^{4} -6t^{2} + 8t+ 5$ ,

$t^{4} -4t-5 = 0$

L’equazione ammette la radice $x=-1;$ applicando la regola di Ruffini si ottiene:

$(t + 1)(t^{3} - t^{2} + t -5) = 0 (*)$

Osserviamo che, essendo $t<0,$ l’espressione $(t^{3} - t^{2} + t -5)$ è sempre negativa, quindi
l’unica soluzione di (*) è $t=-1.$

Il punto $C$ ha quindi ascissa $-1.$ Da $m=$
$t^{3}-3t+2$
otteniamo quindi: $m= -4$ .

La retta richiesta è quindi $y= -4x.$

Con la collaborazione di Angela Santamaria
