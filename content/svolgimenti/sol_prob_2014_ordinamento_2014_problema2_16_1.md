---
title: Ordinamento 2014 — Problema 2 — Problema 1 — Svolgimento
tipo: soluzione
item_id: sol_prob_2014_ordinamento_2014_problema2_16_1
of_item: prob_2014_ordinamento_2014_problema2_16_1
prova_id: prova_2014_ordinamento_2014_problema2_16
anno: '2014'
pdf: Prova_Maturita_2014.pdf
cluster: Studio di Funzione
tags:
  - maturita/soluzione
  - area/analisi
  - cluster/studio_di_funzione
  - anno/2014
---

**Problema:** [[Problemi/prob_2014_ordinamento_2014_problema2_16_1|Ordinamento 2014 — Problema 2 — Problema 1]] · **Prova:** [[Prove/2014_ordinamento_2014_problema2_16|Ordinamento 2014 — Problema 2]]

ORDINAMENTO 2014 - PROBLEMA $2$

f(x) $= x\sqrt{4}-x$

Anche se non richiesto esplicitamente, facciamo lo studio completo della funzione.

Dominio: $4 -x \ge 0 \implies - \le x\le$
Simmetrie notevoli: $f(-x)=-f(x),$ quindi la funzione è dispari (grafico simmetrico rispetto
all’origine).
Intersezioni con gli assi cartesiani:
$x=0, y=0$
$y=0, x=0, x=2, x=-2$
Segno della funzione: f(x) $\ge 0$ se $x\ge 0$ (nel dominio)
Limiti: la funzione è continua in un intervallo chiuso e limitato.
Derivata prima:

La funzione non è derivabile in $x= -2$ ed in $x=2.$
Il limite per $x$ che tende a $(-2)+$ oppure a $2-$ della derivata è - infinito, quindi avremo
tangenti verticali in $x= -2$ ed in $x= 2$

$f (x) \ge 0$ se $x -2 \le 0 \implies -\sqrt \le x\le \sqrt$

Quindi la funzione è crescente in tale intervallo e decrescente negli intervalli

$-2 \le x< -\sqrt{2}$ e $\sqrt{2} < x\le 2$

Avremo quindi un minimo (assoluto) in $m= (-\sqrt ; -$ ) ed un massimo (assoluto) nel
punto $M=(\sqrt$ ; )

Derivata seconda:

$f (x) \ge 0$ se $2x(x -6) \ge 0 \implies$
(fuori dominio),

Quindi il grafico ha la concavità verso l’alto nell’intervallo
, verso il basso nella
parte rimanente del dominio. Flesso nel punto $(0;0)$ con tangente inflessionale di

coefficiente angolare $f’(0)=2$

Il grafico della funzione è pertanto il seguente:

Come detto nel punto $1)$ la funzione è dispari, quindi il suo grafico è simmetrico rispetto
all’origine.

f(x) $= x\sqrt{4}-x$

$f’(0)=2,$ quindi, detto $\alpha$ l’angolo formato dalla tangente nell’origine con il semiasse
positivo delle $x,$ risulta:

$tg \alpha = 2,$ da cui $\alpha = arctg(2) \cong 63^\circ 26'$

Consideriamo la curva di equazione $y = x (4-x$ )
Posto $4 -x \ge 0,$ cioè $-2 \le x\le 2,$ otteniamo:

$y= \pm |x|\sqrt{4} -x =$ {
$\pm x\sqrt{4} -x$ se $0 \le x\le 2$
$\mp x\sqrt{4} -x$ se $-2 \le x< 0$

Il grafico è quindi quello della f(x) unito al grafico di - f(x)

L’area richiesta è data da:

$Area= 4 ∙\int f(x)dx=$

$4 ∙\int x\sqrt{4} -x dx=$

$-2 ∙\int -2x\sqrt{4} -x dx= -2 ∙[2$
$3 (4 -x$ )

$= -4$
$3 (0 -8) = 32$

h(x) $= sen(x\sqrt{4} -x$ ) con $0 \le x\le 2$

I punti del grafico di h(x) di ordinata $1$ sono quelli per cui: $sen(x\sqrt{4} -x ) = 1,$ cioè:

$x\sqrt{4} -x =$

$+ 2k\pi$ : osservando il grafico di f(x) con la limitazione imposta alla $x$ può
essere solo $k=0$ e si hanno DUE PUNTI DI INTERSEZIONE tra $y=f(x)$ ed $y=$

, quindi i
punti del grafico di h(x) di ordinata sono $2: 0 < x < \sqrt{2}$ e $\sqrt{2} < x < 2$

Per $0 \le x\le 2$ la f(x) cresce da $0$ a $2,$ e siccome $2 >$

sen (f(x)) ha il massimo assoluto $1$
quando

$x\sqrt{4} -x =$

, cioè per $x= x$ .

La f(x) decresce dal valore $2$ al valore $0,$ passando per $x= x$ dove $h(x)=1.$

Quando f(x) passa da

a $2$ h(x) decresce da $1$ a $sen(2);$ quando f(x) decresce da $2$ a

, h(x) cresce da $sen(2)$ a $1:$ quindi $sen(2)$ è minimo relativo (per $x=\sqrt$ ).
Infine quando f(x) passa dal valore

al valore $0,$ h(x) decresce da $1$ a zero: il minimo
assoluto di h(x) è quindi $0,$ assunto per $x=0$ e $x=2.$

Forniamo il grafico di h(x) $= sen(x\sqrt{4} -x$ ) con $0 \le x\le 2$

Dobbiamo ora trovare per quali valori di $k$ l’equazione $h(x)=k$ ha $4$ soluzioni distinte.

Come detto nello studio precedente sulla variazione di h(x), essa cresce da $0 ad$
$x$ (dal valore $0$ al valore $1)decresce$ da $x$ a $2$ (dal valore $1$ al valore $sen(2)),$ cresce di
nuovo da $2$ a $x$ (dal valore $sen(2)$ al valore $1)$ e decresce infine da $x$ a $2$ (dal valore $1$ al
valore $0):$

ne segue che il grafico di h(x) taglia la retta $y=k$ in $4$ punti distinti quando:

sen( ) $< k< 1$

come è messo meglio in evidenza nel grafico seguente:

Con la collaborazione di Angela Santamaria, Simona Scoleri e Stefano Scoleri

PROBLEMA $1$

Si consideri la funzione ( ) | |

Si mostri che

| | | |

| | [F. I. ]

Se ne disegni il grafico di ( ).

E’ sufficiente studiare il grafico di $y g($ ) .

Dominio:
Simmetrie notevoli: la funzione non né pari né dispari.
Intersezioni con gli assi:

$$ y y $$

Segno della funzione:

$y>$ se > > > >

Limiti:

( ) ( ) $(y$ asintoto orizz. per )

Non esiste asintoto obliquo per poiché $g($ ) , non è un infinito di ordine $1.$

Derivata prima:

$g'($ ) ; poniamo

$g ( ) \ge$ se $\ge \ge \ge$

Quindi $g ( ) \ge$ se $\ge$

$\ge$ (

La funzione è quindi decrescente per (

) e crescente per > (

) punto di minimo relativo (che è anche assoluto), di ordinata
$g($ (

$M u M$ ( (
$$ ) 9 4) $$

Derivata seconda:

g''( ) $4$

$g ( ) \ge$ se $4 \ge \ge$

$\ge$ (

La funzione è concava verso il basso se (

), verso l’alto se > (

) punto di flesso, con ordinata $g($ (

$F F$ ( (
$$ 4) 9 ) $$

Il grafico di $y$ g(x) $x x$ è il seguente:

Il grafico di ( ) $|g( )|$ si ottiene da quello di g(x) confermando la parte positiva e
ribaltando rispetto all’asse $x$ la parte negativa; abbiamo quindi il grafico seguente:

Notiamo che M’, simmetrico di $M$ rispetto all’asse delle $x,$ è massimo relativo per la $f$ e
che F’, simmetrico di $F,$ è flesso per la $f.$ In A, di coordinate ( ), $f$ non è derivabile.

Si dica se alla funzione ( ) si può applicare il teorema di Rolle nell’intervallo [ ] e il
teorema di Lagrange nell’intervallo [ ], giustificando le risposte.

( ) | | soddisfa il teorema di Rolle in [ ]
( ) è continua in [ ] e derivabile in ( ) (ricordiamo che la funzione non è
derivabile solo in > ;
rimane da verificare se la funzione assume lo stesso valore agli estremi dell’intervallo:

( ) ( ) $|4 |$ : quindi il teorema di Rolle vale.
Notiamo che nell’intervallo dato abbiamo la tangente orizzontale in M’.

( ) | | soddisfa il teorema di Lagrange in [ ]

No, il teorema di Lagrange non vale in tale intervallo, poiché, pur essendo la funzione
continua nell’intervallo chiuso, non è derivabile nell’aperto ( ), di cui $fa$ parte il punto di
non derivabilità .

Si calcoli l’area della regione finita di piano limitata da e dall’asse $x$ sull’intervallo

L’area richiesta si ottiene calcolando il seguente integrale:

$Ar \int$

$$ \int ( ) [ ] $$

$u .5 u$

Con la collaborazione di Angela Santamaria, Simona Scoleri e Stefano Scoleri

PROBLEMA $2$

I lati di un triangolo rettangolo misurano $k, 2k$ e $\sqrt{5k}$ , essendo $k$ un numero reale positivo.

Si calcoli, in gradi e primi sessagesimali, l’ampiezza degli angoli acuti del triangolo. Si
determini $k$ in modo che l’altezza relativa all’ipotenusa abbia lunghezza uguale a $2$ e si
determinino le misure delle proiezioni dei due cateti sull’ipotenusa.

$$ e k $$
$\sqrt{5k}$
$$ \sqrt{5 $$
$5$ e $(\sqrt{5}$
$$ 5 ) 2 $$

$h k∙2k$
$\sqrt{5} k$
$2k$
$$ \sqrt{5 $$
$2 k \sqrt{5}$
$AC$ AB∙AH (I teorema di Euclide) da cui:
$AH AC$
$AB k$
$\sqrt{5k}$
$$ k \sqrt{5 $$

Analogamente: $BC$ AB∙BH (I teorema di Euclide) da cui:

$BH BC$
$AB k$
$\sqrt{5k}$
$$ k \sqrt{5} 1 $$

Una retta parallela all’ipotenusa, avente distanza dal vertice dell’angolo retto, divide il
triangolo in un triangolo e un quadrilatero .
Si mostri che il rapporto tra l’area di e l’area di , in funzione di , è uguale a

Anche se non è detto esplicitamente, consideriamo il valore di $k$ trovato nel punto
precedente $k \sqrt{5}.$

Dalla similitudine fra i triangoli ABC e CDE si ha:

ED: $AB CF: CH$ ED AB∙CF
$$ CH $$
$\sqrt{5k}$ ∙
$2k$
$$ \sqrt{5} 5 2 $$
Quindi: Area( )
∙

∙

$Q$ è un trapezio la cui area si può trovare per differenza tra l’area di ABC e quella di $T:$

Area( ) Area(ABC) Area( ) AC∙BC
$$ 2 5 $$
$2k∙k$
$$ 2 5 $$
$k 5$
$$ 5 5 $$

Il rapporto richiesto è quindi:

Area( )
Area( )

$$ 5 5 $$

Si tracci il grafico di ( ) senza tener conto dei limiti geometrici.

Dominio: $2, 2 2, 2$

Simmetrie ( ) ( ): funzione pari, simmetria rispetto all’asse $y.$

Intersezioni con gli assi:

Segno della funzione:

> se $> 2 2$

Limiti:

$$ lim $$

$1 ( 1$ asintoto orizzontale; non possono esistere asintoti obliqui)

$$ lim $$

( $2$ asintoti verticali)

Derivata prima:

( ) $\ge$ se $\ge$ (nel dominio di $f)$

Quindi (nel dominio di $f)$ la funzione è crescente se $x>0$ e decrescente se $x<0.$
In $(0;0)$ abbiamo un minimo relativo.

Derivata seconda:

( ) > e $> , 2 2$ (concavità verso l’alto); no flessi.

Il grafico di ( )

è il seguente:

Si calcoli infine l’area della regione finita di piano limitata da e dalle rette di equazioni
$1, 1$ e $1$

L’area richiesta si ottiene calcolando il seguente integrale:

Area(S) $\int$ (

$1) d$

$$ 2 \int ( $$

) $d 8 \int$ (
) $d$

Cerchiamo una primitiva di

$$ A 2 B $$
$2$ A( $2) B( 2)$

(A $B) 2A 2B$

(A $B) 2A 2B 1$ { A $B$
$2A 2B 1$ {
A $B$
A $B$

Quindi:

Area(S) $8 \int$ (
) $d$

$$ 8 \int ( 1 $$

$$ 2 1 $$

$2) d$

$2[l | 2| l | 2|]$
$2[ l (l 2 l 2)] 2l u 2 2 u$

Con la collaborazione di Angela Santamaria, Simona Scoleri e Stefano Scoleri

*Fonte:* [📄 PDF p.16](https://drive.google.com/file/d/1WH26K6aDhT0rm439hBww1SX_Dwrvky9g/view)


#maturita/soluzione #area/analisi #cluster/studio_di_funzione
