---
title: 2023 Suppletiva — Problema 1 — Problema 1 — Svolgimento
tipo: soluzione
item_id: sol_prob_2023_sessione_suppletiva_2023_problema1_70_1
of_item: prob_2023_sessione_suppletiva_2023_problema1_70_1
prova_id: prova_2023_sessione_suppletiva_2023_problema1_70
anno: '2023'
pdf: Prova_Maturita_2023.pdf
cluster: Studio di Funzione
tags:
  - maturita/soluzione
  - area/analisi
  - cluster/studio_di_funzione
  - anno/2023
---

**Problema:** [[Problemi/prob_2023_sessione_suppletiva_2023_problema1_70_1|2023 Suppletiva — Problema 1 — Problema 1]] · **Prova:** [[Prove/2023_sessione_suppletiva_2023_problema1_70|2023 Suppletiva — Problema 1]]

Sessione suppletiva 2023 - Problema $1$

LICEO SCIENTIFICO SUPPLETIVA 2023 - PROBLEMA $1$

Assegnata la funzione: f(x) = a $xlnx-$
$2 x$

a)

Determinare il valore del parametro reale a in modo che $f$ abbia un punto di minimo assoluto in $x= \sqrt{e}.$
Si studi la funzione ottenuta e se ne disegni il grafico.
Si ponga, d’ora in avanti, $a= 1.$

f(x) = a $xlnx-$
$2x$

La funzione è definita per $x>0$ e per tali valori è continua e derivabile, perciò deve essere $f'(\sqrt{e}) = 0.$
f'(x) = a $ln x+$ a $x^{1}$
$x-3$
$2 ; f'(\sqrt{e}) = a∙1$
$2 + a-3$
$$ 2 = 0, 3 $$
$2 a= 3$
$2 : a= 1$
Per tale valore di a si ottiene la funzione equazione:
f(x) $= xlnx-$
$2x$
Verifichiamo che $x= \sqrt{e}$ è effettivamente un punto di minimo assoluto.
Dominio: $x> 0$
f'(x) $= ln x+ x∙1$
$x-3$
$2 = ln x-1$
f'(x) $> 0$ se: $ln x>$
$2 : x> \sqrt{e}.$
La funzione è quindi decrescente se $0 < x< \sqrt{e}$ e crescente per $x> \sqrt{e} : x= \sqrt{e}$ è punto di minimo
relativo (e assoluto).

Studiamo ora la funzione.

Dominio: $x> 0$
La funzione, come già detto, è continua e derivabile in tutto il suo dominio e non può essere né pari né
dispari (visto il dominio).
Intersezioni con gli assi:
non ci possono essere intersezioni con l’asse $y,$ essendo $x>0;$
se $y= 0,$ si ha: $xlnx-$
$2 x= 0, ln x=$
$2 , x=$ e
$2 = \sqrt{e}^{3} = e\sqrt{e}\cong 4.5$
Segno della funzione: f(x) $> 0$ se $xlnx-$
$2 x> 0$ , ed essendo $x> 0: lnx-$
$2 > 0, x> \sqrt{e}^{3}$
Limiti alla frontiera del dominio:

$$ lim $$
$x\to 0+ (xlnx-$
$2 x) = 0$ (ricordiamo che: lim
$x\to 0+(xlnx) = 0-)$
$$ lim $$
$x\to +\infty (xlnx-3$
$2 x) = +\infty$
(si ha la forma $indeterminata+ \infty -\infty$ ma xlnx è infinito di ordine superiore rispetto a $3$
$2 x)$

Controlliamo se ci può essere asintoto obliquo:

Sessione suppletiva 2023 - Problema $1$

$$ lim $$
$x\to +\infty$
f(x)
= lim
$x\to +\infty (lnx-3$
$2) = +\infty$ : non c'è asintoto obliquo .

Studio derivata prima:

f'(x) $= ln x+ x∙1$
$x-3$
$2 = ln x-1$
f'(x) $> 0$ se: $ln x>$
$2 : x> \sqrt{e}.$
La funzione è quindi decrescente se $0 < x< \sqrt{e}$ e crescente per $x> \sqrt{e} : x= \sqrt{e}$ è punto di minimo
relativo (e assoluto). Ordinata del minimo: $f(\sqrt{e}) = \sqrt{e}∙$
$2 -$
$2 \sqrt{e}= -\sqrt{e}: m= (\sqrt{e}; -\sqrt{e}).$

Osserviamo che: lim
$x\to 0+$ f'(x) = lim
$x\to 0+ (ln x-$
$2) = -\infty$ , quindi il grafico della funzione si avvicina
all’origine degli assi con tangente verticale.

Studio derivata seconda:

f''(x) =
$x> 0:$ il grafico della funzione volge sempre la concavità verso l’alto, non ci sono flessi.

Grafico:

$b)$

Si verifichi che esiste una sola retta tangente $t$ alla curva di equazione $y=$ f(x), condotta dal punto
$Q(0, -1).$ Determinare l’equazione di $t$ e le coordinate del corrispondente punto di tangenza.

Generica retta per $Q: y+ 1 = mx , y= mx-1 =$ r(x)

Tale retta è tangente al grafico di $f$ se:

{ f(x) = r(x)
f'(x) = r'(x) ,
$xlnx-3$
$2 x= mx-1$
$ln x-1$
$2 = m$
: $xlnx-3$
$2 x= (lnx-1$
$2) x-1: -3$
$2 x= -1$
$2 x-1$

Dal sistema si ottiene $x= 1$ ed $m= -$
$2$ :retta tangente $t: y= -$
$2 x-1$ ; perciò $y= -$
$2 -1 = -$
$$ 3 2 . $$
Punto di tangenza: $T= (1; -3$

Sessione suppletiva 2023 - Problema $1$

Rappresentazione grafica:

$c)$

Determinare i parametri reali $h, k$ in modo che le curve di equazioni $y=$ f(x) e $y=$
$x+h$
$x+k=$ s(x)
risultino tangenti nel loro punto comune di ascissa $1.$

f(x) $= xlnx-$
$2 x,$ quindi: $f(1) = -$
$$ 3 2 . $$

Il punto comune $ad f$ ed $s$ è quindi: $T= (1; -$
$2).$ Deve pertanto essere: $s(1) = -$
$2$ perciò:
$1 + h$
$1 + k= -3$
$2 + 2h= -3 -3k$ ,
$2h+ 3k= -5$ .

Dobbiamo ora imporre che sia: $f'(1) = s'(1).$ Ricordiamo che: f'(x) $= ln x-$
$2$ . Calcoliamo $s':$
s'(x) $= x+ k-x-h$
$(x+ k)2$
$k-h$
$(x+ k)2 ; s'(1) =$
$k-h$
$(1 + k)2 = f'(1) = -1$
$2$ , da cui:
$k-h$
$(1 + k)2 = -1$
$2k-2h= -1 -2k-k^{2}$ ,
$k^{2} + 4k+ 1 -2h= 0$ .

$2h+ 3k= -5$
$k^{2} + 4k+ 1 -2h= 0$ ; {
$h= -5 -3k$

$k^{2} + 4k+ 1 -2 (-5 -3k$
$$ 2 ) = 0 $$
$\Rightarrow k^{2} + 7k+ 6 = 0: k= -6,$
$k= -1$

Se $k= -1$ si ha: $h= -1 , y=$
$x+h$
$x+k= y=$
$x-1$
$x-1 = 1$ (con $x\ne 1):$
non accettabile perchè il punto comune alle due curve ha ascissa $1.$

Se $k= -6$ si ha: $h=$
$$ 13 $$
$2 , y=$
$x+13$
$x-6$ .

Quindi i valori di $h$ e $k$ richiesti sono: $h=$
$$ 13 $$
$2$ e $k= -6.$

Anche se non richiesto rappresentiamo graficamente le due funzioni, dopo aver notato che s(x) è una
funzione omografica di centro $(6; 1),$ asintototi: $x= 6$ e $y= 1$ , grafico passante per $T= (1; -$
$$ 3 2). $$

Sessione suppletiva 2023 - Problema $1$

$d)$

Studiare la funzione
g(x) $= \int$ f(t)
$$ x 1 dt $$
dopo averne scritta l’espressione analitica. Determinare l’equazione della retta tangente al grafico di $g$ nel
suo punto di ascissa $x=$ e.

g(x) $= \int$ f(t)
$$ x 1 $$
$dt= \int (tlnt-3$
$2 t)$
$$ x 1 dt $$

Cerchiamo una primitiva di tlnt integrando per parti:

$\int tlntdt= \int (t^{2}$
'
$lntdt= t^{2}$
$2 lnt-\int t^{2}$
$2 ∙1$
$tdt= 1$
$2 t^{2} ln t-1$
$4 t^{2} + c$

Quindi:

$\int (tlnt-3$
$2 t)$
$$ x 1 $$
$dt= [1$
$2 t^{2} ln t-1$
$4 t^{2} -3$
$4 t^{2}]$
$$ 1 x = [1 $$
$2 t^{2} ln t-t^{2}]$
$$ 1 x = 1 $$
$2 x^{2} ln x-x^{2} -(-1):$

g(x) $= 1$
$2 x^{2} ln x-x^{2} + 1$

Studio della funzione.

Dominio: $x>0;$ né pari né dispari, non può intersecare l’asse $y$ (essendo $x>0);$ intersezioni con l’asse $x:$
$2 x^{2} ln x-x^{2} + 1 = 0,$
$2 x^{2} ln x= x^{2} -1:$ sicuramente abbiamo la soluzione $x=1,$ perché per tale valore
gli estremi dell’integrale che definiscono $g$ sono uguali. Per altre eventuali intersezioni con l’asse $x$ e per lo
studio del segno della funzione si potrebbe ricorrere al metodo grafico, ma aspettiamo lo studio della
derivata prima, che probabilmente ci fornirà delle informazioni al riguardo.

Limiti:

$$ lim $$
$x\to 0+$ (
$2 x^{2} ln x-x^{2} + 1) = 1$ ( ricordiamo che lim
$x\to 0+xalnbx= 0 \forall a> 0$ e $b> 0).$

Sessione suppletiva 2023 - Problema $1$

$$ lim $$
$x\to +\infty$ (
$2 x^{2} ln x-x^{2} + 1) = +\infty$

(ci sarebbe la F.I. $+\infty -\infty$ ma
$2 x^{2} ln x$ è infinito di ordine superiore rispetto a $x^{2}).$
Verifichiamo se c’è asintoto obliquo. Si può subito dire che non c’è perché per $x\to + \infty$ , per quanto notato
sopra, la parte principale della funzione è
$2 x^{2} ln x$ , che è un infinito di ordine superiore a $1,$ mentre è
necessario che sia un infinito di ordine $1,$ dovendo essere: lim
$x\to +\infty$ (
$2x^{2} ln x-x^{2}+1$
) $= m\ne 0.$ In effetti si ha:
$$ lim $$
$x\to +\infty$ (
$2 x^{2} ln x-x^{2} + 1$
) = lim
$x\to +\infty (1$
$2$ xln $x-x+ 1$
$x) = +\infty$ .

N.B. Per un approfondimento sugli asintoti di una funzione si veda questa pagina di matefilia.it:

$https://www.matefilia.it/argomen/asintoti/asintoti.htm$

Studio derivata prima:

Per il Teorema di fondamentale del calcolo integrale si ha: g'(x) = f(x) $= xlnx-$
$2 x$ .
Risulta: g'(x) $\ge 0$ se $xlnx-$
$2 x\ge 0, x(lnx-$
$2) \ge 0$ se $lnx-$
$2 \ge 0$ (ricordiamo che $x> 0)$
$lnx\ge$
$2 , x\ge$ e
$2 = e\sqrt{e}.$ Pertanto:

se $0 < x< e\sqrt{e}$ la funzione è decrescente
se $x= e\sqrt{e}$ la derivata si annulla (punto stazionario)
se $x> e\sqrt{e}$ la funzione è crescente

Concludiamo che $x= e\sqrt{e}$ è punto di minimo relativo (e anche assoluto) che vale:
$g(e\sqrt{e}) =$ (
$2 x^{2} ln x-x^{2} + 1)$
$x=\sqrt{e}^{3} =$
$2 e^{3}$ ∙
$2 -e^{3} + 1 = 1 -$
$4 e^{3} \cong -4.0$ .

Calcoliamo il coefficiente angolare della tangente lungo la quale il grafico si avvicina al punto $(0; 1):$
$$ lim $$
$x\to 0+(g'(x)) =$ lim
$x\to 0+ (xlnx-3$
$2 x) = 0:$ tangente di avvicinamento orizzontale.
Il minimo relativo (e assoluto) ha quindi coordinate: $m= (e\sqrt{e}; 1 -$
$4 e^{3}).$

Osserviamo che il calcolo dei limiti e lo studio della derivata prima ci consentono di dire che il grafico
della funzione interseca l’asse delle $x,$ oltre che in $1,$ in un altro (solo) punto di ascissa maggiore di $e\sqrt{e}.$

Studio derivata seconda:

g'(x) $= xlnx-$
$2 x$ , g''(x) $= ln x+ 1 -$
$2 = ln x-$
$2 \ge 0$ se $ln x\ge$
$2 , x\ge \sqrt{e}.$
Se $0 < x< \sqrt{e}:$ concavità verso il basso
Se $x> \sqrt{e}:$ concavità verso l’alto.

$x= \sqrt{e}$ punto di flesso con ordinata $g(\sqrt{e}) =$ (
$2 x^{2} ln x-x^{2} + 1)$
$x=\sqrt{e}=$
$2$ e∙
$2 -e+ 1 = 1 -$
$4 e\cong -1:$
Flesso: $F= (\sqrt{e}; 1 -$
$4$ e).

Sessione suppletiva 2023 - Problema $1$

Il grafico della funzione g(x) =
$2 x^{2} ln x-x^{2} + 1$ è quindi il seguente:

Determiniamo ora l’equazione della retta tangente al grafico di $g$ nel suo punto di ascissa $x =$ e.

g(e) =
$2 e^{2} -e^{2} + 1 = 1 -$
$2 e^{2}: P=$ (e; $1 -$
$2 e^{2})$

g'(e) = f(e) $= e-3$
$2 e= -1$
$2$ e.

La tangente richiesta ha quindi equazione:

$y-1 + 1$
$2 e^{2} = -1$
$2 e(x-e)$ ,
$y= (-1$
$2$ e) $x+ 1$ .

Con la collaborazione di Angela Santamaria

*Fonte:* [📄 PDF p.70](https://drive.google.com/file/d/1SlPvapgs-eNHrpZYtGGUXCPHVcacwdKR/view)


#maturita/soluzione #area/analisi #cluster/studio_di_funzione
